## 1. Schema: StageLoopSchema discriminated union

- [x] 1.1 In `src/core/pipeline-registry/types.ts` (current `StageLoopSchema` at line ~170), convert the single `z.object({ kind: z.literal('review-cycle'), maxRounds })` into `z.discriminatedUnion('kind', [...])` with two variants: the existing `review-cycle` object (shape byte-for-byte identical — parsed output must remain `{ kind: 'review-cycle', maxRounds: 3 }`), and a new `goal` object per planning-context.md: `kind: z.literal('goal')`, a required `gate` discriminated union (`measure` with `command`(optional in schema), `threshold`, `target`, `direction: gte|lte` default gte, `timeoutSec` default 120; `evaluate` with `goal`, optional `rubric`), `maxRounds` default 5, `loopStallLimit` default 2 (NOT `stallLimit`, NOT `measureStallLimit`), `runArtifact` default `goal-run.json`. Add a `.superRefine` rejecting a measure gate that has neither `threshold` nor `target`. Update the doc comment (currently "Currently only the 'review-cycle' kind is supported").
- [x] 1.2 Verify the derived `StageLoop` type still compiles for existing consumers; confirm no `.loop?.kind === 'review-cycle'` narrowing exists in `src/` (planning-context grep-verified none — `loop` is consumed only via `if (stage.loop)` + playbook prose). Run `pnpm tsc` (or the repo's typecheck) to confirm the union narrows cleanly.

## 2. Run-state additions (additive)

- [x] 2.1 In `src/core/pipeline-registry/run-state.ts`, add two optional fields to the `RunStateSchema` object (which is already `.passthrough()`): `loopConfig` (kind `'goal'`; gate union of measure `{kind, command, threshold?, target?, direction}` / evaluate `{kind, goal, rubric?}`; `maxRounds`, `loopStallLimit`, `workProduct: 'code'|'prose'`) and `loopProgress` (kind `'goal'`; `round`, `lastScore?`, `measurePassed?`, `evaluateSatisfied?`, `stallStreak`, `historyRef` → `goal-run.json`). Both optional so existing run-state files and `parseRunState`/`readRunState` are unaffected.

## 3. Orchestration playbook: generalize Step E + add Step L

- [x] 3.1 In `src/core/templates/workflows/_orchestration.ts`, generalize the Step E preamble (currently at line ~94, "Step E — The review -> fix loop") so the LEAD narrows on `loop.kind`: `review-cycle` runs the existing review→fix protocol UNCHANGED; `goal` runs the new Step L. Preserve the review-cycle path verbatim — do not alter its 5 numbered steps.
- [x] 3.2 Append a new **Step L — Goal-loop** section to the `ORCHESTRATION_PLAYBOOK` template string (after Step K / before Step F ordering is fine — match the existing step-letter convention). It SHALL define: **Inject** (once before round 1: read `goal-plan.md`, merge concrete gate config into `iterate.loopConfig` in run-state; assert a measure gate has its `command`); **Each round** (dispatch the implementer warm-reused across rounds — round-1 seed = goal-plan.md, round-N>1 seed = plan + prior round's judgment; then run the gate — measure: run `gate.command`, parse `{score, passed, detail}`; evaluate: dispatch a FRESH reviewer worker ≠ implementer returning structured `{satisfied: boolean, gaps: string[]}`); **measure failure branch** (non-zero exit / timeout / unparseable JSON → record `{round, error}`, treat not-passed, feed stderr as gap; no deadlock); **Record** (`{round, score?, measurePassed?, evaluateSatisfied?, detail?, gaps?, error?, gitTreeFingerprint}` appended to `goal-run.json`); **Stop** (satisfied → tail; `maxRounds` exhausted → tail marked `outcome: maxRounds-exhausted`, never lie about success); **Stall** (gate-neutral: round progresses if measure score moved favorably or evaluate gap-set shrank/newly satisfied; round 1 counts as progress; `loopStallLimit` consecutive non-progressing → LEAD strategy review); **Resume** (authoritative = `goal-run.json` last record: satisfied → tail; not-passed complete round → lastRound+1; no record → round 1; MAY re-run gate once on current tree before resuming); **Context/handoff** (implementer warm-reused; on context fill follows standard Step H.3; research pipeline sets lower implementer threshold for earlier relay).

## 4. Pipeline display generalization

- [x] 4.1 In `src/commands/pipeline.ts` at line ~641, generalize `if (stage.loop) meta.push(\`loop=${stage.loop.kind}(max ${stage.loop.maxRounds})\`)` to also render goal-loop gate info. After narrowing on `loop.kind`, for `review-cycle` keep the existing `loop=review-cycle(max N)` label; for `goal` render the gate kind (e.g. `loop=goal[measure](max N, stall L)` or `loop=goal[evaluate](max N, stall L)`). Use the narrowed variant's `maxRounds`/`loopStallLimit`/`gate.kind`.

## 5. Three backend pipeline YAMLs (auto-discovered)

- [x] 5.1 Create `pipelines/goal-loop-measure/pipeline.yaml`: `define-goal` (skill `openspec-goal-plan`, role planner, requires [], gate true) → `iterate` (skill `openspec-goal-iterate`, role implementer, requires [define-goal], loop `{kind: goal, gate: {kind: measure}, runArtifact: goal-run.json}`) → `ship` (skill `openspec-opsx-ship`, role shipper, requires [iterate], gate true, model sonnet) → `archive` (skill `openspec-archive-change`, role shipper, requires [ship], model sonnet). Match the shape of `pipelines/small-feature/pipeline.yaml`.
- [x] 5.2 Create `pipelines/goal-loop-evaluate/pipeline.yaml`: same as measure but `gate: {kind: evaluate}` in the iterate loop.
- [x] 5.3 Create `pipelines/goal-loop-research/pipeline.yaml`: `gate: {kind: evaluate}` in the iterate loop; iterate is prose-flavored; tail is a `report` stage (skill `openspec-goal-report`, role shipper, requires [iterate]) INSTEAD of ship/archive; set pipeline-level `handoff: { roles: { implementer: { threshold: 0.35 } } }` for earlier relay. Verify all three are auto-discovered via `openspec pipeline list --json`.

## 6. Skill templates (3 goal skills + goal command)

- [x] 6.1 Create `src/core/templates/workflows/goal-plan.ts` exporting `getGoalPlanSkillTemplate()` returning a `SkillTemplate` named `openspec-goal-plan` (planner role; input = task description; output = `goal-plan.md` with `goal`, `gate` (measure XOR evaluate), `workProduct` (code|prose), `maxRounds`; does NOT produce proposal/design/specs). Follow the same file/module pattern as `src/core/templates/workflows/review-cycle.ts`.
- [x] 6.2 Create `src/core/templates/workflows/goal-iterate.ts` exporting `getGoalIterateSkillTemplate()` returning a `SkillTemplate` named `openspec-goal-iterate` (implementer role; work-product-aware dispatch: code → edit toward goal + MAY self-run measure informally; prose → research inline + write/refine). MUST state the flat-hierarchy invariant (never spawn child subagents) and the Step H.3 self-handoff contract (write handoff doc, return HANDOFF when context fills).
- [x] 6.3 Create `src/core/templates/workflows/goal-report.ts` exporting `getGoalReportSkillTemplate()` returning a `SkillTemplate` named `openspec-goal-report` (shipper role; research pipeline's report tail; summarizes `goal-run.json` into a final report artifact; no code to ship).
- [x] 6.4 Create `src/core/templates/workflows/goal-command.ts` exporting `getGoalCommandSkillTemplate()` (SkillTemplate named `openspec-opsx-goal`) and `getOpsxGoalCommandTemplate()` (CommandTemplate for `/opsx:goal`), mirroring `src/core/templates/workflows/auto.ts`'s structure. Instructions: run LEAD pre-flight + classification (keywords: score|latency|optimize|lighthouse|benchmark|p99|memory|throughput → measure; rubric|quality|clean|standard|refactor-quality → evaluate; research|investigate|write report/brief|autoresearch|literature → research), pick ONE backend pipeline (explicit `measure|evaluate|research` selector or `--pipeline goal-loop-<variant>` wins), then embed `ORCHESTRATION_PLAYBOOK` and drive it.

## 7. Skill/command registration

- [x] 7.1 In `src/core/shared/skill-generation.ts`, add the four new skill templates to the `workflowSkills` array in `getSkillTemplates()` (line ~151): `openspec-goal-plan`, `openspec-goal-iterate`, `openspec-goal-report`, `openspec-opsx-goal` (each with a `dirName` and `workflowId`). Add the `/opsx:goal` command to the `all` array in `getCommandTemplates()` (line ~210). Import the new factory functions.
- [x] 7.2 In `src/core/templates/skill-templates.ts`, re-export the new factory functions from their workflow modules (matching how `review-cycle` and `auto` are re-exported).

## 8. Unit tests

- [x] 8.1 In `test/core/pipeline-registry/pipeline.test.ts`, ADD goal-loop parsing cases (do NOT modify the existing review-cycle assertion at line ~74 — it must stay green): a goal loop with a measure gate parses and narrows; a goal loop with an evaluate gate parses; a goal loop with a measure gate missing both `threshold` and `target` is rejected by the superRefine; the existing `{ kind: 'review-cycle', maxRounds: 3 }` default still equals the parsed review-cycle loop.
- [x] 8.2 In `test/core/pipeline-registry/builtins.test.ts`, add `goal-loop-measure`, `goal-loop-evaluate`, and `goal-loop-research` to the set of pipelines that must parse, pass all validators (load + reparse, acyclic, skills referenced exist, requires references resolve). Note: the 3 new pipelines reference the 3 new skills, so this test also proves skill registration (task 7) is complete — run after 7.1. Confirm `goal-loop-research` is NOT decompose-free-asserted (only small/bug-fix are).
- [x] 8.3 Add a schema test for the `goal` gate: a measure gate with a stop condition validates; an evaluate gate validates; a `gate` attempting to combine measure and evaluate is rejected by the discriminated union; `loopStallLimit` defaults to 2 and `maxRounds` defaults to 5 when omitted.

## 9. Verification (no regression)

- [x] 9.1 Run `pnpm test` (or the repo's test command). Confirm the existing `full-feature`/`small-feature`/`bug-fix` built-in pipeline tests and the `review-cycle` loop test pass with zero behavior change. Investigate any Windows CLI-spawning flakiness per memory (EBUSY rmdir / 10s timeout on spec/artifact-workflow tests) by isolated re-run, not as a logic regression.
- [x] 9.2 Run `openspec pipeline list --json` and confirm all three `goal-loop-*` pipelines appear; run `openspec pipeline show goal-loop-measure --json` and confirm a valid DAG with the goal loop rendered; run `openspec validate` and confirm the package pipelines (including the 3 new ones) validate.
- [x] 9.3 Cross-platform check: confirm no hardcoded path separators were introduced in the new YAML/template/test files (pipelines use forward-slash YAML keys; TS uses `path.join`); the new pipeline YAMLs are data files discovered via `path.join` in the registry, which is already cross-platform.
