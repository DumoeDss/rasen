# telemetry-rollups-dashboard

Permanent telemetry retention via D1 daily rollups (cron aggregation from Analytics Engine before its ~90d expiry) + dashboard v2: time ranges incl. all-history, command/version/os filters, hide-test-traffic toggle, richer charts
