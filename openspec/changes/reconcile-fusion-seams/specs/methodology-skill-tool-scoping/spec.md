# methodology-skill-tool-scoping — Delta

## REMOVED Requirements

### Requirement: domain-modeling allowed-tools scoped to its write actions

**Reason**: The `domain-modeling` skill is removed from the roster in this change (user decision 2026-07-07); a tool-scoping requirement for a deleted skill has no live subject. The `codebase-design` scoping requirement in this capability is unaffected.
