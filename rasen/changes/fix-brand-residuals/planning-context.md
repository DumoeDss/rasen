# Planning Context — fix-brand-residuals

Seeded by the LEAD, 2026-07-10. Source of truth for the propose stage. INVESTIGATE-FIRST run (user: "先排查再工作").

## User intent (verbatim)

"开始修复，subagent全部使用sonnet模型，由你推进，先排查再工作。" — fix the residual findings from the independent brand sweep (sweeper1, 2026-07-10, post specs-brand-rewrite), investigating the two behavioral questions BEFORE deciding the fix shape.

## Confirmed findings to fix (brand-token residuals, behavior-neutral)

1. **A1 `rasen/specs/cli-completion/spec.md:316`** — `isOpenSpecProject(): boolean` described as checking "Rasen-enabled". Sweeper verified NO such method exists in `src/core/completions/completion-provider.ts` (only `getChangeIds()`/`getSpecIds()`) — it is a fictional interface sketch remnant. Rewrite token to `isRasenProject()` (still fictional; if the planner judges the whole sketch stale vs actual provider API, adjudicate: minimal token fix vs aligning the sketch — lean minimal, semantics-neutral).
2. **A2 `rasen/specs/cli-update/spec.md` ~8 sites (L24/30/59/87/112/118/124/131)** — "OpenSpec-managed"/"OpenSpec managed markers"/literal `<!-- OPENSPEC:START/END -->` describing the CURRENT update mechanism. Brand layer: "Rasen-managed". BUT see F5 below — the mechanism claim itself may be stale; investigation decides whether this is a token swap or a semantic correction.
3. **B1 `rasen/config.yaml:9`** — active governance config: "Write OpenSpec proposals and specs..." → "Write Rasen proposals...". One word. NOTE: config.yaml may be read by CLI (workspace config) — check nothing parses that sentence programmatically (it's prose in a YAML value).

## Investigation questions (MUST be answered in design.md with file:line evidence before fix shape is final)

- **F5**: Does ANY marker-scoped partial update mechanism exist in the current update path? Sweeper's evidence: `src/core/update.ts:252` whole-file `FileSystemUtils.writeFile`; `OPENSPEC_MARKERS` (`src/core/config.ts:16`) commented "recognized ONLY to identify pre-existing legacy artifacts ... Never written into new content"; `gemini.ts` formatFile() writes no markers. BUT check the CONFIG-file path too (CLAUDE.md/AGENTS.md-style tool config files historically used marker blocks — `legacy-cleanup.ts` removeMarkerBlock, `ensureClaudeAgentTeams`?) — the cli-update spec's marker language may be a true description of an OLDER mechanism that partially survives for some tools, or fully stale. Answer per-tool if the truth differs per tool (the 8 sites span CodeBuddy/Factory Droid/Codex/Copilot/Gemini/iFlow).
  - If mechanism fully gone: those spec lines need SEMANTIC correction (describe whole-file regeneration + preservation semantics as actually implemented), which is a REAL spec behavior-description fix → belongs in a MODIFIED delta for the owning capability, not a silent token swap.
  - If partially alive: brand-swap the token, keep mechanism text where true, correct where false.
- **F6**: `src/core/completions/installers/{bash,zsh,powershell}-installer.ts` hardcode `# OPENSPEC:START`/`# OPENSPEC:END` and WRITE them into newly generated shell profile blocks (e.g. powershell-installer.ts:219) — violating `rasen-cli-identity/spec.md:59` "SHALL NOT be written into newly generated content". Verify, then fix shape:
  - Code fix (lean): installers write `# RASEN:START/END` in new blocks; MUST still RECOGNIZE legacy `# OPENSPEC:*` blocks for replace/uninstall (upgrade path — otherwise re-install duplicates the block in existing profiles). Check for tests covering installers; add regression tests (write-new uses RASEN, upgrade replaces legacy OPENSPEC block, uninstall removes either).
  - Confirm whether these installers share/duplicate `OPENSPEC_MARKERS` or define their own literals (sweeper: independent definitions — flag the duplication in design, fix minimally, don't refactor beyond need).

## Investigation results (planner, filed after artifact creation)

**F5 — ANSWERED: fully stale, no per-tool nuance.** The marker-scoped partial-update mechanism described in `cli-update/spec.md`'s 8 flagged sites does not exist for ANY of the 6 named tools (CodeBuddy, Factory Droid, Codex, GitHub Copilot, Gemini CLI, iFlow). Evidence: `update.ts:224`/`:252` write skill/command files via whole-file `FileSystemUtils.writeFile()`; all 6 tool adapters' `formatFile()` in `src/core/command-generation/adapters/` build content purely from the fresh template, never reading the existing file or referencing a marker constant; `config.ts:14-15`'s own comment says the marker is "Never written into new content." While reconstructing the full `Slash Command Updates` requirement for the MODIFIED delta, found the SAME false claim in 3 more scenarios the sweep didn't flag (Antigravity, Windsurf, Kilo Code) — verified their adapters too (no marker wrapping) and fixed those as well for internal consistency (same requirement block, same delta, no scope expansion). Fix: MODIFIED delta on `cli-update` rewriting all 11 affected scenarios to describe whole-file regeneration (Gemini CLI keeps "preserve TOML framing, regenerate body" since that structural preservation is real). Separately flagged but NOT fixed: the same spec's root-`AGENTS.md`/`CLAUDE.md` "stub" scenarios describe a feature with zero implementation trace in `src/` — bigger, separate staleness issue, left as a follow-up.

**F6 — ANSWERED: confirmed violation in 3 independent places.** `bash-installer.ts:17-20`, `zsh-installer.ts:17-20` each independently define `# OPENSPEC:START`/`# OPENSPEC:END` (not sharing `config.ts`'s `OPENSPEC_MARKERS`) and route through `file-system.ts:240-276`'s `updateFileWithMarkers()`, which writes that literal into brand-new content when no existing block is found. `powershell-installer.ts:217-223` inlines a third independent literal directly in `configureProfile()`, plus a 4th duplicate in `removeProfileConfig()`'s removal search. Fix shape: write `# RASEN:START/END` for new blocks in all 3 installers; `updateFileWithMarkers()` gains optional legacy-pair recognition so an existing legacy `OPENSPEC` block is still found and replaced in place (no duplicate); uninstall removes either marker family. Existing tests in `test/core/completions/installers/{bash,zsh,powershell}-installer.test.ts` currently assert the bug (OPENSPEC on fresh-install paths) and need flipping; upgrade/uninstall regression tests are new additions (tasks.md section 5).

**Delta strategy landed:** 3 delta specs — `cli-update` (MODIFIED, the marker-mechanism rewrite), `cli-completion` (MODIFIED, `isOpenSpecProject`→`isRasenProject` token only, full requirement block copied per convention), `rasen-cli-identity` (MODIFIED, adds 3 new scenarios for the F6 dual-marker-recognition behavior — existing 3 scenarios untouched). `config.yaml` B1 fix is a plain non-delta task (section 3). All 4 artifacts complete; `rasen validate fix-brand-residuals --strict` exits 0; `rasen validate --specs --strict` on the main corpus (unaffected by the uncommitted delta) passes 116/116.

## Scope guards

- OUT: the F1-F4 ledger items from specs-brand-rewrite's design.md (rasen-cli-identity phase-1 wording, other OPENSPEC_* env vars, capability folder renames, command-generation category example) — do NOT pull them in.
- OUT: explorations/office-hours historical docs (legitimate historical records).
- Spec edits follow specs-brand-rewrite conventions: K1-K7 keep-classes (archived change 2026-07-10-specs-brand-rewrite/design.md); `spec-brand-consistency` governance spec now exists — this change should leave the corpus passing its grep gate.
- Delta mechanics: `rasen validate` requires ≥1 delta. F6 code fix likely lands a delta on `rasen-cli-identity` or `cli-completion` (planner adjudicates: the marker-write prohibition already exists as a requirement — a fix bringing code INTO conformance needs no delta per the conformance precedent, so the delta-of-record may be A2's semantic correction (MODIFIED on cli-update's owning capability) or an ADDED completions-installer marker requirement).
- Worktree run: all work in an isolated worktree branch from local HEAD; tests need `pnpm install` there first (LEAD prepares). Windows EBUSY flake is known non-regression.
- CLI: `node "E:/AI/ChatAI/Agents/VibeCodingProjects/workflow/Reference/OpenSpec-code/dist/cli/index.js"` (main-tree dist) — but for CODE changes, build INSIDE the worktree (`pnpm build`) and use the worktree's dist for validation there.
- Shared-tree: another session is active on main; commits pathspec-scoped always.
- Runtime directive (user): ALL workers sonnet this run, planner included.
