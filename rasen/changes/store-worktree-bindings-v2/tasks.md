## 1. Target Lines

- [ ] 1.1 Add `src/core/store/target-lines.ts` with `add` / `set-ref` / `list` / `show` / `resolve` over the target-line catalog contract the planning foundation already validates, resolving Store and per-project code locators to concrete refs and commit identities at use time.
- [ ] 1.2 Make authoring explicit and non-inferring: refuse to overwrite an existing line id, never derive a line from a branch name or the current checkout, and refuse removal of a locator a live Change depends on, naming that Change.
- [ ] 1.3 Implement the re-point gate so an operation naming a target line other than the Change's own is refused with both lines named, and the current checkout never re-points a Change.
- [ ] 1.4 Port `test/core/store/target-lines.test.ts`. Beyond the happy paths, assert each refusal is refused **for its own reason**: prove the overwrite refusal is not merely the id-validation refusal, and that the re-point refusal fires on selector disagreement rather than on an unresolvable ref.

## 2. Workspace Module (whole, all 13 files)

- [ ] 2.1 Add `src/core/store/workspace/{types,dependencies,identity,scope,locks,binding,registry,plan,apply,cleanup,diagnostics,module,index}.ts` at the 0.1.7 tip state, which per design Decision 3 is the squash base plus both mandated fixups. Keep the file layout byte-comparable to `origin/dev/0.1.7`; do NOT extract the two string constants the next child takes out of `binding.ts`.
- [ ] 2.2 Confirm before writing that the whole module compiles against this line's imports only — `utils/file-system`, `canonical-json`, `change-metadata`, `file-state`, `global-config`, `project-registry`, `store/{errors,foundation,identity-types,registry,target-lines}`, `store/{planning-foundation,planning-identity,planning-layout-v2}`, node builtins, `yaml`. If any import reaches `store-planning/`, `layout-migration/`, `issues/`, or `query/`, stop and report it rather than porting that slice in.
- [ ] 2.3 Fold in `2026-08-09-fix-existing-change-workspace-binding` (`apply.ts` +78, `module.ts` +41): applying a plan for an already-created Change completes the binding to bound with a verified pair identity when both worktrees are present, leaves it prepared when the execution identity is missing, refuses on drift, and stays idempotent.
- [ ] 2.4 Fold in `2026-08-10-fix-workspace-claim-portability` (`dependencies.ts`, the atomic coordination-write block): exact-match claim resumption, retained-but-refused on changed or replaced state, mandatory journal authority, exact Windows filesystem identity, and directory-durability degradation only for proven-unsupported outcomes.
- [ ] 2.5 Add the additive `fileLockOwnerIsProvablyDead` predicate to `src/core/file-state.ts` and use it for the read-only lock probe, so a lock the acquirer would steal is never reported as a blocking holder. Do not change any existing exported behavior in that file.

## 3. Binding, Locks, and Refusal

- [ ] 3.1 Implement the four-carrier authority order — committed Change identity is portable authority, the two per-worktree markers are local locators, the machine index is a re-verified rebuildable cache that is never authority alone — and prove the index is re-verified on every use rather than trusted.
- [ ] 3.2 Implement the closed conflict taxonomy so every disagreement refuses with its own named code rather than a shared generic failure, and nothing is silently corrected. Assert the codes are distinguishable: a marker/metadata contradiction and an unresolvable target line must not produce the same code.
- [ ] 3.3 Implement the four semantic lock kinds in fixed acquisition order with bounded retry, retrying contention and never retrying a semantic conflict. Prove the two are actually distinguished — a semantic conflict must be shown NOT to consume the retry budget, not merely to fail.
- [ ] 3.4 Implement the non-mutating preparation rule: a reused worktree on another ref is refused rather than switched, uncommitted work is left byte-identical, and an occupied destination is refused. Assert the working tree is unchanged after each refusal, not only that the call threw.

## 4. Cleanup and the Git Verb Guard

- [ ] 4.1 Implement cleanup as plan then apply, removing a worktree only on proof of all six preconditions (recorded worktree, recorded ref, clean tree, branch commits reachable from the recorded integration or target ref, no live session, no held lock), resuming from its recorded phase after interruption, and never deleting a branch, merging, resetting, or forcing a removal.
- [ ] 4.2 Add the explicit allowed-Git-verb constant and the source guard that reads the workspace adapter and fails naming any forbidden verb. Per the repo rule, the allowed set is a named constant, not a pattern.
- [ ] 4.3 Mutation-prove the verb guard: introduce a forbidden verb (e.g. `reset`) into the adapter, record that 4.2 goes RED and names the verb and the file, revert byte-exactly, record it GREEN. Record both observations in the evidence directory. A guard never observed to fail is not evidence.

## 5. Command Surface

- [ ] 5.1 Add `rasen store workspace plan|apply|show|cleanup` in `src/commands/workspace.ts`, with a machine-readable form whose content matches the human form, printing scoped commit suggestions while staging, committing, fetching, and pushing nothing.
- [ ] 5.2 Add `rasen store target-line add|set-ref|list|show` in `src/commands/store-target-line.ts` on the same terms.
- [ ] 5.3 Make `store workspace show` the full audit surface required by the spec: both roots, both worktree identities, both refs and head commits, the target line, the Change instance, the pair identity, the binding state, and every verification finding — reported as inert locators, with drift reported and never repaired.
- [ ] 5.4 Register both groups in `src/cli/index.ts`, `src/core/completions/command-registry.ts`, and all three locale trees (`src/locales/{en,ja,zh-cn}.json`) in ONE step, and assert parity by count across the three locale files rather than by inspection — this repo has repeatedly shipped these out of lockstep.

## 6. Verification

- [ ] 6.1 Add pinned-literal anchors for all six digest sites named in design Decision 4 — `plan.ts:794`, `cleanup.ts:170` and `:251`, `locks.ts:128`, `registry.ts:201`, `binding.ts:93`, `dependencies.ts:343`. Each anchor pins its OWN literal inputs and its expected value; no anchor may be computed from another anchor's output, or one break smears across all of them instead of localising.
- [ ] 6.2 Strengthen the ported "equal inputs produce an identical plan" case, which as written compares two derivations to each other and is therefore blind to any uniform change of preimage or serialization: it must also assert the token equals the pinned value for those exact inputs.
- [ ] 6.3 Make each anchor walk back through the value's own reader or verifier (plan token through plan revalidation, registry digest through index verification, carrier digest through the snapshot reader), so a preimage change cannot hide behind a verifier that stopped checking. Prove each anchor discriminates by mutating the preimage once and recording RED, then reverting.
- [ ] 6.4 For each of the four lock kinds, name its taker in shipped code. Record `change` and `integration` as deliberately unenforced-by-design in this child because their taker is the deferred finalization slice. Do NOT treat an assertion that the order array is well-formed as evidence that the ordering is enforced.
- [ ] 6.5 Port the test surface: `test/core/store/{target-lines, workspace-apply, workspace-atomic-write, workspace-baseline, workspace-binding, workspace-cleanup, workspace-git-verb-guard, workspace-identity, workspace-locks, workspace-pairing, workspace-plan, workspace-windows-paths}.test.ts`, `test/commands/{workspace-cli, store-target-line-cli, store-v2-workspace-concurrency, store-v2-workspace-journey}.test.ts`, and `test/helpers/store-workspace-fixture.ts`. Also check `test/core/workspace-migration.test.ts` and report whether it belongs to this child. `test/commands/context-workspace.test.ts` is NOT ported — it belongs to the carved-out session projection (design Decision 2).
- [ ] 6.6 Any ported case that requires the deferred finalization slice (the journey suite's archive-eligibility assertions are the likely ones) must be handled explicitly: defer that CASE with a recorded reason and author finalization-free substitute coverage for the behavior it protected. Do not silently drop it, and do not pull `finalization/` into this child to satisfy it.
- [ ] 6.7 Record that `test/core/store/workspace-atomic-write.test.ts` runs and passes, and state in the evidence that its production caller arrives with the finalization slice (design Decision 3) — so the situation is visible to a reviewer rather than discovered by one.
- [ ] 6.8 Run the focused suites from 6.5 and record the exact command and counts. Then run the compatibility suites this change touches: `test/core/file-state*.test.ts`, `test/commands/store.test.ts`, `test/core/store/` as a whole, and the completion/locale suites.
- [ ] 6.9 Windows CI verification: the path-identity, alias, case, and long-path fixtures and the atomic-write durability cases must run on the Windows leg of the CI matrix, not only on Linux and macOS. Record the run reference. Measure whether the new suites need `KNOWN_SLOW_TEST_WEIGHTS_MS` entries in `vitest.config.ts` — unlike the previous child's pure unit suites, these use real Git worktrees and real filesystem fixtures, so the size-derived fallback will likely skew the shards.
- [ ] 6.10 Run the portfolio no-regression baseline and compare: `pnpm exec vitest run test/core/store test/core/change-run test/commands/store.test.ts test/commands/store-root-selection.test.ts test/cli-e2e/store-lifecycle.test.ts`. The post-S1 reference is 110 files / 1511 passed / 1 skipped. Report the new counts explicitly; any previously-passing test that now fails is a blocker.
- [ ] 6.11 Prove the carve-out held: assert `RUNTIME_CONTEXT_VERSION` is still 1, that `src/core/session-runtime-context.ts` and `src/commands/context.ts` are unmodified by this change, and that the daemon and session suites (`test/core/management-api/`, `test/core/session-runtime-context*.test.ts`) are green.
- [ ] 6.12 Run `pnpm exec tsc --noEmit`, `pnpm run lint`, `pnpm run build`, and `rasen validate 'store-worktree-bindings-v2' --type change --strict`, and record each result.
- [ ] 6.13 Audit the child diff: confirm no write outside the two planned worktree roots, no staging/commit/fetch/push in any code path, no `store-planning/` or `layout-migration/` file added, `membership-layout.ts` untouched, no new branded type introduced (design Decision 5), and no unrelated formatting change.
