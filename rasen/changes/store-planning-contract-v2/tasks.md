## 1. Shared Validation Foundation

- [ ] 1.1 Add `src/core/canonical-json.ts` (RFC 8785 canonical JSON + UTF-8 bytes) and make `src/core/workflow-package/canonical.ts` re-export it, so the repository has exactly one canonical-JSON implementation and no new dependency is added.
- [ ] 1.2 Create `src/core/store/planning-validation.ts` with the branded value types (`ProjectId`, `TargetLineId`, `ChangeId`, `FullGitRef`, `GitOid`, `Sha256Digest`, portable relative path) and the `StorePlanningValidationError` family with stable codes `invalid_store_layout_v2`, `invalid_project_catalog`, `invalid_target_line_catalog`, `invalid_planning_identity`, `invalid_archive_v2`, `planning_path_escape`. Do NOT add the `invalid_issue_record` / `invalid_execution_plan` codes or the `IssueId` / `ExecutionPlanRevisionId` brands — design Decision 2 assigns those to `store-issue-resources`.
- [ ] 1.3 Move `WINDOWS_RESERVED_DEVICE_NAMES` into `planning-validation.ts` as the single definition, expose `isWindowsReservedDeviceName`, and make `src/core/store/project-records.ts` consume and re-export it instead of holding its own copy. Its existing exported name must stay importable so no current caller breaks.
- [ ] 1.4 Implement the portable-segment rule and the canonical v2 project-id, target-line-id, Change-id, full-Git-ref, Git-OID, SHA-256 digest, and portable-relative-path validators, rejecting traversal, both separators, control characters, trailing dot or space, reserved device names, and non-canonical case — on every platform, never sanitizing.
- [ ] 1.5 Add `test/core/store/planning-validation-v2.test.ts` with table-driven cases: valid UUID and kebab project ids, dotted target lines (`main`, `line-0.1`, `release-2026.08`), non-canonical case, traversal, separators, control characters, trailing dot/space, reserved device names, malformed refs/OIDs/digests, and portable evidence paths.

## 2. Layout V2, Catalogs, and Store Metadata

- [ ] 2.1 Extend `src/core/store/foundation.ts` with the optional `layoutVersion: 2` declaration on both the v1 and v2 metadata forms, accepted by the parser and preserved by the serializer, never injected when absent, and never inferred from directories on disk.
- [ ] 2.2 Create `src/core/store/planning-catalogs.ts` with the strict v2 project catalog: filename/id agreement on `projectId`, `roles.planning`, `planningBinding` state (`unbound` | `bound`, `bound` requires `boundAt` and planning membership), credential-free remote validation, and no adoption ownership list. Carry `id` through as an unvalidated human display name (design Decision 6) — it must accept exactly what the v1 membership record accepts, including values like `Elftia` and `my app`.
- [ ] 2.3 Add the strict target-line catalog (record version 1) with filename/id agreement, a Store full ref, and a project-id-keyed map of full code refs, validated against a conservative portable Git-ref subset with no machine paths and no credentials.
- [ ] 2.4 Create `src/core/store/planning-layout-v2.ts` implementing the layout v2 addresses from design Decision 3 for `native` | `win32` | `posix` flavors, with validation before any join and a resolved containment proof for every result. Do NOT add the `issue`, `issue-record`, `execution-plans`, or `execution-plan` addresses — design Decision 2 assigns those to `store-issue-resources`.
- [ ] 2.5 Require a self-contained Store root: reject a relative root, and under Windows semantics (explicit `win32`, or `native` on a win32 host) reject a root without a drive, UNC share, or device root, so no address is ever completed from the process's current drive or directory (design Decision 5).
- [ ] 2.6 Implement the instance-suffixed Archive entry address `YYYY-MM-DD-<changeId>--<instanceShort>` below the project's stable target-line Archive location, derived only from a verified Change instance identity, inspecting no branch name and nothing on disk.
- [ ] 2.7 Add `test/core/store/planning-layout-v2.test.ts`: legacy/v2 catalog round trips, strict unknown-field and cross-field failures, filename mismatches, no-machine-path fixtures, the display-name acceptance cases from 2.2, same-alias-multi-project addresses, target-line Archive partitioning, same-day retry disambiguation, containment escapes, relative and drive-less roots refused, and expectations built with `path.win32.join` / `path.posix.join` (never hardcoded separators) plus native win32 case/alias behavior.

## 3. Stable Identity and Change Metadata

- [ ] 3.1 Create `src/core/store/planning-identity.ts` with canonical-JSON + SHA-256 helpers and strict parsers for the `ps_`, `ci_`, `wt_`, `wp_` prefixed 64-hex branded identities, rejecting the wrong prefix, wrong length, non-hex, uppercase, and cross-domain values.
- [ ] 3.2 Implement `PlanningScopeId` derivation, 16-byte instance-seed mint/parse, and `ChangeInstanceId` derivation and verification using the versioned domain preimages in design Decision 4, excluding the Change alias, paths, branches, and refs from every portable preimage.
- [ ] 3.3 Implement `WorktreeInstanceId` derivation from adapter-supplied canonical repository/worktree identity strings (shape-verified only, never interpreted as paths) and role-ordered `WorkspacePairId` derivation from the verified Change, planning-worktree, and execution-worktree identities.
- [ ] 3.4 Extend `src/core/change-metadata/schema.ts` with the optional v2 `identity` block (re-derived and rejected on mismatch) and optional `implementation: code | none`, keeping metadata without them valid and round-tripping without injected fields. Mint and write behavior stays unchanged — no command writes v2 identity in this change.
- [ ] 3.5 Make `ChangeMetadataSchema` strict AND admit the engine-owned `quality` accounting as a carried-through passthrough (design Decision 8), so an unknown field is reported while the archive's own output stays readable. Its shape must tolerate historical records, including the older ones that also carry `rulesExtracted`.
- [ ] 3.6 Add `test/core/store/planning-identity-v2.test.ts`: determinism across property insertion order, domain separation across the four kinds, multi-project / multi-line / cross-attempt cases, branch-and-path stability, worktree replacement, role ordering, malformed typed ids, metadata tampering, planning-only intent, and legacy metadata round trips.
- [ ] 3.7 Add change-metadata strictness cases: a misspelled field is reported rather than dropped; a real archived `.openspec.yaml` carrying `quality` (use `rasen/changes/archive/2026-07-06-add-context-handoff/.openspec.yaml` as the shape reference) still reads and preserves that accounting unchanged.

## 4. Finalization Outcome and Archive V2

- [ ] 4.1 Create `src/core/store/finalization-v2.ts` with the four-way outcome contract (`landed`, `superseded`, `cancelled`, `abandoned`) and pure semantic validation of the reason/successor rules plus same-Store/same-project supersession that allows a different target line.
- [ ] 4.2 Implement the strict Archive v2 subcontracts: planning worktree/ref/OID facts, code-merge proof, create/update/delete spec-sync actions, portable unique evidence, missing-evidence names, and ISO timestamps, all over verified identity values.
- [ ] 4.3 Type spec-action `capabilityId` as a canonical capability address — one or more lowercase kebab segments separated by `/`, rejecting empty segments, `.`, `..`, and backslashes (design Decision 7). It must NOT be the single-kebab Change-id validator.
- [ ] 4.4 Implement the Archive v2 discriminated cross-field schema so landed-only applied spec sync, null code merge on non-landed records, `implementation: none` landed records with no code merge, and successor-only-on-superseded are structurally impossible to violate.
- [ ] 4.5 Implement deterministic parse/serialize/verify with stable field order, two-space JSON, one trailing newline, UTF-8 without BOM, strict unknown-field rejection, and validate-before-write, leaving Archive v1 accounting and every `archive-engine.ts` mutation caller untouched.
- [ ] 4.6 Add `test/core/store/finalization-v2.test.ts`: outcome and successor matrices, landed reachability shape, `implementation: none` fixtures, passive-history spec-action rejection, spec-action digest matrices, nested capability addresses accepted, evidence escape and case-alias duplicate rejection, strict-field failures, and byte-identical round trips across property insertion order.

## 5. Boundary, Purity Proof, and Exports

- [ ] 5.1 Create `src/core/store/planning-foundation.ts` re-exporting the five modules, with the docstring stating the no-filesystem / no-registry / no-cwd / no-environment / no-command / no-Git-process rule, and export it from `src/core/store/index.ts` and `src/core/index.ts` (the latter also gains the `change-metadata` export) without changing any command or mutation caller.
- [ ] 5.2 Add `test/core/store/planning-foundation-consumer.test.ts` proving through the public `src/core/index.js` surface that a consumer can compose validated values without importing any internal regex, hash, or path rule, and that the brands actually discriminate (a bare `string` and an unverified id must not satisfy the APIs that require verified ones).
- [ ] 5.3 Add `test/core/store/planning-foundation-purity.test.ts` reading the Layer-0 sources from disk and asserting every import specifier is in the explicit allowlist (`zod`, `yaml`, `node:crypto`, `node:path`, `../canonical-json.js`, `../zod-issues.js`, `../id.js`, `./identity-types.js`, `./remote.js`, `./planning-validation.js`, `./planning-identity.js`) and that no `node:fs`/`fs`, `node:child_process`, `node:os`, `process.cwd`, `process.env`, `execSync`, `spawn`, or Store-registry reference appears (design Decision 9).
- [ ] 5.4 Mutation-prove the purity guard: inject a forbidden import (e.g. `node:fs`) into one Layer-0 module, record that 5.3 goes RED and names the offending file, revert byte-exactly, record it GREEN again. Record both observations in the evidence directory. A guard never observed to fail is not evidence.

## 6. Verification

- [ ] 6.1 Run the five focused suites — `pnpm exec vitest run test/core/store/planning-validation-v2.test.ts test/core/store/planning-layout-v2.test.ts test/core/store/planning-identity-v2.test.ts test/core/store/finalization-v2.test.ts test/core/store/planning-foundation-consumer.test.ts test/core/store/planning-foundation-purity.test.ts` — fix root causes, and record the exact passing command and counts.
- [ ] 6.2 Run the compatibility suites this change touches: `test/core/store/foundation.test.ts`, `test/core/store/project-records.test.ts`, `test/utils/change-metadata.test.ts`, `test/core/archive-accounting.test.ts`, `test/commands/change-initiative-link.test.ts`, `test/core/workflow-package/`, and record results. Any failure here is a regression, not a flake.
- [ ] 6.3 Prove the strict-metadata change regresses nothing on real data: read every `.openspec.yaml` under `rasen/changes/` (active AND `archive/`) and `.rasen/changes/` through the new `ChangeMetadataSchema` and record that none is rejected. If any is, fix the schema (widen the passthrough) — do not weaken strictness and do not edit the data.
- [ ] 6.4 Run the no-regression baseline established for this worktree and compare against it: `pnpm exec vitest run test/core/store test/core/change-run test/commands/store.test.ts test/commands/store-root-selection.test.ts test/cli-e2e/store-lifecycle.test.ts` — the pre-change reference is 104 files / 1264 passed / 1 skipped. Report the new counts explicitly (they will be higher by this change's own files); any previously-passing test that now fails is a blocker.
- [ ] 6.5 Windows CI verification: confirm the path fixtures run on the Windows leg of the repository's CI matrix, that native win32 case/alias behavior is exercised there, and that the POSIX-flavor expectations still run on the Linux and macOS legs. Record the run reference. (No `KNOWN_SLOW_TEST_WEIGHTS_MS` entry is expected for these pure unit suites — confirm rather than assume.)
- [ ] 6.6 Run `pnpm exec tsc --noEmit`, `pnpm run lint`, `pnpm run build`, and `rasen validate 'store-planning-contract-v2' --type change --strict`, and record each result.
- [ ] 6.7 Audit the child diff: it must contain only contracts, pure validation, exports, and tests. Confirm zero selector/command routing, Store or migration mutation, Git worktree operation, archive apply logic, management-API, UI, dependency addition, or unrelated formatting change — and confirm the sibling-owned surfaces named in design Decision 2 (Issue ids, execution-plan revisions, Issue addresses) are absent.
