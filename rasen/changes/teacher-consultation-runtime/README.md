# teacher-consultation-runtime

Implement the canonical ECP consultation lifecycle: implementer-initiated Teacher requests, direct read-only admission, durable advice, replay-safe delivery, bounded policy, and same-implementer continuation.
