> **Worktree note**: this child is implemented in the git worktree at `E:\AI\ChatAI\Agents\VibeCodingProjects\workflow\Reference\OpenSpec-wt-workflows` (branch `feat/ui-config-workflows-page`, dependencies already installed). Change artifacts live in the MAIN tree (`E:\AI\ChatAI\Agents\VibeCodingProjects\workflow\Reference\OpenSpec-code\rasen\changes\...`); the LEAD copies them over. Run implementation commands, builds, and tests inside the worktree; run `rasen validate` from the main tree.
>
> **Stacked-delta note**: the `management-http-api` and `change-submission` deltas in this change REMOVE requirements that exist only as pending ADDED text in `ui-config-redesign-spaces-page` (W6). W6 must archive first; before this change archives, re-check that the REMOVED blocks still match W6's archived wording verbatim.

## 1. Whitelist and mutation bridge (server)

- [x] 1.1 Add the four bounded-cli rows `import-workflow`, `init-workflow`, `export-workflow`, `delete-workflow` to `WHITELIST` in `src/core/management-api/whitelist.ts` (tier count 4 → 8 after W6; if W6's rows are not yet merged into the worktree's base, coordinate with the LEAD rather than renumbering), and update the table's doc comment to describe the enumeration by tier rather than a hardcoded count that drifts.
- [x] 1.2 Create `src/core/management-api/workflow-submit.ts`: a workflow mutation submitter mirroring `submit.ts` mechanics — server-own `dist/cli/index.js` resolution, argv array + `shell: false`, cap-1 in-flight with slot release on child close, 60s timeout with SIGTERM→SIGKILL grace, cwd = launch project root falling back to server cwd (design D2). Ops per design D4: `import` → `workflow import <path> --json`; `init` → `workflow init <id> --output <output> --json`; `export` → `workflow export <id> <path> --json` (+`--force` iff `force`); `delete` → `workflow delete <id> --yes --json` (+`--force` iff `force`).
- [x] 1.3 Implement the input guards in the submitter, before any spawn: unknown `op` → 400; every path (`path`, `output`) must be absolute via `path.isAbsolute` (400 otherwise, works for both `E:\...` and `/...` forms); `id` must match the same identifier rule the workflow manifest schema enforces (check `src/core/workflow-registry`/manifest schema for the actual pattern and reuse or re-export it — the guard must never refuse an id the CLI accepts) and must not begin with `-`.
- [x] 1.4 Map subprocess outcomes: exit 0 + parsed `--json` stdout → 200 (201 for `import`/`init` which create things) passing through the CLI payload (imported/reused ids, draft output path, deleted id + forcedReferrers); non-zero → 422 with the CLI's `status[0].message` (fallback stderr) verbatim; busy → 409; spawn failure/timeout → 500/504, mirroring `submit.ts`.

## 2. Read endpoints (server)

- [x] 2.1 Create `src/core/management-api/workflows.ts` with the listing handler: fresh `loadWorkflowCatalog()` per request, entries shaped exactly like `workflow list --json` (`id, source, sourcePath, digest, kind, skillName, commandId, unused`) using the machine-level usage context (`createWorkflowUsageContext(catalog)` with no projectRoot, matching `src/commands/workflow-library.ts` list), plus `invalid` records and catalog `diagnostics`.
- [x] 2.2 Add the detail handler for one-segment `/api/v1/workflows/<id>`: `workflowDefinitionForJson(definition)` + `scanWorkflowUsage(id, {}, context)`, 404 envelope for unknown ids; copy the segment-matching pattern from the existing `/api/v1/tasks/<id>` matcher in `router.ts`.
- [x] 2.3 Add the validation handler for `GET /api/v1/workflow-validation?target=`: if `target` matches a catalog id (valid or invalid), validate the id; else require `path.isAbsolute(target)` (400 otherwise, never probing relative paths) and run `validateWorkflowInput` with the launch project root as project context, mirroring the CLI. Response mirrors `workflow validate --json`.
- [x] 2.4 Register the new paths in `router.ts`: `/api/v1/workflows` and `/api/v1/workflow-validation` in `KNOWN_PATHS`, GET/POST admission for `/api/v1/workflows`, GET-only for `/api/v1/workflow-validation`, 405 for everything else, trailing-slash normalization inherited, deeper suffixes (`/api/v1/workflows/<id>/extra`) falling through. Wire the mutation POST to the submitter from group 1.
- [x] 2.5 Add the wire types to `src/core/management-api/wire-types.ts` (listing entry, invalid record, detail payload, validation payload, mutation request/response shapes).

## 3. Server tests

- [x] 3.1 Endpoint tests following the existing management-api test patterns: listing (fresh read, unused marker agreement with a seeded unreferenced user workflow, invalid entry reporting), detail (payload parity with `workflow show --json` shape, 404), validation (id target, absolute-path target using `path.join`-built Windows/POSIX-native paths, relative target → 400, read-only), auth (401 without token) and 405 coverage for unadmitted methods on both paths.
- [x] 3.2 Mutation bridge tests with a stubbed CLI entry (the pattern change-submission tests use): per-op argv construction (single tokens, `--yes` always on delete, `--force` only when flagged), unknown op → 400 with no spawn, relative path → 400 with no spawn, option-shaped id → 400 with no spawn, cap-1 → 409, CLI failure message passthrough → 422 (including a built-in delete refusal), success payload passthrough.
- [x] 3.3 Whitelist tests: the bounded tier enumerates exactly the eight ops post-W6, `getBoundedCliEntry` rejects supervised ops and vice versa, and the workflow bridge admits only its own four (cross-admission scenarios from the change-submission delta).

## 4. UI

- [x] 4.1 Mirror the new wire shapes into `packages/ui/src/api/types.ts` and add client methods in `packages/ui/src/api/client.ts`: `listWorkflows`, `getWorkflow`, `validateWorkflow`, `mutateWorkflow`.
- [x] 4.2 Create `packages/ui/src/components/WorkflowsPage.tsx`: provenance groups (Built-in / User / Invalid-when-present) as cards using existing card idioms — id, kind chip, source, skill name, abbreviated digest, dependency summary, unused badge; no delete affordance on built-ins; visible lock treatment for built-ins.
- [x] 4.3 Add the detail view (route or expanding panel — match existing task-detail idiom): full definition (skill, command, four requires slots, recommends, file inventory) + usage referrers with consumer kinds.
- [x] 4.4 Implement the five action flows with dialogs: init (id input + output directory via the existing local-path browser component, success shows draft path + edit/validate/import hint), validate (installed id or picked path, diagnostics rendered), import (directory or `.rasenpkg` file pick — file entries are selectable), export (directory pick + filename defaulting to `<id>.rasenpkg`; on `destination_exists` offer explicit overwrite retry with `force`), delete (confirm dialog; on referrer refusal show the CLI message verbatim and offer a separately confirmed force). Disable action submission while a mutation is in flight; refresh the listing after any success without a full reload.
- [x] 4.5 Register the space-agnostic `/workflows` route in `app.tsx` and add the always-rendered `Workflows` header navigation entry in `Layout.tsx` with active detection (**LEAD merge point**: W3 adds a Pipelines entry to the same nav — keep the addition minimal and additive). Navigation must be SPA-click (hard navigation drops the token).
- [x] 4.6 UI tests following `packages/ui/test` patterns: listing groups and badges, built-in lock (no delete control), delete flow refusal → force path, export overwrite path, import success refresh, nav entry present without a resolved space, and absence of any model/handoff/gate control on the page.

## 5. Verification

- [x] 5.1 Build CLI (`node build.js`) and UI (`pnpm --filter @atelierai/rasen-ui build`) in the worktree; run the full test suite; isolate-rerun any Windows CLI-spawn EBUSY/timeout flakes before treating them as regressions. Note for CI: path guards and tests must pass on the Windows matrix (paths built with `path.join`, `path.isAbsolute` for both native forms).
- [ ] 5.2 Manual smoke via the running UI (chrome-use; curl needs `--noproxy '*'`): open `/workflows` from a space and from the bootstrap state, import a scratch workflow package, export it, delete it (guarded then forced), confirm CLI messages surface verbatim and the visual style stays within existing idioms.
- [ ] 5.3 From the MAIN tree: `rasen validate ui-config-redesign-workflows-page --strict`; re-verify the stacked-delta note (W6 archived text matches the REMOVED blocks verbatim) before archive.
