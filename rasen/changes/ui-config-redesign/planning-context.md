# Planning context — ui-config-redesign portfolio

## User intent (verbatim chain)
- Session invocation: `/rasen:auto --no-gate auto-decompose 阅读交接文档：ui-theme-and-space-redesign.md 然后开始实现吧。planner使用fable，implementer和reviewer使用opus，其他使用sonnet`
- The handoff doc's open item (theme decision) is already resolved: CRT variant + toggle committed as `91af05a`; working tree clean. The implementable work is the ratified office-hours design at `rasen/office-hours/ui-config-and-library-redesign.md` (2026-07-22, forks chosen by the user in dialogue). Its own Next Steps say: propose as a decomposed portfolio.

## Authoritative design source
`rasen/office-hours/ui-config-and-library-redesign.md` — READ IT FIRST, in full. It contains verified-in-code premises (file:line pointers), chosen forks with rejection rationale, per-workstream specs, open questions, and success criteria. Do not re-litigate chosen forks (W1 store-as-scope via existing `store:` key; page-level scope toggle; config-key namespace for pipeline overrides — NOT YAML forking; W4 pure library management, no per-workflow model).

## Decomposition (LEAD-audited, taken)
Children (prefix `ui-config-redesign-`), ALL SERIAL in this order:
1. `store-scope` (W1) — store becomes a configuration scope. Resolution chain env > project > store > global. `store:` + local planning shape = config inheritance (notice changes from ignored → inheriting). Wire: `scopeValues` gains `store`. Registry: 14 dual keys + 3 project-only keys gain `store`. No transitivity.
2. `config-page` (W2, dep 1) — page-level Global/Local segmented control (write target + filter), remove per-row Scope select, inherited-value line (继承自 global/store), read-only store rows with "在 store <id> 中编辑 →" space-switch link, 4 tabs: General/Project/Privacy/Advanced, human labels. Autopilot group leaves the page (fully removed only in W3 child; W2 may leave it in place untouched or stage its removal — planner decides coherence).
3. `spaces-page` (W6, independent) — `/spaces` route: list/search/pin (client-side filter; `ui.pinnedSpaces` global array key), capped header dropdown (~8 + "All spaces…"), create-space via `GET /api/v1/local-paths` + `POST /api/v1/spaces` (spawns CLI subprocess — server never writes workspace files), fix stale `management-http-api` "only mutating endpoint" clause in the same delta. Open questions 6/7/8 of the design must be resolved in the proposal.
4. `wildcard-config` (W3 enabler, dep 1) — grow wildcard config machinery past `featureFlags.<name>` global-only two-segment limit (`config-keys.ts:354`) to three-segment families (`pipelines.<name>.gates.<stage>` etc.) at global/store/project scope.
5. `pipelines-page` (W3, dep 4) — new Pipelines route: stage-graph visualisation + per-stage gate/model/handoff config rows; Defaults table (role matrix moves here from Config page); `autopilot.gates` becomes a mask (precedence: per-stage > autopilot.gates off > stage definition); pipeline import/export/init/delete UI; re-point `rasen pipeline agents` at the config family (stop freezing YAML copies).
6. `vet-removal` (W5, dep 5) — remove `gate: 'vet'` everywhere (3 YAMLs, Zod union, wire type, display branch, 7 template-prose blocks, main spec delta via REMOVED+ADDED with DISTINCT names — validator rejects same-name; check the RENAMED form as possibly cleaner); user YAML with `gate: vet` reads as `true` + one-time warning, never hard-error. Plus telemetry disclosure help button (five fields verbatim).
7. `workflows-page` (W4, independent) — new Workflows route: pure library management (list/show/init/validate/import/export/delete, built-ins locked, kind/source/digest/deps, unused marker). NO model/handoff controls.

## Constraints (hard)
- Visual style FROZEN: warm-editorial default + CRT variant toggle stay exactly as committed. Information architecture only.
- Reference projects never named in code/comments/commit messages.
- UI ≠ full CLI: display/create/manage yes; work-performing commands (apply/ship/archive/agent context…) no.
- `workflow` and `pipeline` stay distinct concepts.
- Server mutates ONLY by spawning the CLI subprocess (existing pattern); space resolution stays non-mutating.
- Version stays as-is — never bump major/minor (user standing order).
- Branch: `feat/ui-config-redesign` (off feat/ui-space-redesign, stacked on open PR #21). Children ship LOCAL commits only; ONE portfolio-level delivery at the end.
- Archive spec-merge guard: scenario-set changes need REMOVED+ADDED with distinct requirement names (`specs-apply.ts findMissingCurrentScenarios`, `validator.ts:237`).

## Environment gotchas (from prior sessions)
- chrome-use CDP curl MUST use `--noproxy '*'` (Clash proxy 502s localhost).
- git-bash node resolves `/tmp/x` as `E:\tmp\x` — pass values inline.
- UI hard navigation drops the token → SPA-click nav only.
- Build: root `node build.js` builds CLI only; UI = `pnpm --filter @atelierai/rasen-ui build`.
- Windows test flake: CLI-spawning tests may EBUSY/timeout; isolate-rerun before calling regression.

## Findings log (planner: append durable findings after each propose)

### store-scope (W1) propose — 2026-07-22

- **Config API still addresses by `?project=`, NOT `?space=`** — planning-space-addressing moved only the management route group onto spaces (`packages/ui/src/api/client.ts:95` documents this; `src/core/config-api/router.ts:141` resolveProjectContext). W1's D6 adds `space` (query/body) to the config endpoints beside `project` (both present → 400); `resolveSpaceSelector` already exists in `config-api/project-addressing.ts:94` and is reused verbatim. W2's ConfigPage must switch from `listConfig(projectId)` to space-based calls + the new `scope: 'store'` write and the response-level `store: { id, root } | null` ref (added in W1 for the 继承自/edit-in-store affordances).
- **Store registry reads are async** (`listRegisteredStores`, fs/promises) while every layer resolver is deliberately sync/pure. W1 resolves this with ONE async helper `resolveConfigStoreLayer(projectRoot)` (effective-config.ts) and optional `storeRoot`/`storeConfig` params on the sync resolvers; `resolveHandoffThresholdReport` (agent-context.ts:494) becomes async (its only caller `commands/agent.ts:66` is async). Any later child needing the store layer must await this helper — never re-derive the edge.
- **Complete layered-resolver inventory** (verified callers): `resolveEffectiveConfig` (commands/config.ts:136,308; config-api/router.ts ×3), `resolveHandoffThresholdLayers`+`resolveModelConfigLayers` (commands/pipeline.ts:253-254,808-809; agent-context.ts:500), `resolveAutopilotGatePolicy`/`resolveAutopilotSelectionPolicy` (NO production callers — consumed via template prose in auto.ts §0.5/0.6 + recorded in run-state `gatePolicy.source` enum at run-state.ts:145, which needs 'store' and keeps legacy 'config'). Pure stage resolvers (`resolveStageRuntimeConfig`/`resolveStageHandoffConfig`, pipeline-registry/types.ts:449,588) take layers as params — W3's per-stage config family will slot ABOVE these same chains.
- **Template prose precedence lives ONLY in auto.ts §0.5/0.6** (goal templates mention gates only for the vet carve-out — W5's concern, untouched by W1). auto.ts edits require repasting the `rasen-auto` golden hash in `test/core/templates/skill-templates-parity.test.ts:159`.
- **The `ignored-store-pointer` notice is NOT owned by any main spec** (greps across rasen/specs found nothing normative on its wording) — W1's new `store-config-inheritance` spec now owns the notice contract (split into inheriting/inactive kinds; three locale catalogs `src/locales/{en,zh-cn,ja}.json` key `ignoredStorePointerWarning` replaced in lockstep, plus pipeline-messages.ts type/key/branch).
- **Editor source labels are locale data**: `configEditor.editor.source` map in all three catalogs needs the `store` entry (en.json:519).
- **W1 delta surface** was 9 specs (1 new + 8 modified: config-resolution, config-key-registry, config-http-api, autopilot-gate-policy, autopilot-selection-policy, pipeline-handoff-config, opsx-pipeline-registry, cli-agent-context) — every REMOVED+ADDED pair uses distinct requirement names. cli-config spec was deliberately NOT touched: its editor-source enumeration (line 344) predates the store scope but W1 adds no CLI write surface; if a reviewer flags it, the fix is a small ADDED-only delta, not a rewrite.
- **CLI-in-store-root asymmetry (accepted, D3 note)**: the CLI addresses a store root's own config as `project` scope/source (today's behavior); only space-addressed API reads present it as `store`. W2 must not "fix" this on the CLI side.
- **No-transitivity is implemented as "a registered store root never inherits"** (canonical-path comparison), which also kills the self-pointing store edge case. `rasen init`'s pointer guard (init.ts:122-147) only fires on config-only dirs — untouched by W1.

### spaces-page (W6) propose — 2026-07-22

- **Two more specs were stale/conflicting than the plan listed**: besides management-http-api's "only mutating endpoint" clause, (a) its "The spaces listing is a management endpoint..." requirement mandates 405 on POST /api/v1/spaces — REMOVED+ADDED in the W6 delta; (b) `change-submission`'s "Whitelisted operations only" requirement pins the bounded-cli whitelist tier to EXACTLY one op (`whitelist.ts:33` doc comment mirrors it) — REMOVED+ADDED to enumerate four ops (create-change + create-project-space/register-store-space/setup-store-space). Any future child adding a CLI-spawn endpoint must delta change-submission's enumeration again.
- **`rasen init` has NO `--json`** but mints projectId + registers the project itself (`resolveProjectHome(ensure:true)` at init.ts:259), so POST /api/v1/spaces detects success by exit-0 + re-reading `handleSpaces()` — never by parsing init's human output. `store register/setup` DO have `--json` (`src/commands/store.ts:817-837`); register needs `--yes` for non-interactive metadata confirmation.
- **OQ resolutions (ratified in W6 proposal/design)**: OQ6 = home-only start point, any client-supplied absolute path enumerates (explicit entry is the sole escape), relative → 400; OQ7 = yes, register-existing covered — kind:'store' picks register vs setup by whether `<path>/rasen` exists, response carries `operation`; OQ8 = whitelist table rows (bounded-cli tier) + absolute-path requirement as option-injection guard + `validateStoreId` reuse + own cap-1 60s-timeout submitter mirroring submit.ts (cwd = server cwd, NOT client input — the space doesn't exist yet).
- **W1↔W6 merge point (config-keys.ts)**: W6 appends `ui.pinnedSpaces` (global-only array, group Appearance) + typed `ui` block in GlobalConfigSchema/GlobalConfig; W1's planned "8 global-only keys" count assertion in config-keys.test.ts becomes 9 after merge — LEAD reconciles; W6's tasks deliberately avoid count assertions. CLI editor needs the key disabled like `workflows` (`config.ts:170`) — the editor has NO array prompt; UI array control is already read-only (`controls.ts:90`).
- **UI recency is localStorage, pins are config** (design D1): extending the spaces listing with lastSeen was rejected (would delta planning-space-addressing's listing requirement; stores have no lastSeen). Switcher cap = 8, pinned → recent → alpha fill, current space always included, `__all__` sentinel routes to `/spaces` (a space-agnostic route — Layout already renders fine with no space).

### workflows-page (W4) propose — 2026-07-22

- **config-ui-package is deliberately NOT deltaed for new routes — W6 set the precedent, W4 followed it.** config-ui-package's "Platform shell" requirement enumerates nav views (board/archive/config) and bans only a Sessions page; W6 added `/spaces` + the capped switcher purely via its own new `spaces-ui` spec without touching it. W4 does the same (`workflows-ui` owns the `/workflows` route + always-rendered nav entry). This keeps config-ui-package free for W2 (tabs/scope, which MUST delta it) and W3 (Autopilot-group removal + GatesInventoryPanel death, which must too) — those two are the only children that should touch it, and W3 must draft against W2's ADDED text if W2 archives first.
- **Bounded-CLI whitelist tier grows 4 → 8**: W4 adds `import-workflow`, `init-workflow`, `export-workflow`, `delete-workflow`. W4's change-submission and management-http-api deltas REMOVE W6's pending ADDED requirements verbatim and re-ADD renamed ("…across the change, space, and workflow bounded-CLI operations" / "…with an enumerated CLI-backed mutation set") — **archive order W6 → W4 is mandatory** and both delta files carry a stacked-delta banner saying so. Main-tree `whitelist.ts` still has ONE bounded op (W6's four live in its worktree); W4's implementer must coordinate with LEAD if W6's rows aren't in the worktree base (tasks 1.1 says so). Any FUTURE CLI-spawning endpoint re-deltas both specs again, now against W4's ADDED text.
- **Workflow ids are user-chosen, so `/api/v1/workflows/<verb>` subpaths are unsafe** — a user workflow named `validate` is legal. Hence validation lives at a separate top-level GET-only path `/api/v1/workflow-validation?target=`, and detail matching is the one-segment `/api/v1/tasks/<id>`-style matcher. Target rule: catalog id, else must be absolute path (400) — never relative probing.
- **CLI facts the endpoints lean on** (`src/commands/workflow-library.ts`): all 8 subcommands have `--json`; `delete` needs `--yes` non-interactively (bridge always passes it — confirmation is the UI dialog's job) and `--force` bypasses only the referrer guard (built-ins refuse regardless, `builtin_delete_forbidden`); `export` fails `destination_exists` non-interactively without `--force` (UI: surface → offer overwrite retry); `list`'s unused marker is MACHINE-level (list passes no projectRoot to the usage scan) — the API mirrors that exactly so UI and CLI never disagree. W6's local-paths listing includes files (`isDir:false`), so import picks `.rasenpkg` with zero new picker work.
- **No space addressing on workflow endpoints (W4 design D2)**: library is user-wide; mutation cwd = launch project root (fallback server cwd). Accepted limitation: an import whose pipeline/schema dep exists only in another project's layer may be refused — CLI from that project is the escape hatch. Don't "fix" this in later children without a design decision.

### config-page (W2) propose — 2026-07-22

- **W2's config-ui-package delta replaces THREE requirements** (REMOVED+ADDED, distinct names): "…source transparency" → "…layer transparency across project and store spaces"; "Editing is constraint-driven and scope-explicit" → "…with a page-level scope mode"; "Autopilot and Workflow groups lead the configuration page" → "The configuration page is organized into scope-filtered tabs". **W3's pipelines-page delta to config-ui-package must draft against W2's ADDED text** (W2 archives first, serial chain): W3 deletes the interim-Workflow-tab clause from the tabs requirement (REMOVED+ADDED again) and REMOVES "The Autopilot group shows a read-only gates inventory" (left untouched by W2 — the panel survives inside the Workflow tab until W3).
- **Interim fifth tab decision (W2 D2)**: Workflow tab = Workflow + Autopilot groups + GatesInventoryPanel, all rendered exactly as today. Rationale: hiding 14 still-functional keys for a release window is a regression; W3's removal becomes one tab-entry deletion. Tab map: General(Profile+Appearance+Behavior) · Project(Project+Archive) · Privacy(Telemetry) · Advanced(Advanced) · Workflow(Workflow+Autopilot), unmapped groups → trailing bucket (no key can vanish). W6's `ui.pinnedSpaces` (group Appearance) lands in General automatically — zero merge friction, and W2 adds no count assertions.
- **Store-inherited rows are READ-ONLY with an edit-in-store space-switch link (ratified)** — consequence recorded in W2 D3: the UI offers no project-level override of a store-inherited key; the CLI can still set one, after which the row edits normally with the store value shown shadowed. Don't "fix" without user say-so.
- **Design-doc Chinese strings (继承自/在 store 中编辑) are intent, not copy** (W2 D6): the UI has no localization layer (CLI catalogs don't apply to packages/ui), so W2 ships English copy for the inherited line and store link; spec text is language-neutral. A future UI-localization child would move these with everything else.
- **Human labels = UI-local `labels.ts` constant with dot-path fallback** (W2 D5) — the registry gets no `label` field (presentation-only data would widen W1's just-shipped wire surface). New registry keys degrade to their dot-path, never break.
- **W2 removes the config `?project=` plumbing from the UI client entirely** (D7): all four config calls move to `?space=`; W1 keeps `?project=` alive server-side for compatibility, but the UI is its only caller and keeps exactly one addressing form. Default page mode = Local (space context), Global one click away.

### wildcard-config (W3-enabler) propose — 2026-07-22

- **Both spec deltas are ADDED-only and order-independent** with W1/W6's pending deltas — no stacked REMOVED blocks, no archive-order coupling. Rationale documented in each delta's banner: config-key-registry gains "Wildcard configuration key families"; config-http-api gains "Wildcard family instances are first-class config API keys". The v1 `not_supported` wildcard-write carve-out (router.ts validateWriteKey) exists ONLY in code, never in spec text, so lifting it needs no REMOVED. **The carve-out is lifted for featureFlags too** — `featureFlags.<name>` becomes API-writable at global (UI never sends such writes today; renders wildcard entries read-only).
- **"Three-segment families" in the design doc is a miscount** — `pipelines.<name>.gates.<stage>` is FOUR dot segments with two placeholders. The machinery is pattern-shaped (fixed-length template, literal + `<placeholder>` segments, `[A-Za-z0-9_-]+` charset), not segment-counted; featureFlags re-expressed through the same matcher with `key: 'featureFlags'` kept for wire compat. Placeholder validation is STRUCTURAL only — no pipeline/stage existence checks (config validation must not depend on space-dependent pipeline resolution; featureFlags typo precedent).
- **New families: no default value — absent ≠ defaulted.** W3's mask semantics need "no override" distinguishable from any concrete value. Instance entries surface in effective-config/list with a new additive `instanceKey` wire field; **W2 must NOT pre-mirror `instanceKey` into packages/ui/src/api/types.ts** (that mirror lands with W3 — keeps W2∥enabler touch-sets disjoint). Group `Pipelines` is unmapped in W2's tab map → trailing bucket transiently until W3 claims the keys.
- **Touch-set (for the parallel cohort)**: src/core/{config-keys,effective-config,global-config,project-config}.ts + src/core/config-api/{router,serialize,wire-types}.ts + src/commands/config.ts (verify-only) + tests. Fully disjoint from W2's packages/ui/**. Merge point: global-config.ts `pipelines` block vs W6's `ui` block on the same schema (both additive).
- **W1 is LANDED on main** (verified in code: effective-config.ts store layer, router.ts contextStoreRef/space addressing, three-scope validateWriteKey hints) — enabler and W2 draft against real code, not just W1's promised text.
- **W3 planning note**: CLI `config list` prints the raw config dump, so set instances already print there — no cli-config delta needed anywhere in this portfolio for list visibility. W3's config-ui-package delta must also handle the `Pipelines` group claim (move instances/templates off the Config page trailing bucket to the Pipelines page).

### pipelines-page (W3) propose — 2026-07-22

- **`pipeline agents` sets RUNTIMES (claude/codex), not models — the freeze fix needed a FOURTH family** `pipelines.<name>.runtimes.<role>` (enum claude|codex, per-ROLE not per-stage). This forced a stacked REMOVED+ADDED on the enabler's "four families" enumeration (config-key-registry). Any future family addition re-cuts that enumeration again — it is now "five families" in W3's ADDED text.
- **W3 carries FIVE stacked-delta sources** (the portfolio maximum): W1 (autopilot-gate-policy, opsx-pipeline-registry, pipeline-handoff-config), W2 (config-ui-package), enabler (config-key-registry), W4 (change-submission + management-http-api). **Required archive order: W1, W2, W6, W4, enabler ALL before W3; W5 after W3.** Bounded-CLI tier is 12 ops post-W3. The pipelines-inventory requirement MIGRATED out of config-http-api (REMOVED-with-migration) into new `pipeline-http-api` — **W5's vet-scenario delta must target `pipeline-http-api`'s "Pipelines inventory endpoint with effective stage configuration" requirement, NOT config-http-api** (the handoff's note about config-http-api:98-107 is now obsolete). W5's autopilot-gate-policy delta targets W3's "Gate policy is a mask over per-stage gate configuration" ADDED text (which contains one vet sentence: "A stage whose definition declares the always-pausing `'vet'` gate SHALL be outside this mask entirely, per the vet requirement" — W5 must re-cut it when removing the vet requirement).
- **Mask design (W3 D2/D3)**: one resolver (`stage-overrides.ts`) computes per-stage effective gate/model/handoff/runtime; `pipeline show --json` and the pipelines endpoint both report through it; auto.ts §0.5 rewritten so the LEAD consults `pipeline show` effective gates instead of combining layers (golden-hash repaste). Run-state shape UNCHANGED — per-stage instances resolve live at gate time, never frozen (avoids recreating the YAML-freeze defect at run scope). Per-stage model/handoff instances sit ABOVE stage-level YAML values (the whole point: override without touching definitions).
- **Frozen legacy pipeline copies are NOT auto-migrated** (W3 D4): a copy written by the old `agents` behavior keeps winning as the project layer, visibly badged; deleting it is the user's explicit action. CHANGELOG notes required: `off` becomes pierceable by per-stage `on`; agents no longer freezes.
- **Endpoint split**: GET /api/v1/pipelines stays in the config-api router (gains `?space=` + effective fields); POST rides the management whitelist (4 new ops, bridge mirrors W4's). Merge points with W4's in-flight worktree: whitelist.ts rows, Layout.tsx nav, possibly a shared bounded submitter if W4's review extracts one.

### vet-removal (W5) propose — 2026-07-22 — ALL 7 CHILDREN PROPOSED

- **The "no `'vet'` literal in src/" success criterion is refined**: the legacy-coercion shim (StageSchema preprocessing, `'vet'` → `true` + one warning per pipeline per process) must match the string to detect it — it is the single permitted occurrence, and W5's guard test encodes exactly that (source scan asserting the shim site is the only match). Reviewers should not flag the shim as a criterion violation.
- **Main-spec vet surface was 2 requirements, not 1**: besides "A vet gate is never auto-approved" (pure REMOVED, no successor — Reason carries the autonomy consequence verbatim + the one-value restoration recipe `pipelines.goal-loop-measure.gates.define-goal: on`), the "Existing boolean gate configuration parses unchanged" requirement (the vet-WIDENING compat guarantee) inverts into "Legacy vet gate values read as ordinary gates" (the narrowing compat). config-http-api and config-ui-package's gates-inventory needed NO W5 delta — W3 already removed/migrated them, as predicted by the W3 findings.
- **RENAMED delta form checked and rejected** for all W5 re-cuts (design D3): RENAMED is name-only with content unchanged; every W5 change alters content, so REMOVED+ADDED throughout. The telemetry-disclosure delta is ADDED-only and order-independent with W2/W3's config-ui-package deltas.
- **Telemetry payload fields verified in code** (`telemetry/index.ts:190-194`): `command`, `version`, `distinctId` (anonymous random UUID), `os` (process.platform), `node_version` (process.versions.node); disclosure is an isolated `TelemetryDisclosure` component keyed to the one dot-path (no generic per-key help framework), with a parity test pinning the field list.
- **W5 touched files**: 3 goal-loop YAMLs, pipeline-registry/types.ts (StageSchema + shim), config-api/wire-types.ts + packages/ui/src/api/types.ts (gate → boolean), commands/pipeline.ts (vet branch), templates auto/_orchestration/goal-command/goal-plan/experts-workflow-author (+ golden-hash repastes), new TelemetryDisclosure.tsx + tests. Stacked on W3 ×2 (autopilot-gate-policy mask re-cut "…over every stage gate"; pipeline-http-api inventory re-cut "…with boolean gates").
- **Portfolio propose phase COMPLETE**: 7/7 children proposed, all validate-green first-run. Final archive chain: W1 → W2 → W6 → W4 → enabler → W3 → W5.
