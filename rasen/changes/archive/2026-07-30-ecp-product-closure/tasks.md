## 1. Engine selection policy (config + CLI enforcement)

- [x] 1.1 Register `runs.engine` in the config-key registry (`src/core/config-keys.ts`) and schema (`src/core/config-schema.ts`): values `auto | reconciler | legacy`, default `auto`, resolution project > store > global — the literal string `runs.engine` must exist in both files (grep-falsifiable)
- [x] 1.2 Add `--engine <reconciler|legacy>` option to `rasen pipeline start` in `src/commands/pipeline.ts` (flag > config); resolve the effective engine + deciding source before `facade.start`
- [x] 1.3 Enforce explicit disable: effective `legacy` → `pipeline start` throws a typed error whose code string `engine_disabled_by_config` names the deciding config layer; no Run Record is created
- [x] 1.4 Enforce forced reconciler: `--engine reconciler` (or configured `reconciler`) with `analyzeReconcilerSupport` unsupported → fail with that support reason; no Run created under either engine
- [x] 1.5 Surface the effective engine + source in the `pipeline start` receipt (`engine`, `engineSource` fields) and in `pipeline show --json` (resolved policy for the pipeline)
- [x] 1.6 Create `test/core/change-run/engine-selection-policy.test.ts`: default auto→reconciler for a supported pipeline; configured `legacy` → refusal with `engine_disabled_by_config` and zero Records on disk; `--engine reconciler` overrides configured `legacy`; forced reconciler on unsupported shape fails with the support reason; configured policy never affects a change that has only legacy run-state (resume path untouched)
- [x] 1.7 Wire `assertSingleEngineOwner` into production per design D8 (declared-engine discriminator): compute `canonicalPresent` instance-scoped from the RunStore (association-registry-bound, never name/alias — Gap-E lesson) and `legacyPresent` from the resolved run-state artifact — `auto-run.json` present AND `engine.effective !== 'reconciler'` (absent field or `'legacy'` → legacy owner; unparseable → legacy-present, fail-closed; `goal-run.json`/reports never count); refuse `engine_owner_conflict` at `pipeline start` (before Record creation) and in `complete`/`control`/`resume-run`, with an actionable message naming both artifacts; the runtime never writes/rewrites/deletes run-state to resolve a conflict. Verification: `grep -rn "assertSingleEngineOwner" src/` shows call sites at the launch and mutation seams, not only the definition
  - **RESOLVED (planner, design D8):** the original finding stands — the guard had ZERO production callers and naive wiring contradicts D3. The discriminator is now specified in design.md D8 and in the `ecp-change-run-runtime` delta requirement "Run-state declares its engine and ownership conflicts are refused from disk"; implement against those, not this note.
  - **WIRED.** `grep -rn "assertSingleEngineOwner\|classifyEngineOwnership" src/` now shows production call sites in `src/commands/pipeline.ts` (mutation seams via `resolveEngineOwner`; launch seam via `assertCanonicalLaunchAllowed`), not only the definition. Discriminator: `resolveLegacyOwnerSignal` in `src/core/pipeline-registry/run-state.ts`. `pipeline cancel` is deliberately NOT guarded — it is one of the two resolutions the refusal names, so guarding it would deadlock the change.
- [x] 1.8 Create `test/core/change-run/engine-ownership-wiring.test.ts`: run-state declaring `engine: reconciler` beside its canonical Record → `complete`/`control`/`resume-run` proceed; engine-less `auto-run.json` beside a Record → `pipeline start` AND every canonical mutation refuse with `engine_owner_conflict`, and both artifacts are byte-identical after the refusal; engine-less run-state alone → legacy resume path untouched; derived `goal-run.json` beside a Record with no run-state → no conflict; unparseable run-state beside a Record → refusal (fail-closed); archive + same-name recreate → the old instance's artifacts do not conflict with the new instance's Run (instance-scoped, Gap-E regression shape)

## 2. One v2-migration predicate; v1 parallel-only repair (Candidate 2)

- [x] 2.1 Export a single predicate `definitionRequiresV2Lowering` from the pipeline-registry layer (BoundedLoop or FanOut/Join in the normalized root, or `authoredVersion === 2`); the exported symbol name must be grep-able in `src/`
- [x] 2.2 Consume it in `lowerRuntimePlanInput` (`src/core/change-run/internal/lowerer.ts`), deleting the inline `requiresV2Lowering` node-scan so `grep -n "const requiresV2Lowering" src/core/change-run/internal/lowerer.ts` returns nothing
- [x] 2.3 Consume it in `resolveCapabilityBindings` and `resolveRuntimeExecutionProfile` (`src/core/pipeline-registry/profile-resolver.ts`): a v1 definition with FanOut/Join and no ReviewCycle loop now gets v2 hierarchical (`root:<id>`) capability bindings and remapped policy stages (extend the existing `remapPolicyStagesForV2` walk to cover FanOut/Join/member nodes)
- [x] 2.4 Consume it in `resolveReconcilerSupport` (`src/core/pipeline-registry/execution-plan-internal.ts`) so the strict binding comparison and the binding resolver derive expected node IDs from the same rule; keep fail-closed `unsupported_pipeline_shape` for incomplete bindings
- [x] 2.5 Create `test/core/pipeline-registry/v1-parallel-only-lowering.test.ts`: a v1 fixture with `parallelGroup` and no review-loop — bindings are `root:<id>`-keyed; support reports `supported_v2_parallel`; `lowerRuntimePlanInput` produces fan-out + member + join nodes; first `reconcile` admits the FanOut condition evaluator (semantic smoke, not just shape); incomplete bindings still report `unsupported_pipeline_shape` before launch
- [x] 2.6 Semantic re-review of the revived path (lesson: fixing one defect makes the next reachable): walk the admitted FanOut evaluator's brief/policy stage synthesis for v1 parallel-only (role, workspace access, evaluator capability) and record findings in the evidence ledger — any defect found becomes its own task before ship

## 3. `rasen-auto` + playbook convergence (Step E, engine-aware launch, run-state boundary)

- [x] 3.1 `src/core/templates/workflows/auto.ts`: add the engine-resolution step (read `pipeline show --json` engine support + resolved `runs.engine`), display `Engine: <engine> (<source>)` at run start beside the gate/selection policy lines; launch reconciler-engine runs via `rasen pipeline start` and route legacy-engine runs to the legacy playbook path
- [x] 3.2 `src/core/templates/workflows/_orchestration.ts` Step E: for reconciler-engine runs, replace the LEAD-owned Steps 1–5 mechanics with the canonical-Run protocol mirroring Step L (`pipeline resume-run` → dispatch per granted action, briefs composed from the `review-cycle` section → `pipeline complete` with the result contract); the replacement text must contain the literal string `pipeline resume-run` inside Step E
- [x] 3.3 Delete the prompt-owned duplicates for the reconciler path, each with its named replacement: round-cap ownership (`Default cap: 3` phrasing) → kernel bounded-loop cap (`test/core/change-run/review-cycle.test.ts`); clean determination → kernel ship guard (open Blocker/Major cannot settle clean, `test/core/change-run/facade-settle-completeness.test.ts`); author≠verifier enforcement → kernel same-actor rejection (`test/core/change-run/review-cycle-runtime.test.ts`); malformed-result acceptance → pre-commit validation (`test/core/change-run/facade-runtime.test.ts`). Verification: `grep -n "Default cap: 3" src/core/templates/workflows/_orchestration.ts` returns nothing
- [x] 3.4 Label the retained legacy mechanics explicitly as the legacy-engine path (one engine branch, not a second document); keep worker lifecycle, warm-reuse, briefing, Tier fallbacks, the H ladder for non-Run concerns, and portfolio orchestration engine-neutral
- [x] 3.5 Rewrite the run-state contract in the playbook (Step F) per design D3: reconciler-engine `auto-run.json` carries engine + operational bookkeeping (workers, gate-policy freeze, retention mode, strategyAttempts, session-relay generation) and labeled projections only; mechanical facts are read from the canonical view; legacy runs unchanged
- [x] 3.6 Rewrite auto's Resume section: reconciler-engine runs resume from the canonical frontier (`pipeline resume-run`); `pipeline resume` artifact heuristics remain legacy-only; never resume under a different engine than the owner
- [x] 3.7 Update the affected template-consuming specs' wording only where tasks 3.1–3.6 changed behavior (delta specs in this change already carry it); run the template checks (`pnpm exec tsx scripts/skill-check.ts` or the repo's template parity test) and fix drift
  - Ran the repo's template parity tests (`npx vitest run test/core/templates/` — 58/58 green after regenerating the golden hashes). **Count corrected (ECP-5 review finding):** section 3's template work regenerated **three skills** — `rasen-auto` and `rasen-review-cycle` in `06131648`, and **`rasen-goal` in `11dd2381`**, which this note omitted entirely — and **six values**, because each skill has an entry in BOTH `EXPECTED_FUNCTION_HASHES` and `EXPECTED_GENERATED_SKILL_CONTENT_HASHES`. Both numbers are written out because "three" alone would have been wrong the other way. `58/58` is left standing as the observation it was at the time; the suite is **59** at the integrated HEAD, after task 7.7 added a case. NOTE: `scripts/skill-check.ts` is pre-broken on this branch — it imports the deleted `test/helpers/skill-parser` (removed in `8d6ae877`, unrelated to this change) and cannot run.
- [x] 3.8 Discharge ECP-3 task 9.3: after 3.1–3.6, annotate `rasen/changes/ecp-goal-loop/tasks.md` 9.3 as discharged by this change (tick with a pointer note naming this change and the evidence tasks), not a bare tick
- [x] 3.9 Discharge ECP-3 task 9.4 with evidence: record in the evidence ledger the grep results showing `goal-iterate.ts`/`goal-report.ts` read `sections[].kind === 'goal'` and treat `goal-run.json` as projection-only (`grep -n "goal-run.json" src/core/templates/workflows/*.ts` shows only projection-language references); then annotate ECP-3 tasks.md 9.4 the same way as 9.3

## 4. Operations review-cycle section (Candidate 1) + provenance-by-construction constants (Candidate 3)

- [x] 4.1 Add a `ReviewCycleSection` component to `packages/ui/src/components/OperationsSection.tsx` consuming `getReviewCycleSection` (round/maxRounds, phase, outcome, findings with severity + status, actors, wait reason), wired beside the parallel/choice sections — after this, `grep -rn "getReviewCycleSection" packages/ui/src` shows at least one consumer outside `api/types.ts`
- [x] 4.2 Add the new UI strings to both locale catalogs in `packages/ui` (follow the parallel/choice section keys' naming pattern); the i18n completeness check must pass
  - **Task premise was false; closed at the root cause instead (user-approved, commit `d36a41f5`).** The Operations plane carried **zero** locale keys — `ParallelSection`/`ChoiceSection` shipped as hardcoded English with no `t()` anywhere in the file — so "follow the parallel/choice section keys' naming pattern" resolved to *no keys*, and `packages/ui` has **three** catalogs (`en`/`ja`/`zh-cn`), not "both". Rather than leave the plane half-translated, the whole plane is localized: **88 keys** under one `operations.*` namespace in all three locales, covering the new review-cycle section, ECP-4's parallel/choice sections, and every root-DAG label, wait/terminal sentence, control affordance and panel string.
  - **Key pattern established for later sections** (this is the first Operations namespace, so it sets the convention): `operations.<area>.<thing>` with areas mirroring the render tree — `wait`, `terminal`, `parallel`, `choice`, `review_cycle`, `detail`, `drift`, `control`, `summary` — snake_case leaves, and the repo's existing `.one`/`.other` plural convention (`archive.child_count.*`) for counted strings.
  - **The rule that matters:** values the SERVER projected are rendered verbatim and never translated (status, phase, outcome, severity, finding status, join state, member status/condition, wait/terminal codes, ids, digests), because the CLI and Management API print the same tokens and ECP-1's delta requires all three planes to agree. Identifier tooltips naming wire fields (`RunId:`, `WaitId:`) stay untranslated for the same reason. Pinned by `packages/ui/test/components/operations-i18n.test.tsx`, which asserts under `zh-cn` that the chrome moves AND the kernel tokens do not, and that the namespace carries genuine translations rather than copied English (key-for-key parity cannot detect that). Mutation-verified: rendering the loop phase from the catalog instead of the projection turns it red plus 2 parity tests — a failure the parity suites alone miss, since they run under the default locale where a translated token is still its English self.
  - i18n completeness check green (`packages/ui/test/i18n/catalog.test.ts`); catalogs edited by text insertion so their blank separator lines and CRLF endings survive.
- [x] 4.3 Create `packages/ui/test/fixtures/canonical-sections.ts`: move the four ECP-4 constants (`CANONICAL_PARALLEL`, `CANONICAL_PARALLEL_FAILED`, `CANONICAL_CHOICE`, `CANONICAL_CHOICE_UNDECIDED`) into it and add `CANONICAL_REVIEW_CYCLE` (mid-round with open findings) and `CANONICAL_REVIEW_CYCLE_ESCALATED` (cap reached, open Major); update `packages/ui/test/components/ecp4-parallel-choice-parity.test.tsx` to import from the module instead of local copies
- [x] 4.4 Create `test/core/change-run/ui-constants-provenance.test.ts` (node-side): import every constant from `packages/ui/test/fixtures/canonical-sections.ts` and deep-equal each against the REAL projector's output for its documented fixture — the kernel's answer, per the shared-reader lesson; leave `reviewer-r3-ui-constants-provenance.test.ts` untouched as regression
- [x] 4.5 Create `packages/ui/test/components/review-cycle-operations-parity.test.tsx`: render the real `OperationsSection` against the shared review-cycle constants; include deliberately-incoherent-section probes (round/findings/outcome that disagree) so client-side recomputation fails, mirroring the ECP-4 convention

## 5. Canvas suite closure (Candidate 4)

- [x] 5.1 Adjudicate failure 1 (fanout/join `data-editor-supported` expected `'false'`, actual `'true'`) against ECP-4's `executable-parallel-pipelines` Canvas requirements: if authoring is the shipped promise, update the assertions in `packages/ui/test/canvas/pipeline-canvas-page.test.tsx` with the spec citation in a comment; if edit mode is genuinely display-only for FanOut/Join, fix the product instead — record the verdict + citation in the evidence ledger
  - **Verdict (commit `bb448317`): the failure is not on fanout/join — it is on `composite`, and the test is STALE.** The loop covers `['composite','loop','fanout','join']`; actual values are composite=`true`, loop=`true`, fanout=`false`, join=`false`, and vitest reported the first mismatch. **Citation (composite/loop):** ECP-2 `rasen/changes/ecp-custom-composite/specs/executable-custom-composite/spec.md` — "Requirement: Canvas creates and references a Custom Composite declaration" ("The user SHALL be able to reference the declaration from the root graph via a `CompositeRef` node or embed it in a `BoundedLoop`") and "Requirement: Canvas deletes a CompositeRef or declaration". Implemented by commit `60bfeaa9`, which updated `packages/ui/test/canvas/draft.test.ts:453-455` but not this file. **Citation (fanout/join — product is correct, keep `false`):** ECP-4 `rasen/changes/ecp-full-feature/specs/executable-parallel-pipelines/spec.md`, "Requirement: Canvas provides parallel authoring with legality feedback" promises DISPLAY of structural details + legality validation, not root authoring; `draft.test.ts:455` pins `['FanOut','Join'].some(isV2EditableNodeKind) === false`.
  - **Product gap found while adjudicating, fixed here:** ECP-4's `FanOutDetails`/`JoinDetails` renderers in `V2NodePanel.tsx` were gated behind the editable-kind check, so the shipped scenario "FanOut panel shows members and limits — WHEN a FanOut node is selected in the Canvas" was **unreachable in production**. Moved to the read-only branch; covered by the new test "shows the FanOut and Join structural details on the read-only panel". Residual, NOT fixed: that scenario also says cap/budget are "configurable scalars" — ECP-4 shipped them as read-only `<output>` by design; making them editable would contradict the display-only boundary above. Flagged for the ledger, not silently ticked.
- [x] 5.2 Adjudicate failure 2 (creatable kinds missing `composite`) against ECP-2's `executable-custom-composite` Canvas-create requirement: locate the real creation flow; if it exists outside the root palette, rewrite the test to exercise that flow (citation in comment); if no edit-mode creation path exists, fix the Canvas — record the verdict + citation
  - **Verdict (commit `bb448317`): the failure is a DELETION assertion, not a creation one, and the test is STALE.** `pipeline-canvas-page.test.tsx:1191` removed the `composite` node and asserted it survived. **Citation:** ECP-2 `executable-custom-composite`, "Requirement: Canvas deletes a CompositeRef or declaration" — "The Canvas SHALL allow the user to delete a `CompositeRef` node from the root graph." The assertion encoded the pre-ECP-2 refusal, i.e. current behaviour rather than correct behaviour. Test now asserts deletion succeeds, plus a companion assertion that a FanOut removal IS still refused.
  - **Creation-flow question answered, and two product gaps fixed:** `addV2RootNode` (`PipelineCanvasPage.tsx`) DID implement `CompositeRef`/`BoundedLoop` branches, but `PalettePanel` rendered only the four pre-ECP-2 kinds — no affordance could reach them; and `isSafelyEditableV2Node` (`layout.ts`), which drives React Flow's `deletable`/`connectable`, still returned `false` for both kinds, so even the delete handler was unreachable through the real canvas. Both fixed, with the availability rule factored into one exported function per kind (`referenceableDeclaration`/`loopBodyDeclaration`) read by palette and insertion alike.
  - **OPEN when 5.2 was written; now CLOSED by section 5A below** — the other half of that ECP-2 requirement ("The Canvas SHALL allow the user to create a new `CompositeDeclaration`") had **no UI affordance at all**: `addDeclaration`/`removeDeclaration`/`addBodyStage` existed in `draft.ts` and were unit-tested, but `grep -rn "addDeclaration\|removeDeclaration\|addBodyStage" packages/ui/src` returned **nothing** — ECP-2 tasks 8.5 and 8.6 were false ticks. Escalated to the LEAD, taken to the user, and approved as added scope.
- [x] 5.3 Run `pnpm --filter @atelierai/rasen-ui test` — zero failed test files (the two baseline failures are gone and nothing else regressed); enumerate the full failure list on any red, never extrapolate from a truncated tail

## 5A. Custom Composite declaration editor — USER-APPROVED SCOPE ADDITION (closes ECP-2 8.5/8.6)

Added after task 5.2 found ECP-2's declaration-authoring requirement shipped
with a model layer and no affordance. The user chose "build it" over "record
the gap" or "defer to ECP-6", because the **Canvas Custom Composite exit
condition cannot be honestly evidenced** while the Canvas cannot author a
declaration — and this change's acceptance requires evidence for all 14.
Delivered in commit `b5e9fcd0`; ECP-2's ledger is annotated by task 5A.5.

- [x] 5A.1 Add `packages/ui/src/canvas/DeclarationsPanel.tsx` — declaration list with create (id input) and delete, plus a `DeclarationEditor` sub-panel for the selected declaration; wired into `PipelineCanvasPage` for v2 edit mode only. Closes ECP-2 8.5's "declaration editor sub-panel"
- [x] 5A.2 Contract editing: inputs and artifacts as add/remove/rename `name: type` rows, outcomes as a commit-on-blur list (the Gate/Choice outcomes idiom, so an intermediate `"a,"` draft is not destroyed). Closes ECP-2 8.5's "inputs list … artifacts list, outcomes list"
- [x] 5A.3 Body graph navigator + constrained body palette: body stages listed with kind and capability, add/remove wired to `addBodyStage`/`removeBodyStage`. The palette renders from the new exported `V2_BODY_PALETTE_KINDS` (`AtomicStage` only) — closes ECP-2 8.6, and applies this change's own census lesson: both palette vocabularies now live in `draft.ts` beside `V2_EDITABLE_NODE_KINDS` (`V2_ROOT_PALETTE_KINDS` moved out of `PalettePanel.tsx`) rather than becoming a fifth encoding of "which kinds go where"
- [x] 5A.4 Tests in `packages/ui/test/canvas/pipeline-canvas-page.test.tsx`, in the shape ECP-2's own task 8.7 names: create declaration → reference from root → save round-trip (asserted on the **posted definition**, not the DOM); delete-referenced-declaration blocked; duplicate id rejected; body palette constrained (all five forbidden kinds asserted absent). Plus a **discriminating probe** — a declaration referenced ONLY by a `BoundedLoop` body. Mutation-verified: re-implementing the reference check in the panel over `CompositeRef` nodes alone turns exactly that probe red while the doubly-referenced case stays green, so the coherent fixture could not have caught the orphaning bug
- [x] 5A.5 Annotate `rasen/changes/ecp-custom-composite/tasks.md` 8.5 and 8.6 — they are ticked `[x]` and were false. Do NOT untick silently and do NOT bare-tick: use the annotation-with-pointer convention this change uses for ECP-3's 13.1–13.4, citing commit `b5e9fcd0` and task 5A.4's tests as the evidence that they are NOW true
- [x] 5A.6 Record the consequence for task 7.6: a Custom Composite can now genuinely be **Canvas-authored**, so the dogfood matrix can produce that evidence instead of recording it unmet. (Before this, "author one via Canvas if not" was impossible and 7.6 could only ever have been closed as programmatic.)

## 5B. Canvas body-graph authoring — USER-APPROVED SCOPE ADDITION (closes ECP-2 8.2, verify-pass F1)

The `draft.ts` caller sweep that section 5A's census lesson prompted found the
zero-caller signature a **third** time. `addBodyConnection`/`removeBodyConnection`
had zero callers anywhere, and `updateBodyStage` — ticked by name in ECP-2 task
8.2 — did not exist in the repo at all. So `executable-custom-composite`'s
"Requirement: Canvas edits composite body stages" (`spec.md:102-117`) was
unreachable in three respects: the **edit** verb, "connects it to an existing
body stage", and "#### Scenario: Body connection creating a cycle is rejected".

Why the user chose **build** over recording the residual (the option taken for
the FanOut/Join "configurable scalars" wording): that residual was a display
nicety, whereas this one silently turns a Canvas-authored multi-stage body into
a **parallel fan-out instead of a sequence** — the reconciler admits
disconnected stages concurrently. That is a wrong pipeline, not a missing
affordance, and it would have been evidenced as an honest-looking dogfood cell.

**Affordance choice — (a) form-based connector, chosen deliberately.** The spec
says the user "draws a connection" but does not pin a mechanism; what it pins is
observable and satisfied either way — an edge gets created, and cycles are
refused by the same rules as root connections. The alternative, (b) a real
React Flow sub-canvas with drag-to-connect, buys only the widget at the cost of
a second flow instance with its own layout, selection model, handles and bug
surface, inside a closure slice whose stated constraint is not to grow. **If (b)
is ever wanted, none of this work is wasted:** the model, the cycle rule and all
four probes are identical — only `BodyConnections` in `DeclarationsPanel.tsx`
would be replaced.

- [x] 5B.1 Extract the cycle rule into ONE implementation: `reachesThrough` over a bare adjacency map, with `wouldCreateCycle` (root, v1 `requires` / v2 connections) and the new `bodyWouldCreateCycle` (one declaration's body graph) both delegating to it. This IS the compliance, not tidiness — `spec.md:104` requires that "Body connections SHALL be validated against the **same** DAG-cycle rules as root connections", which a second copy would break while satisfying the words
- [x] 5B.2 Add `updateBodyStage` (the spec's "edit" verb; ECP-2 8.2 ticked it by name and it never existed): edits a body AtomicStage's id and/or capability, and a **rename rewrites both endpoints of every incident body connection**, mirroring `renameV2Node`. Patching the node alone would leave edges pointing at an id the graph no longer has — a silently disconnected body, the same failure class F1 exists to close
- [x] 5B.3 Wire `addBodyConnection`/`removeBodyConnection` (zero callers before this) through `PipelineCanvasPage` handlers to a `BodyConnections` affordance in the declaration editor: from/to stage selects, Connect, and a connection list with Remove. **Every refusal stays in the model** — unknown declaration, unknown endpoint stage, duplicate edge, cycle — and the panel surfaces the throw, exactly as the delete reference guard and the F3 blank-id fix do. The target select deliberately does NOT hide illegal targets: a filter would be a second, invisible implementation of the cycle rule
- [x] 5B.4 Model tests in `packages/ui/test/canvas/draft.test.ts`: cycle refusal (including self-edge), unknown-stage and duplicate-edge refusal, rename-rewrites-connections, capability edit leaves the graph intact, blank/duplicate/unknown edit refusal. Plus the **cross-declaration isolation probe** — `b→a` is a cycle in declaration X (which has `a→b`) and legal in declaration Y (same stages, no edges); a pooled check or one reading `root.connections` refuses both, and a single-declaration fixture distinguishes neither
- [x] 5B.5 End-to-end tests in `packages/ui/test/canvas/pipeline-canvas-page.test.tsx` against the real `PipelineCanvasPage`, in ECP-2 task 8.7's shape: author a **connected** two-stage body and assert the edge reaches the **POSTed definition** (the discriminating pair — a "body has 2 stages" assertion passes for the broken product too); cycle rejected with the model's diagnostic and the edge not added; rename carries its connection; connection removal leaves its stages. All four probes mutation-verified, each mutation **grepped to confirm it landed** before trusting the colour
- [x] 5B.6 Annotate ECP-2 `tasks.md` 8.2 with the annotation-with-pointer convention, naming plainly that it ticked a symbol which exists nowhere in the repo and that the connection functions had zero callers — not unticked, not bare-ticked
- [x] 5B.7 Record the consequence for task 7.6 (Section 7 territory, not this one): the Canvas-authored dogfood should now exercise a multi-stage **connected** body, and its evidence must cite the **prepared plan showing sequential ordering**, not stage count. Sequential-versus-fan-out is the observable that separates correct authoring from the broken path; "the body has N stages" is true of both and therefore evidences nothing

## 6. Product wording, capability discovery, docs

- [x] 6.1 Enumerate every `reconcilerSupport` reason string emitted by `resolveReconcilerSupport` (grep the union in `src/core/pipeline-registry/execution-plan-internal.ts`) and verify each renders as product language in CLI `pipeline show` and in `packages/ui/src/canvas/EngineSupportPanel.tsx`; add any missing mapping (list the reason-code → copy table in the evidence ledger)
  - **The enumeration found a defect bigger than the copy table: NO read plane could report a `supported_*` reason at all.** Both discovery call sites (`pipeline show`, `handlePipelineDetail`) passed `profile: null` to `analyzeReconcilerSupport`, which short-circuits to `execution_profile_unavailable` — so `executable-parallel-pipelines` scenario 1 ("`rasen pipeline show` SHALL report ... reason `supported_v2_parallel`") was **unsatisfiable in production** while its kernel-level sibling test passed. Closed by `resolveDiscoveryReconcilerSupportProfile` (`profile-resolver.ts`), which resolves the SAME bindings through the SAME `resolveCapabilityBindings` the launch profile uses, reports the discovery digest (unchanged from the previous synthetic marker, so it can never be mistaken for a frozen launch profile), and returns `null` — fail-closed — when a binding cannot resolve.
  - **A second, latent defect surfaced while proving it:** the three expected-node-ID comparisons in `analyzeReconcilerSupport` silently depended on the profile arriving sorted by `normalizeProfileInput`. A discovery profile has no such pass, so the first attempt reported `unsupported_pipeline_shape` for every pipeline — a false NEGATIVE from ordering alone. `supportProfileNodeIds` now sorts, making the analyzer independent of how its input was built. Pinned by the reversed-order case in `test/core/change-run/engine-product-surface.test.ts`.
  - **Wire-mirror drift found and closed:** `packages/ui/src/api/types.ts`'s `ReconcilerSupportReason` was missing `supported_v2_executable` (ECP-2) and `supported_v2_parallel` (ECP-4), and carried two reasons (`unsupported_capability`, `unsupported_verify_policy`) the analyzer has never emitted. Both halves were invisible while every plane reported `execution_profile_unavailable`.
  - Copy table: `RECONCILER_SUPPORT_REASON_KEYS` (`src/commands/pipeline-messages.ts`) is keyed by the analyzer's own union, so a reason added without copy is a **type error**; `EngineSupportPanel`'s `REASON_LABELS` is keyed by the wire union for the same reason. Human `pipeline show` now renders `Engine support:` with engines, the reason CODE beside product copy, and the resolved policy — in en/ja/zh-cn. Table reproduced in the evidence ledger.
- [x] 6.2 Show the engine owner in run-facing surfaces that omit it: CLI `pipeline status` human output and the Operations run detail header render `engine: reconciler` from the wire view's existing field
  - `ChangeRunView.engine` has existed since run-spine and was rendered by **no** plane. `pipeline status` human output now prints `Engine: <engine>`; the Operations detail header renders it at `data-testid="ops-run-engine"`. Verbatim in both — it is a server token all three planes print alike. `packages/ui/test/components/cross-plane-parity.test.tsx` promised "without ... omitting any" and omitted exactly this field; it now asserts it, and `operations-i18n.test.tsx` asserts zh-cn does NOT translate it.
- [x] 6.3 Refresh `docs/architecture/executable-composite-pipelines.md`: correct the stale 0.1.6 scope header (Composite/BoundedLoop/GoalLoop/FanOut/Join execution HAS landed), add the engine-selection policy section (`runs.engine`, `--engine`, defaults, refusal), and record the legacy-engine retirement conditions (recorded, not enacted — decision is dogfood-driven)
  - The stale header was not the only stale claim: the RuntimePlan box in §2's diagram, the RuntimePlan/Reconciler rows in §3's table, and §9's scope list ALL still said Composite/Loop/FanOut/Join were rejected before Run creation. All corrected, with the correction called out in the header rather than silently rewritten. New §9 (engine selection policy + §9.1 ownership guard + §9.2 the five retirement conditions, recorded not enacted) and a pointer to this change's evidence ledger.
- [x] 6.4 Update `docs/autopilot.md` for the engine-aware auto behavior (engine line, canonical-Run driving, run-state boundary) and add a 0.1.6 migration note (engine ownership, compatibility projections `auto-run.json`/`goal-run.json`/reports, the disable switch) in the migration documentation
  - `docs/autopilot.md`: new §0 "Run engine", the engine row in the policy table, the Store layer in the precedence sentence, `Engine: reconciler (auto)` in the run-start display line, the run-state boundary, and engine-aware resume. `docs/migration-guide.md`: new "0.1.6: the reconciler engine owns Run progression" section leading with the ONE refusal an upgrading user actually hits (`engine_owner_conflict` on `pipeline start` for a change holding pre-convergence run-state) and its two resolutions, plus the projection rule, the off-switch, and what is unchanged.
- [x] 6.5 Cross-platform pass on all new/changed code: `path.join()`/`path.resolve()` only, no hardcoded separators; tests assert paths via `path.join()`
  - Swept the whole ECP-5 diff (`git diff 2fcd5438..HEAD -- src/ test/ packages/ui/`): zero hardcoded separators and zero `path.sep`/`process.platform` branches in new code. `engine-ownership-wiring.test.ts` (16 `path.join`) and `engine-selection-policy.test.ts` (11) build every path through `path.join`; `v1-parallel-only-lowering.test.ts` and `ui-constants-provenance.test.ts` touch the filesystem not at all. The `/p/proj_x/...` strings in the UI tests are ROUTE paths, not filesystem paths. Section 6's own code adds no path handling. Executed natively on Windows — see task 9.4.

## 7. Dogfood matrix (real fresh-process CLI, integrated HEAD)

- [x] 7.1 Build first (`pnpm build`) and run all CLI dogfood through the built `dist/` — stale-dist lesson; record the HEAD revision in the ledger
  - Rebuilt before every dogfood pass and re-run in full TWICE — after the section-7 fixes, and again after `3b33d5be` fixed the Blocker task 7.6 found — so every RunId in the matrix is anchored to ONE revision: `3b33d5be`. All four scripts re-run each time rather than patching the changed cell: a matrix whose rows come from different trees is not a matrix. The global `rasen` binary points at a different checkout and is never used; each scenario runs `node dist/cli/index.js` from a throwaway project with an isolated `XDG_DATA_HOME`.
- [x] 7.2 Create the matrix + ledger document at `rasen/work/issue-centered-automation-platform/executable-composite-pipelines/slices/product-closure/result.md` (ECP-1 `result.md` convention): pipeline × RunId × engine owner × terminal state × evidence refs
  - Twelve cells (pipeline x provenance x RunId x engine owner x terminal x evidence) in the ledger, driven by four scripts. The new one, `test/dogfood-ecp5-closure.mjs`, is a sibling of the three existing dogfood scripts and shares their conventions.
- [x] 7.3 Cite existing real-Run evidence with pointers: bug-fix ReviewCycle (ECP-1 RunId `b23b2c…`, `slices/review-cycle-vertical-closure/result.md`), custom composite success/failure/recovery (ECP-2 tasks 14.2–14.5), goal-loop-measure/-research/-exhaustion (ECP-3 tasks 12.2–12.4), full-feature (ECP-4 tasks 13.3–13.4)
  - Cited AND re-run. Every sibling cell carries a RunId produced at `3b33d5be` rather than the sibling's original: citing a sibling's RunId would evidence that slice's tree, not the integrated one. The originals (ECP-1 `run:b23b2cce…`) are named beside the new ones so provenance survives.
    **Finding while re-running:** `test/dogfood-review-cycle.mjs` asserted the raw `actor_separation` token, which the CLI stopped printing once the refusal became a localized product message — so the script printed WARNING on a passing guarantee. The rejection happened every time; only the assertion was stale. Fixed, and the exact-tree caveat that created is now moot (the fix is committed and the current cell came from a clean tree) — recorded as superseded in the ledger rather than deleted.
- [x] 7.4 Run a REAL `small-feature` Run end-to-end on the reconciler engine (fresh process, real CLI): capture RunId, per-stage ActionIds, ReviewCycle round/phase evidence, terminal `completed` view — this cell currently has only a normalization test
  - Scenario A. `run:308c9ca2d42f576a…`, engine `reconciler (default)`, 9 actions, terminal **`completed`**; review-cycle round 1/3 with F1 (major) `resolved` and outcome `clean`; per-stage ActionIds recorded.
- [x] 7.5 Run a REAL `goal-loop-evaluate` Run through at least one full work→judge iteration to a genuine termination: capture RunId, judge evidence (score/gaps), terminal view — closes the Measure-AND-Evaluate exit condition
  - Scenario B. `run:92156d532e5c2762…`, terminal **`completed`** through define-goal -> work -> evaluate judge (`satisfied`, `gaps: []`, one rubric criterion) -> ship -> retain -> archive. `dogfood-goal-cycle.mjs` scenario 3 (`run:1b8e7a48…`) already drove the LOOP to `satisfied`; this carries the whole Run to a terminal.
    **The dogfood's first attempt was REFUSED** with "The worker cannot judge their own GoalCycle work" — the goal-loop actor-separation guarantee firing against a real CLI call, which is better evidence for the shared loop lifecycle than a passing run would have been.
- [x] 7.6 Verify whether ECP-2's composite dogfood was Canvas-authored (read its recorded evidence); if programmatic, author a Custom Composite via the Canvas UI, save it, and run it to completion via the reconciler — capture the definition export + RunId; if already Canvas-authored, record the pointer
  - **It was not.** ECP-2's task 14.1 permitted "or construct programmatically" and its dogfood (commit `c6aa7026`, `test/core/change-run/ecp-composite-dogfood.test.ts`) is a facade-level test over a hand-written `DefinitionSourceV2` literal with an in-memory store — no CLI process, no Canvas. Not a criticism of that choice: until `b5e9fcd0` (task 5A) the Canvas had no affordance to create a declaration, so the option its task offered was unreachable.
    Produced here end to end: `packages/ui/test/canvas/canvas-authored-composite-export.test.tsx` drives the REAL `PipelineCanvasPage` over an EMPTY v2 definition (zero declarations, zero root nodes) through the real affordances and captures **what save POSTs**; the dogfood feeds it the machine's real production capability catalog, installs it with `rasen pipeline save`, reads discovery back (`supported_v2_executable`), and runs it to **`completed`**.
    **Re-run after F1 (`7f2fc33d`) with a CONNECTED two-stage body** — `run:07ef84266520254b…`. The cell asserts **ordering, not stage count**, because a disconnected two-stage body is a different pipeline (the reconciler admits disconnected stages concurrently), so "the body has N stages" is true of the correct and the broken shape alike. Structural: `stage-2.requires = [stage]`, `stage.requires = []`, directional. Behavioural, and the one that settles it: `first frontier: ["root:composite-ref/stage"]` — exactly ONE action, read before any completion. Falsifying contrast: the pre-fix cell `run:3c50063b…` put BOTH stages on the first frontier.
    **This cell found a Blocker and stopped rather than working around it.** `pipeline save` refused the Canvas's own output with `PORT_MISMATCH`: no Canvas-authored connection into an AtomicStage backed by a real capability could save, because all 42 production descriptors declare no inputs and the synthetic control contract fired only for a MISSING descriptor at `version: 'legacy'`. Root connector affected identically — a pre-existing hole F1 made reachable in a second place, not an F1 regression. Fixed in `3b33d5be`. The cell could have been made green by fabricating a catalog entry with declared ports — exactly what ECP-2's and F1's fixtures do, which is why the hole survived them — and that would have been passing evidence for something the product cannot do.
    **Cycle probe (approved addition):** the closing edge is **refused by the Canvas** — model throws, panel surfaces `cycle`, graph unchanged; guarded by asserting no toast was showing beforehand. It claims the CANVAS refuses, NOT that the server validator is reachable; ECP-2's `GRAPH_CYCLE` (`ecp-composite-validation.test.ts:178`) is cited as the backstop, and the ledger states the two-link composition explicitly rather than implying an end-to-end proof.
    **`profileDigest` moved** `sha256:e0d7168b…` → `sha256:de2b441d…`, correctly: a connected body is a different definition. Pre-announced because the engine reviewer reproduced the old value byte-identically.
- [x] 7.7 Drive one converged `rasen-auto` reconciler-engine run (the Section 3 product path) over a real change far enough to cover launch → granted action dispatch → result submission → review-cycle section read — the convergence's own dogfood, recorded with RunId and the auto transcript pointer
  - Scenario D. `run:fbcebfe852884e77…` -> **`completed`**: engine read from `pipeline show --json` (`Engine: reconciler (default)`), one canonical Run launched, then 10 `resume-run` -> dispatch -> `complete` cycles with 9 `review-cycle` section reads tracking `phase: review -> triage -> fix -> re-review` and `outcome: null -> clean`. Full transcript in the ledger.
    **This cell found two real defects** by following the playbook literally: a phantom `--action-id` flag that does not exist on `pipeline complete` (also in ECP-1's `rasen-review-cycle` template, so it predates this slice), and `pipeline complete` dropping the actions it granted — which made the converged loop unfollowable at its own seam. Both fixed; see the ledger and commit `11ce4d69`.
- [x] 7.8 Run the full root/composite crash-recovery and cross-plane parity suites by name and record pass counts in the ledger: `test/core/change-run/fault-journeys.test.ts`, `archive-recreate-journeys.test.ts`, `ack-loss-journeys.test.ts`, `cross-plane-parity.test.ts`, `review-cycle-parity.test.ts`, `ecp-composite-parity.test.ts`, `projector-parallel-choice.test.ts`, plus the new `ui-constants-provenance.test.ts` and both UI parity files
  - Root: 8 files / **102 passed** / 0 failed (fault-journeys 50, archive-recreate 13, ack-loss 12, cross-plane-parity 2, review-cycle-parity 2, ecp-composite-parity 5, projector-parallel-choice 11, ui-constants-provenance 7). UI: 3 files / **22 passed** / 0 failed. Per-file counts in the ledger.

## 8. Exit-conditions evidence ledger (research doc §15.4)

- [x] 8.1 Enumerate all 14 exit conditions from `rasen/work/issue-centered-automation-platform/executable-composite-pipelines/deterministic-pipeline-kernel-research.md` §15.4 (lines 1828–1847) verbatim in `result.md`, each with ≥1 concrete evidence pointer (test file, RunId, doc section, or commit); conditions without evidence are listed as OPEN with the blocking gap — no condition is marked satisfied without a pointer
  - All 14 quoted verbatim in the ledger with per-condition evidence. **14 of 14 evidenced, 0 OPEN.** Condition 11 cites the WIRING commit `2fa693d8` plus `engine-ownership-wiring.test.ts`, deliberately NOT `engine-ownership.test.ts` alone — that file passed for the whole period in which the guard had zero production callers, so citing it would have been this portfolio's seventh false tick.
- [x] 8.2 Cross-check the Slice E closure list (research doc §11 Slice E, lines 1477–1487) against the ledger and the shipped siblings; record any residual item and its disposition (done-with-pointer, this-change task, or explicitly user-parked)
  - All eight Slice E items dispositioned in the ledger. One **residual, recorded not fixed**: ECP-4's FanOut/Join cap and budget render as read-only `<output>` while its shipped scenario calls them "configurable scalars". Adjudicated at task 5.1 (`bb448317`) — making them editable would contradict the display-only authoring boundary the same delta sets, so it is left for a deliberate ECP-6 decision rather than silently ticked. One item is **done as recording only** by design: the legacy-retirement conditions are written down (`docs/architecture/executable-composite-pipelines.md` §9.2) and NOT enacted.
- [x] 8.3 Re-verify the ledger's grep-falsifiable claims in one pass (the Section 1–4 grep assertions) and paste the command outputs into the ledger — the anti-false-tick sweep, by claim, not by token
  - Twelve grep/`node -e` commands re-run in one pass, outputs pasted verbatim into the ledger (sections 1-6 claims plus section 7's phantom-flag removal), at `3b33d5be` — re-run again after the Blocker fix. Every one holds.

## 9. Release checks and portfolio verification (discharges ECP-3 13.1–13.4)

- [x] 9.1 `pnpm build` + root `npx tsc --noEmit` + UI tsc — zero errors
  - Build clean; root tsc **0 errors**; `packages/ui` tsc **0 errors**. Natively on Windows 11, Node 24.14.0, at `3b33d5be`.
- [x] 9.2 Full test suite (root + `packages/ui`) — zero regressions; on any failure enumerate the complete FAIL list per file with attribution (known-flake protocol: clean temp, rerun isolated) — never report from a truncated tail
  - **At the final HEAD `3b33d5be`, running alone: root 393 files / 6364 passed / 1 failed / 33 skipped; UI 56 files / 604 passed / 0 failed.** The single failure is `token-audit/zed`, pre-existing and unrelated, verified by reproducing it against the `2fcd5438` version of that test. The four Windows CLI-spawn flakes did not recur — the expected consequence of having the machine alone, and the stronger confirmation of the isolated re-runs below.
    At the previous revision: root **393 files / 6357 passed / 6 failed / 33 skipped**; UI **56 files / 592 passed / 0 failed**. The complete FAIL list is enumerated per file with attribution in the ledger, each suspect re-run in isolation before being called a flake: 4 tests across 3 files are the known Windows CLI-spawn flake (EBUSY/EPERM rmdir + timeout, all passing in isolation); 1 was a **REAL regression fixed here** (`command-registry.test.ts` — `--engine` shipped in section 1 without its completion-registry entry or its three locale descriptions); 1 (`token-audit/zed`) is **pre-existing and unrelated**, verified by checking out that test at `2fcd5438` and reproducing the failure.
    **An invalidated first attempt is recorded rather than deleted**: it reported 235 failures because the release-contract checks were run concurrently, and two of them remove and rebuild `dist/` while hundreds of tests spawn `node dist/cli/index.js`. Its numbers appear nowhere as evidence.
- [x] 9.3 ESLint on changed files — zero errors
  - `pnpm lint` at `3b33d5be`: **0 errors**, 1 warning at `facade-settle-completeness.test.ts:139` (an unused eslint-disable introduced by `27faedd3` in `ecp-settle-completeness`) — reported, not opportunistically fixed.
- [x] 9.4 Windows cross-platform verification on this machine: run the goal-cycle and change-run suites (`npx vitest run test/core/change-run/`) natively on Windows and record the result — this plus 9.1–9.3 is the evidence that discharges ECP-3 13.1–13.4; annotate `rasen/changes/ecp-goal-loop/tasks.md` 13.1–13.4 with a pointer to this change's ledger (tick with note, not bare tick)
  - Every check in section 9 ran natively on Windows 11 (`win32`, Node 24.14.0) at `3b33d5be`, including the whole `test/core/change-run/` suite and four real fresh-process CLI dogfood scripts (re-run in full after the Blocker fix). ECP-3 tasks 13.1-13.4 are annotated in `rasen/changes/ecp-goal-loop/tasks.md` with a pointer to ledger Section 9 — annotation with evidence, never a bare tick.
- [x] 9.5 Run the release-contract checks: `node scripts/release-contract.mjs`, `node scripts/pack-version-check.mjs`, `node scripts/paired-pack-check.mjs` (lockstep CLI=UI) — green at the current 0.1.5 versions; record that the 0.1.6 bump is the user's release-time action (version discipline: report, never bump)
  - Re-run at `3b33d5be`: `release-contract.mjs` -> `verified lockstep release contract 0.1.5`; `pack-version-check.mjs` -> OK; `paired-pack-check.mjs` -> `verified paired CLI/UI packages 0.1.5`. Versions stay **0.1.5** in-repo; the 0.1.6 bump is the **user's** release-time action, reported not performed.
    **Operational finding, widened after the engine reviewer lost two runs to the same trap:** the safe rule is **no root vitest, no build, and nothing triggering `prepare`, concurrently with any of the others** — my original "pack-checks only" reading was too narrow. Root `vitest.setup.ts`'s globalSetup calls `ensureCliBuilt()`, which launches a full `pnpm run build` when `dist/cli/index.js` is missing, and `build.js` opens with `rmSync('dist')` — so once any build has deleted `dist`, the next root vitest invocation becomes a second `dist` destroyer, even one with no `dist` dependency in its test bodies. Mechanism, diagnostic signature and durable-fix recommendations are in the ledger.
- [x] 9.6 Verify no stray artifacts: `git status` clean except intended files; `packages/ui/package-lock.json` untouched (user-parked); no edits under `rasen/changes/ecp-*` siblings beyond the two annotation tasks (3.8, 3.9, 9.4)
  - `git status` carries only this change's intended files; `packages/ui/package-lock.json` remains untracked and untouched; sibling change directories are edited only by the annotation tasks (ECP-2 5A.5, ECP-3 3.8/3.9/9.4). `pack-version-check.mjs` leaves a gitignored `.tgz` in the repo root — deleted after the check. Dogfood temp directories are removed by the scripts and verified absent.

## 10. Session-contract placeholder hygiene (design D9 — no session semantics designed here)

- [x] 10.1 Stamp the synthetic evaluator's non-reuse as definitional: in `synthesizeEvaluatorPolicyStage` (`src/core/pipeline-registry/profile-resolver.ts`) set `provenance.sessionReuse: 'definition'` (evaluator synthesis ONLY — review-cycle and default synthesis keep `'default'`, that placeholder stamp is truthful); the literal `sessionReuse: 'definition'` must appear exactly once in the file
- [x] 10.2 Add the additive fidelity field `sessionReuseAuthored` (`'none' | 'stage' | 'run-planner' | 'review-thread'`, optional) to the EffectiveRunPolicy stage type and the agent action's session block (`buildAgentAction` in `src/core/change-run/internal/actions.ts`): captured verbatim from the authored stage value at the CLI resolver's flattening site (`src/commands/pipeline.ts` session-reuse mapping), OMITTED when nothing was authored or the stage is synthesized (undefined-dropped, so existing fixtures' digests are byte-identical); no behavior reads it; the two-value `reuse` enum is unchanged
- [x] 10.3 Doc-comment all four hardcoded write sites (`profile-resolver.ts` ×3 synthesis functions, `commands/pipeline.ts` resolver) as placeholders naming the spec requirement: each site carries a comment containing the literal string `placeholder` and `Recorded session guidance is placeholder` so the next implementer hits the contract in code, not only in specs; do NOT change the `10_000` / `1` constants (design D9: the real values are the Session execution layer's design output)
- [x] 10.4 Create `test/core/pipeline-registry/session-contract-fidelity.test.ts`: authored `sessionReuse: 'review-thread'` → effective stage AND committed action session block carry `sessionReuseAuthored: 'review-thread'` beside `reuse: 'same-invocation'`; authored `'stage'` and `'run-planner'` remain distinguishable from each other in the recorded block; unauthored stage → `sessionReuseAuthored` absent from stage, action, and serialized JSON; evaluator synthesis → `provenance.sessionReuse === 'definition'`; review-cycle + default synthesis → `provenance.sessionReuse === 'default'` and `handoffTokenLimit`/`reuseRoundLimit` provenance `'default'` at all four sites (pinning the placeholder stamp the spec rule keys on)

## 11. Round-2 verify findings (found by the re-review of the post-fix delta, fixed here)

The round-1 verify pass produced one Blocker (`PORT_MISMATCH`, fixed at
`3b33d5be`), one Major (F1, built at `7f2fc33d`), and four smaller findings. The
**re-review of that post-fix delta** — `7f1b71bf`, `7f2fc33d`, `6a4ea56b`,
`df106499`, `3b33d5be`, `f111ada3` — is a separate pass by a non-author, and it
found three more. The Blocker's own open question is answered in 11.4.

- [x] 11.1 **[Minor] The dogfood cell the ledger leans on hardest could not fail.** Scenario C's semantic claims — `supported_v2_executable`, the directional `requires` chain, the one-entry first frontier, terminal `completed` — were `console.log`ged and dumped into `out`, never asserted, so `node test/dogfood-ecp5-closure.mjs C` exited **0** for a disconnected body too. The only thing that separated the correct pipeline from the wrong one was a human reading four lines of output — which is why the handoff had to spell out "green means, in order: …". Failure scenario: a later slice re-runs the script for fresh evidence, gets exit 0, re-anchors the ledger cell with the new RunId, and the frontier line quietly says two. Now `throw`n: discovery reason, `bodyNodeCount === 2`, `secondRequiresFirst && !firstRequiresSecond` (directional — `secondRequiresFirst` alone is also true of a body wired both ways), the exact one-entry frontier, and the terminal
- [x] 11.2 **[Minor] The capability half of `updateBodyStage` had zero production callers** — the same zero-caller signature this slice closed three times, inside the fix for it. `createBodyStage` stamps `firstExactCapability()` into every body stage and 5B shipped no affordance to change it, so a Canvas-authored multi-stage body could only ever repeat ONE capability ("run X, then run X again"), while `executable-custom-composite`'s own scenario says "the user adds an AtomicStage **with capability `skill:rasen-apply`**". The root graph has had an "Exact capability revision" select since ECP-2 (`V2NodePanel.tsx`); the body graph now offers the same list from the same filter. `capabilityAvailable: boolean` is **replaced** by `capabilities: readonly {id, version}[]` rather than added beside it — "can a body stage be added" and "which revision may it bind" are one fact (`capabilities.length > 0`), and two props would have been two owners of it
- [x] 11.3 **[Observation → mechanism] The Canvas's control ports were bound to the kernel's accepted sets by a comment.** `3b33d5be` fixed the Blocker by widening the kernel and by making the Canvas emit `CONTROL_SOURCE_PORT`/`CONTROL_TARGET_PORT`; the kernel-narrowing direction is covered (`definition.test.ts` loops over all three accepted names), but a **UI-side** edit to either constant left every kernel test and every UI test green while production authored unsaveable definitions — the failure mode the Blocker actually was. The two constants moved from `PipelineCanvasPage.tsx` into the model module `draft.ts` and are now exported, and `test/core/pipeline-registry/canvas-control-port-provenance.test.ts` imports them kernel-side and runs the **real `prepare`** over a production-shaped catalog (`inputs: []`, `outcomes: ['done']`), with a negative control so the test can fail
- [x] 11.4 **The Blocker's open question, closed by a non-author.** Round 1 asked whether any downstream fail-closed guarantee — `unsupported_pipeline_shape` in particular — was relying on `PORT_MISMATCH` to reject a shape `3b33d5be` now admits. **No weakening exists, on three independent paths.** (a) `unsupported_pipeline_shape` is computed by comparing the *sorted set of expected node IDs* (root AtomicStages + referenced declaration-body AtomicStages) against the profile's binding node IDs (`execution-plan-internal.ts`) — it never reads a connection or a port, so it cannot have depended on port validation. (b) Nothing downstream consumes `to.port` at all: the only port read outside the validators is `lowerer.ts:868`, which matches `from.port` against outcome names (the OUTPUT side, untouched), and `incomingRequirements` (`lowerer.ts:79`) derives `requires` from node IDs alone — so the newly-savable shapes lower through exactly the code fixture-catalog tests already exercise. (c) The one other `contractForNode` consumer, the reachable-outcome proof (`resolveGraphTerminalOutcomes`), gets **more** fail-closed, not less: pre-fix a refused edge set `complete = false`, which *suppressed* the "declares terminal outcome X but the graph cannot produce it" diagnostic; post-fix that check runs. And no previously-savable definition changes verdict, because any definition with such an edge was already refused outright, while the legacy branch widened from `{start}` to a superset containing `start`
