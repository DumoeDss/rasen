## 1. Binding module foundation

- [x] 1.1 Create `src/core/issue-execution/types.ts`: `IssueLaunchBinding` (issueId, nodeId, changeInstanceId, alias, projectId, targetLineId, launch `{ form: 'workspace-pair' | 'project-checkout', cwd, attachedRoots }`, pipeline, mode `'fresh' | 'already-running' | 'already-complete'`, runStatePath?, locatedBy?), the closed refusal taxonomy (`issue_start_requires_plan`, `issue_start_frontier_ambiguous`, `issue_start_node_not_runnable`, `issue_start_unprepared`, `issue_start_launch_context_failed`, `issue_start_pipeline_conflict`, `issue_start_pipeline_unknown`), `ResolveIssueLaunchBindingInput` (detail, status, workspaceEntries, injectable `launchContextFor`, optional `pipeline`, `nodeId`), and the ok/refused result union.
- [x] 1.2 Create `src/core/issue-execution/binding.ts` + `index.ts` barrel: frontier derivation per design D3 (change-kind ∧ `not-started` ∧ dependsOn observations all terminal), `--node` validation with observation-based blocker naming, launch-route resolution per D4 (index pair → injectable L6 checkout → unprepared refusal with the exact `store workspace plan --existing-change …` line; L6 diagnostics passed through), pipeline resolution per D5. Explicit lookups only; `path.join` throughout.
- [x] 1.3 Unit tests over synthetic `IssueDetail` + `IssueStatus` + injected entries/launch contexts: every route and refusal, ambiguous-frontier candidate naming, already-running/already-complete modes, pipeline conflict and unknown-pipeline refusals, determinism (same inputs twice).

## 2. Projection widening (the one status seam)

- [x] 2.1 Extend `src/core/issue-status/types.ts`: `ProjectIssueStatusInput` += optional `workspaceEntries` (storeUid-filtered) and `storeRoot`; `IssueNodeStatus` += `locatedBy: 'execution-root' | 'workspace-index' | null` and `attribution: { pipeline: string | null; sessions: readonly IssueNodeSession[]; evidenceLocator: string | null }` with `IssueNodeSession` carrying stage id, role, runtime, and only durable pointers (`sessionId?`, `threadId?`, `transcript?` — `agentId` excluded by construction).
- [x] 2.2 Extend `src/core/issue-status/projection.ts`: after the C1 current-root chain, probe each matching index entry's execution root with its own chain (ephemera first, then the entry's planning-side active-change address; no work-dir leg on index roots — design D6); retain the located run-state's pipeline and per-stage durable worker facts; resolve `evidenceLocator` from the current `changesDir` or the store-side active-change address via `storeRoot` + committed claimant ids; portfolio-observed nodes report empty `sessions` honestly. Omitted inputs reproduce C1 behavior byte-for-byte.
- [x] 2.3 Unit tests: index-located observation from a store-root-style read (no execution root input), `locatedBy` labelling, current-root-first precedence, multi-entry refusal-free first-hit behavior, attribution fact extraction (incl. agentId exclusion and the no-sessions case), and a C1-regression run (existing suites green unchanged with no new inputs).

## 3. CLI surface

- [x] 3.1 Add `rasen store issue start <issue-id>` to `src/commands/store-issue.ts` (`--node`, `--pipeline`, `--store`, `--json`): resolve show + status (widened inputs: `resolveProjectionContext` + once-per-command index gather via `listAllWorkspaceIndexEntries` filtered by the resolved store's uid), call the binding module, render the launch contract (human) / emit JSON; renderers English-literal per file convention; `--pipeline` validated against the prepared pipeline registry when supplied.
- [x] 3.2 Enrich `store issue show`'s per-node block with attribution lines (pipeline, locatedBy, session pointers, evidence locator) and the JSON `status.nodes[].attribution`/`locatedBy` fields — both forms carry the same facts.
- [x] 3.3 Locale entries for the `start` subcommand and its options in en/ja/zh-cn; run the commander structural-parity check.
- [x] 3.4 CLI tests (real `runCLI` + fixture): fresh-binding emission, ambiguous/blocked/unprepared/no-plan refusals with named candidates/blockers/preparation, already-running reporting with recorded pipeline, human/JSON parity, degraded reads (store-root cwd with no entries), `--pipeline` conflict refusal, exit codes.

## 4. Guards and cross-platform

- [x] 4.1 Extend `test/core/issue-status/issue-status-read-only-guard.test.ts` (or add a sibling for the new module) to cover `resolveIssueLaunchBinding` and the widened projection over the same file set plus the workspace index and `.rasen/planning-binding.json` — byte-identical before/after.
- [x] 4.2 Cross-platform verification: `path.join`-built expectations for index-root chains and evidence locators; Windows-semantics assertions; existing Windows CI leg green (config rule: file-path changes need Windows verification). [all suites run on win32 locally; CI legs pend push]

## 5. Dogfood — the closed loop (design D9)

- [x] 5.1 Phase A: rebuild the dogfood store per the C1 recipe and trap list (OS temp, hand-declared `layoutVersion: 2`, `add-project` with expected config double-write, `target-line add/set-ref` with code ref `feat/issue-layer`, explicit-list seeding with quoted-scalar v2 identities, issue + three-node plan), capturing setup receipts. [store branch needed rename master→main; membership keyed by the real project id — see evidence/dogfood-receipt-summary.md]
- [x] 5.2 Phase B (receipt 1): from the STORE ROOT, `store issue start issue-layer-phase1` before any index entry — frontier g-001, L6 checkout route, contract cwd = this worktree; capture command + output. [cwd = the project's REGISTERED checkout (the main checkout) — the "worktree registered as issue-layer" premise is unachievable under the registry's worktree-share rule; recorded in the receipt summary]
- [x] 5.3 Phase C (receipt 2): `store workspace plan --existing-change … --execution-worktree <this worktree>` → `apply`; from the STORE ROOT, `store issue show` observes g-001 run-terminal and g-002 in-flight via `locatedBy: workspace-index`; `store issue start` now reports g-002 already-running with its recorded pipeline. Apply D9's named fallbacks if pair-reuse is refused, recording which path ran. [reuse refused by `execution-root-outside-repository`; D9 fallback (fresh execution worktrees + `initializeRunState`-born state) ran; already-running mode covered by unit+CLI suites over real writer bytes — recorded in the receipt summary]
- [x] 5.4 Phase D: teardown (`store workspace cleanup`, `store remove --yes`, `git restore rasen/config.yaml`, delete temp tree), receipts preserved under `evidence/`; verify zero branch footprint beyond the change's own files.

## 6. Bookkeeping

- [x] 6.1 Update the `architecture-index` skill: `src/core/issue-execution/` module map + quick-locate rows (module and the `store issue start` subcommand) + the store-engine domain detail.
- [x] 6.2 Run `node bin/rasen.js validate issue-execution-binding` (positional form) and the full affected set green (`issue-execution` + `issue-status` + store-issue CLI suites); confirm `git diff` over `src/core/pipeline-registry/`, `packages/ui/`, and both `package.json` files is empty. [round-1 fix re-run: 10 files / 103 tests passed, exit 0 — issue-execution 30 (binding 24 incl. the D3 pin, guard 6), issue-status 33 (projection 21, C1 guard 5, widening 7), store-issue CLI 22 (cli 9, start 10 incl. the command-seam write-guard, status-cli 3), completions 18 (presentation 11, registry 7); plus the two store-query suites 41/41 exit 0. The original bracket's "124" measured a different valid set — store-aggregate-query 33 + store-query-read-only-guard 8 in place of cli-presentation 11 + command-registry 7 (101 + 41 − 18 = 124) — not a double count; corrected here so one canonical number exists. validate exit 0; all four fences byte-empty; whitespace clean; C1 suites byte-untouched]
