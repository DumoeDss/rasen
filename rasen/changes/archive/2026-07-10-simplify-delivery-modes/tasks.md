## 1. Type narrowing and legacy parse layer

- [x] 1.1 In `src/core/global-config.ts`: narrow `export type Delivery = 'skills' | 'both'`; add `type LegacyDelivery = 'commands' | 'skills-first' | 'commands-first'` and a `normalizeDelivery(raw: unknown)` helper with the mapping `skills-first → skills`, `commands → both`, `commands-first → both`, unrecognized/undefined → default `'both'` (design D1)
- [x] 1.2 In `getGlobalConfig()`: run the parsed `delivery` through the normalizer; when a legacy value is found, print the one-time consolidation notice (name the old and new value, mention skills are always installed) and best-effort `saveGlobalConfig()` the mapped config inside try/catch — persistence failure must not fail the read (design D2); unrecognized values fall back to `'both'` WITHOUT persisting
- [x] 1.3 In `src/core/config-schema.ts`: narrow the delivery zod enum to `['both', 'skills']` `.or()` a legacy enum `['commands', 'skills-first', 'commands-first']` with a `.transform()` to the mapped value, so `validateConfig` accepts old values on whole-file validation and explicit sets (design D3); keep `DEFAULT_CONFIG.delivery = 'both'`
- [x] 1.4 Compile sweep: `npx tsc --noEmit` (or `node build.js`) and fix every consumer the narrowed union flags — the compiler errors ARE the consumer checklist; confirm it flags exactly the files in design's consumer table

## 2. Consumer simplification (the fix-goal-deploy-gap machinery removal)

- [x] 2.1 `src/core/init.ts`: generate skills unconditionally (drop `shouldGenerateSkills`, keep `shouldGenerateCommands = delivery === 'both'`); delete the commands-only skill-removal block, the `commands-first` restricted-removal block, the `skills-first` command-removal block, the entire `removeSkillDirs` method with its `restrictToCommandCounterparts` param, the `removedSkillCount` plumbing, and the "Removed: N skill directories (delivery: commands)" summary line; keep `removeCommandFiles` under `delivery === 'skills'` (design D5)
- [x] 2.2 `src/core/update.ts`: same shape as 2.1 — unconditional skill generation, delete `removeSkillDirs` + both `*-first` branches + `removedSkillCount` + its summary line; KEEP `removeUnselectedSkillDirs` (profile deselection) and `removeCommandFiles` under `delivery === 'skills'`
- [x] 2.3 `src/core/shared/skill-generation.ts` + `src/core/shared/index.ts`: delete `deduplicateForDelivery` and its re-export; unwrap the three call sites (init.ts ×2 incl. the summary-count block at ~818-825, update.ts ×1) to use the template lists directly (design D4)
- [x] 2.4 `src/core/profile-sync-drift.ts`: in `hasToolProfileOrDeliveryDrift` make skills forward-required for every selected workflow (delete the skills-absent else-branch including `commands-first` skill-only sparing); commands forward-required iff `delivery === 'both'` with adapter; command files present under `skills` delivery remain drift; in `hasProjectConfigDrift` set `includeSkills` always true, `includeCommands = delivery === 'both'` (design D7)
- [x] 2.5 `src/core/migration.ts`: change `inferDelivery` commands-only arm to return `'both'` (design D6); both/skills arms unchanged
- [x] 2.6 `src/commands/config.ts`: reduce `deliveryChoices` to `both` and `skills`; update the helper text "Delivery = where workflows are installed (skills, commands, or both)" and any other 3-mode wording to 2-mode wording; the rest is type-driven fallout from 1.1

## 3. Test repurposing (keep the seam coverage, retarget the assertions)

- [x] 3.1 `test/core/global-config.test.ts`: retarget the `delivery: 'commands'` round-trip (~line 279) to legacy-mapping assertions — write each legacy value to the config file, assert `getGlobalConfig().delivery` returns the mapped value, the file is rewritten, and a second read prints no notice; add the unrecognized-value → `'both'` (not persisted) case
- [x] 3.2 `test/core/init.test.ts`: repurpose the commands-only tests (~709-790: `delivery: 'commands'` inference/removal) and the commands-first sparing test (~794-830) to assert the new invariants — skills always generated under both modes and under a legacy config value; skill dirs never removed by delivery; keep the goal-loop stage-skill fixtures as the skills-always witnesses
- [x] 3.3 `test/core/update.test.ts`: same repurposing for the `delivery: 'commands'` blocks (~945-1050) and the commands-first sparing test (~1152-1180); add the healing case from the cli-update delta: config with a legacy commands-only value + project missing skill dirs → update maps the value, prints the notice, and restores skills; keep both→skills command-removal tests as-is
- [x] 3.4 `test/core/profile-sync-drift.test.ts`: replace the `'commands'`/`'commands-first'` drift cases (~99, ~136) with: skills missing is ALWAYS drift regardless of delivery; command file present under `skills` delivery is drift; clean `both` install has no drift
- [x] 3.5 `test/core/migration.test.ts` (~84): commands-only artifacts now infer `delivery: 'both'`
- [x] 3.6 `test/commands/config.test.ts` (~297) + `test/commands/config-profile.test.ts` (~242-251): validateConfig accepts legacy values (transformed) and the 2-value enum; picker tests select from two choices; `Commands only` choice assertions retargeted
- [x] 3.7 `test/commands/review-cycle.test.ts` (~265-272): the re-init-with-`commands`-delivery step no longer removes skills — retarget that step to assert skills survive (or use profile deselection if the test needs a removal path); update `test/helpers/run-cli.ts` line-16 comment referencing `delivery: commands-first`
- [x] 3.8 No test file references `deduplicateForDelivery` directly (verified: only the four src files) — deleting it needs no test retargeting beyond groups 3.2/3.3; keep cross-platform `path.join()` conventions in all touched tests (Windows CI runs these)

## 4. Docs (delivery sections only, EN + zh mirrors)

- [x] 4.1 `docs/opsx-workflow-guide.md` §5 (~lines 274-298) + `docs/zh/opsx-workflow-guide.md` (~272-296): rewrite the 5-mode enumeration to `both` (default) / `skills`; replace the "commands/commands-first breaks orchestration" warning and the "commands-first breaks skill-file tests" note with the skills-always-installed guarantee and a one-line legacy-value migration mention
- [x] 4.2 `docs/supported-tools.md` (~lines 3-10, 88): "Skills (if delivery includes skills)" → skills always installed; commands line stays conditional on `both`
- [x] 4.3 `docs/cli.md` + `docs/zh/cli.md`: verify the delivery examples (~1044-1065 / ~1038-1059) — they already use skills/both; adjust only if any text enumerates removed modes
- [x] 4.4 `docs/glossary.md` (~line 55) + `docs/zh/glossary.md`: command-file entry — "The older delivery mechanism, still supported alongside skills" → optional addition installed under `both`
- [x] 4.5 Confirm NO edits to `docs/migration-guide.md` (+zh) line 147 (generic, stands), historical docs (`grill-gstack-absorption.md`, handoff docs), or `docs/brand-independence-status.md` (another session's untracked file)

## 5. Verification tail

- [x] 5.1 `node build.js` (pnpm build is broken — workspace fault); build must pass with zero TS errors
- [x] 5.2 Live verification in a scratch project with isolated global config (`XDG_CONFIG_HOME` pointed at a temp dir; CLI via `node dist/cli/index.js`): (a) fresh init with `delivery: both` → skills + commands present; (b) fresh init with `delivery: skills` → skills present, no command files; (c) write `delivery: "commands-first"` into the isolated config, run update → one-time notice printed, config file rewritten to `both`, orchestration skill dirs (e.g. `rasen-auto`, `rasen-goal-*`) present; second run prints no notice; (d) confirm skill dirs never disappear across a both→skills switch
- [x] 5.3 `npx vitest run` on affected files: `test/core/init.test.ts test/core/update.test.ts test/core/profile-sync-drift.test.ts test/core/global-config.test.ts test/core/migration.test.ts test/commands/config.test.ts test/commands/config-profile.test.ts test/commands/review-cycle.test.ts` (+ any file from 3.8); Windows EBUSY/ENOTEMPTY flakes are known non-regressions — isolate-rerun before judging; update.test.ts's hardcoded `0.1.0` vs package.json `0.1.1` failure is known-external (another session's), not this change's
- [x] 5.4 Parity check (`npx vitest run test/core/templates/skill-templates-parity.test.ts`): expect ZERO hash movement — no template source is in scope (all template "delivery" text is the unrelated ship pr/push/local axis); if any hash moves, STOP and investigate an accidental template edit rather than pasting hashes
- [x] 5.5 Scope audit before handing to ship: `git status` + diff touches only the files in design's consumer table, tests from group 3, and docs from group 4 — no `src/core/templates/**`, no CHANGELOG, no package.json, no other sessions' in-flight files (store/work series); record the CHANGELOG entry line as a ship-log follow-up instead of editing it
