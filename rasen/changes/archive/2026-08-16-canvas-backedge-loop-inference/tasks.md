## 1. Region and decomposition (draft.ts)

- [x] 1.1 Add `backedgeRegion(def, from, to): Set<string>` — `{to, from} ∪ {n | to ⇝* n ∧ n ⇝* from}` computed over the SAME `buildAdjacency`/reachability machinery `wouldCreateCycle` uses (`draft.ts:305-307`), order-stable; unit tests pin both reachability directions on a diamond fixture (side-branch node excluded), a self-loop draw (region = {node}), and endpoints-only
- [x] 1.2 Decompose `extractSubgraph` internally into `extractSubgraphIntoDeclaration(def, { nodeIds, id, contract }) -> { next, declarationId }` and a `replacementId`-parameterized positional rewire — public `SubgraphExtractionInput/Result` signature and behavior unchanged; child-2's existing model+component tests stay green with zero edits (this task lands alone before any loop logic)
- [x] 1.3 Keep `CUT_KEY_SEPARATOR` as `String.fromCharCode(0)` — never a `\0` literal in any Edit/Write payload (the JSON layer materializes a real NUL and grep reports the file binary; repair recipe in child-2's implementer handoff)

## 2. Synthesis transaction (draft.ts)

- [x] 2.1 Add `synthesizeBoundedLoopFromBackedge(def, input): { next; declarationId; loopId }` — input `{ from, to, id, inputs, artifacts, outcomes, maxIterations, exitOutcome }`; recompute the region from the endpoints, re-run `subgraphExtractionRefusalsForNodeIds`, reuse the child-2 validators (blank/unique id via `isDeclarationIdUnique`, `assertNamedContractRows`/`assertNamedOutcomes`), then declare → mint the `WireBoundedLoopNode` exactly like `addBoundedLoopOverDeclaration` (`draft.ts:851-865`) except `body` = the extracted declaration, `limits.maxIterations` = the author's bound, and the last-body-outcome exit resolves to the author's `exitOutcome`; rewire crossings onto `loopId`; no `CompositeRef` is inserted
- [x] 2.2 Unit tests for 2.1: synthesized shape (declaration + loop node + no ref), back-edge absent from root connections, externals rewired onto the loop's derived ports with extension fields preserved, exits follow the author's outcome choice and default to `def.outcomes[0]`, `maxIterations` validated (positive integer), region refused cases surface the child-2 blocker strings verbatim, and no node in `next` (body nodes + loop) gains `legacyRuntimeOwner` (`not.toHaveProperty`)
- [x] 2.3 Unit test that the result stays authorable: the loop is patchable via `updateBoundedLoopContract` and the extracted declaration row accepts a second `insertCompositeRef`

## 3. Review UI and wiring

- [x] 3.1 Create `packages/ui/src/canvas/V2LoopReviewPanel.tsx` (child-2's `V2ExtractReviewPanel` pattern): back-edge endpoints, read-only region node list, editable derived contract rows via the exported `PortListEditor`/`NameListField`, `maxIterations` integer field under the authoring-draft-errors discipline (invalid integer blocks confirm), exit-outcome select over `def.outcomes`, declaration id defaulting `loop-body`/`loop-body-2`/… minted by the page; refusals render in place of Confirm; `data-testid="v2-loop-review-panel"`
- [x] 3.2 Wire the page: in `onConnect`'s v2 branch, when `wouldCreateCycle` fires and both endpoints are editable nodes, open the review prefilled from `backedgeRegion` + `deriveSubgraphContract` (the drawn `Connection` is never written to the draft); Cancel closes with today's refusal outcome exactly; on confirm run `synthesizeBoundedLoopFromBackedge`, then `setDraft` + `recomputeFlow(next, catalog, [result.loopId])` (both selection truths in one tick) + `markDraftChanged()` + success toast; model errors surface as toast + in-dialog line with the review open and edits intact
- [x] 3.3 Leave the palette Loop gesture (`addRootGesture('loop')` → `addBoundedLoopOverDeclaration`) and the declarations-row insert action untouched — assert neither file region changes beyond the refactor's mechanical needs

## 4. Tests

- [x] 4.1 Component tests in `pipeline-canvas-page.test.tsx` (child-1 `onSelectionChange` trigger + child-2 review patterns): a refused cycle-closing draw opens the review with the draft unchanged; cancel leaves the refusal message and draft untouched; confirm yields the loop selected, the region's nodes gone, externals rewired, the declaration row present; refusal text for an unextractable region; an invalid iteration bound blocks confirm; the definition actually sent to validation carries no `legacyRuntimeOwner` on the loop or any body node (POST-body guard); the explicit loop gesture still works
- [x] 4.2 Run the full UI suite via the CI-canonical `pnpm --dir packages/ui exec vitest run`, CITE the file/test counts against the 67 files / 795 tests baseline, and fix regressions (do not pipe the gate through `tail`) — 67 files / 814 tests, exit 0 (+12 model +7 component over the 67/795 baseline)

## 5. Gates

- [x] 5.1 Real-browser gate via direct CDP on a throwaway Chrome (fresh `--user-data-dir` + a fresh `--remote-debugging-port` — 9333+ run busy in sibling sessions; never the user's daily browser): FIRST the m2 repeat-probe — draw several rectangles over known-contained node sets and assert FULL membership each time; if the containment miss reproduces, record the transcript and route it as a child-1 follow-up (do NOT fix in this change); THEN the back-edge flow end-to-end (close the selection summary panel before handle drags, focus before blur on inputs, re-fit-view before every drag, `pnpm --dir packages/ui run build` before serving via `node bin/rasen.js ui --no-open --no-daemon --port <p>`); record the transcript and driver in the change's evidence dir — ALL CHECKS PASSED (evidence/cdp-transcript.md, cdp-results.json, 7 screenshots); m2 containment miss REPRODUCED 3/3, recorded + routed to child 1
- [x] 5.2 Assert `git diff 74568906..HEAD -- src/core/pipeline-registry/` is empty (IR frozen), `V2_BODY_PALETTE_KINDS` is still `['AtomicStage']`, and `addBoundedLoopOverDeclaration` plus the declarations-row insert action are behaviorally untouched (no capability hole) — evidence/gates-5-2.md
