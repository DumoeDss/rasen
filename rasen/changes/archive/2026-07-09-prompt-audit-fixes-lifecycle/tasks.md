## 1. WF-2 — propose consumes office-hours validation

- [x] 1.1 In `src/core/templates/workflows/propose.ts`, add a step to BOTH `getOpsxProposeSkillTemplate()` and `getOpsxProposeCommandTemplate()` — placed after `rasen new change` + `rasen status --json` and before drafting the proposal — that: (a) checks `changeRoot` (from status JSON) for `office-hours-design.md`; (b) checks the office-hours directory alongside the changes directory (derive from `planningHome.changesDir`) for `<change-name>.md`; (c) if either is found, reads it and incorporates its findings into the proposal, naming office-hours as the source. Resolve all paths from status JSON — do NOT hardcode `rasen/...` (keeps clear of WF-3/child #6). If neither exists, proceed normally.
- [x] 1.2 In `src/core/templates/workflows/office-hours.ts`, make the WF-2 producer promise accurate (conformance, no spec delta): the change-directory dual-write note (currently `:78`) stays; the no-active-change note (currently `:84`) SHALL state that `/rasen:propose` auto-detects the file when the new change's name matches the topic slug; the "Downstream Integration" note (currently `:113-115`) SHALL name both scan locations (change dir AND the office-hours directory by slug).

## 2. WF-6 — office-hours workflow delegates facilitation to the expert

- [x] 2.1 In `src/core/templates/workflows/office-hours.ts` (the workflow command — do NOT touch `experts/office-hours.ts`), restructure steps 2/3/4 so the `/office-hours` expert is the single facilitation authority: delegate the session to `/office-hours`; keep the inline six-questions/builder text explicitly as a fallback pre-brief (used only if the expert is unavailable), not a second pass; consolidate design-document production into a single step. State the precedence (expert wins).

## 3. WF-4 — archive verdict + task hard gates

- [x] 3.1 In `src/core/templates/workflows/archive-change.ts`, add a verification-verdict gate to BOTH getters: read `verification-report.md` from the change dir (resolved from status JSON) when present; if its `VERIFY VERDICT:` line is `BLOCKED`, refuse to archive by default, require an explicit blocker-naming override to proceed, and refuse outright non-interactively. `CLEAN` → no gate. Absent report → no hard gate (soft note at most). Do NOT introduce new verdict words — consume child #2's `VERIFY VERDICT`.
- [x] 3.2 In `archive-change.ts` (both getters), elevate the incomplete-tasks check from a soft warning to a hard gate: refuse by default when `- [ ]` tasks exist, proceed only on an explicit override that names the incomplete-task condition, refuse outright non-interactively. Update the affected Output/summary blocks accordingly.
- [x] 3.3 In `archive-change.ts` (both getters), scope the "Don't block archive on warnings - just inform and confirm" guardrail (enumerate-and-gate: sweep the whole Guardrails block) so it applies only to soft warnings (incomplete non-task artifacts, unsynced delta specs, missing ship log, deferred delivery) and explicitly does NOT cover the verification-BLOCKED and incomplete-task hard gates.

## 4. WF-5 + WF-11 — archive delivery precondition + portfolio awareness

- [x] 4.1 In `archive-change.ts` (both getters), add a delivery-precondition check: read `ship-log.md` from the change dir (resolved from status JSON). If absent, soft-warn "This change has no ship log — archive without delivering?" with an explicit escape for changes that legitimately don't ship (e.g. spec-only); proceed on confirm.
- [x] 4.2 In `archive-change.ts` (both getters), when `ship-log.md` exists and its `Status:` line indicates "delivery deferred to portfolio level" (the marker ship writes in local mode), add a minimal note that parent-level portfolio delivery is still pending and archiving the child now may lose track of it; prompt for confirmation. No portfolio graph / parent lookup — just the note.

## 5. WF-5 (apply) + WF-10 (continue) — completion nudges

- [x] 5.1 In `src/core/templates/workflows/apply-change.ts` (both getters), change the all-tasks-complete completion message from "Ready to archive" / "archive this change with /rasen:archive" to steer through `/rasen:verify` then `/rasen:ship`, naming archive only as the post-delivery step. Update the "Output On Completion" block and the step-7 "suggest archive" wording.
- [x] 5.2 In `src/core/templates/workflows/continue-change.ts` (both getters), change the `isComplete: true` nudge ("implement this change or archive it") to steer toward `/rasen:apply` (implement); mention archive only as the later post-implementation step, not an immediate co-equal option.

## 6. F.1 resume ladder — generation-match clause (LEAD-added; _orchestration.ts scope exception)

- [x] 6.1 In `test/core/templates/skill-templates-parity.test.ts`, add getter imports and `functionFactories` + `EXPECTED_FUNCTION_HASHES` entries for the 11 unpinned skill templates: `getOfficeHoursCommandSkillTemplate`, `getVerifyEnhancedSkillTemplate`, `getShipCommandSkillTemplate`, `getRetroCommandSkillTemplate`, `getAutoCommandSkillTemplate`, `getReviewCycleSkillTemplate`, `getHandoffSkillTemplate`, `getGoalPlanSkillTemplate`, `getGoalIterateSkillTemplate`, `getGoalReportSkillTemplate`, `getGoalCommandSkillTemplate`.
- [x] 6.2 In the same file, add the 8 unpinned command payloads to `functionFactories` + `EXPECTED_FUNCTION_HASHES`: `getOpsxOfficeHoursCommandTemplate`, `getOpsxVerifyEnhancedCommandTemplate`, `getOpsxShipCommandTemplate`, `getOpsxRetroCommandTemplate`, `getOpsxAutoCommandTemplate`, `getOpsxReviewCycleCommandTemplate`, `getOpsxHandoffCommandTemplate`, `getOpsxGoalCommandTemplate`.
- [x] 6.3 In the same file, add the 11 skill templates to `GENERATED_SKILL_FACTORIES` + `EXPECTED_GENERATED_SKILL_CONTENT_HASHES` (commands do NOT get generated-content entries — they are `CommandContent`, not run through `generateSkillContent`, matching existing command coverage). This is the task that hash-locks `rasen-auto`, `rasen-goal`, and `rasen-review-cycle` — the three `_orchestration.ts` embedders — so §7's F.1 edit becomes verifiable.

## 7. F.1 resume ladder — generation-match clause (LEAD-added; _orchestration.ts scope exception; do AFTER §6 so the registry covers this edit)

- [x] 7.1 In `src/core/templates/workflows/_orchestration.ts`, Step F.1 step 2 ("Handoff document first, transcript second"), add ONE tight clause (and touch NOTHING else in the file — children 1/3's edits must survive byte-for-byte): a handoff/retirement document counts ONLY if it is the LATEST holder's own distillation of the role's final state; an un-exhausted latest holder that left NO document resumes from its TRANSCRIPT (step 3), which BEATS any earlier generation's document — never seed from a stale predecessor's document when a newer holder's context survives unrecorded. Also add the same-session-restart nuance: when the session directory survived the restart, `SendMessage`-by-NAME MAY still resolve to the latest holder — try that wake first, fall back to the ladder only if it does not resolve. Keep it tight; do not restructure the surrounding ladder. PREDICTION: because §6 just pinned `rasen-auto`/`rasen-goal`/`rasen-review-cycle` (the three templates that embed `_orchestration.ts`), this edit WILL move those three newly-added hashes in the §8 regen — that movement is expected and is exactly how this edit is verified.

## 8. Regenerate + lock hashes + validate

- [x] 8.1 `pnpm build` (fall back to `node build.js` if the pnpm workspace file errors).
- [x] 8.2 `node dist/cli/index.js update` to regenerate skills/commands. Confirm `rasen-goal`, `rasen-goal-plan`, `rasen-goal-iterate`, `rasen-goal-report` SKILL.md files ARE emitted (double-confirms the goal registration is already wired — Decision 7; if any are missing, that reopens the registration debt).
- [x] 8.3 `npx vitest run test/core/templates/` — expect the parity test to fail with the new/moved hashes in the assertion diff. Hand-paste the hashes from the diff into the maps: function-map keys UNQUOTED, content-map keys single-quoted, all values single-quoted (double quotes are silently dropped by the sed path — hand-paste).
- [x] 8.4 Confirm ONLY the expected templates' hashes moved: (a) the 5 edited already-pinned/newly-pinned bodies `rasen-propose`, `rasen-apply-change`, `rasen-continue-change`, `rasen-archive-change`, `rasen-office-hours-command`; (b) the `_orchestration.ts` embedders `rasen-auto`, `rasen-goal`, `rasen-review-cycle` (moved by the §6 F.1 edit); (c) the newly-added parity entries. No `_shared.ts` edits here, so NO PREAMBLE-embedding EXPERT hashes should move; if any expert hash moves, investigate before pasting.
- [x] 8.5 Re-run `npx vitest run test/core/templates/` → green.
- [x] 8.6 `node dist/cli/index.js validate prompt-audit-fixes-lifecycle` → passes.
