## 1. Model (draft.ts)

- [x] 1.1 Add `isPromotableSink(def, node)`: node exists in the root graph, kind is `AtomicStage` or `Join`, and NO root connection has `from.node === id` (out-edge scan over the same root-connection enumeration `buildAdjacency` uses); plus `promoteSinkToFinish(def, sinkId, outcome) -> { next; finishId }` per design D3 — re-validate sink-ness and `outcome ∈ def.outcomes` (non-blank), append the Finish with exactly `addFinishNode`'s node shape (`draft.ts:902-905`) but the picked outcome, and wire sink→Finish via `addV2Connection`/`v2ConnectionIdFor` (AtomicStage sources `CONTROL_SOURCE_PORT`; a Join sources its `outcomes.proceed` value, the rendered barrier output — pin the exact id against `layout.ts`)
- [x] 1.2 Unit tests in `packages/ui/test/canvas/draft.test.ts`: `isPromotableSink` truth table (stage sink, barrier sink, wired node, loop/composite/gate/choice ends, body-graph node not considered); promotion node shape `toEqual`-pinned against the gesture's with the picked outcome; the wired connection's exact endpoint/port ids; refusal strings for a stale non-sink and a foreign outcome; no node in `next` gains `legacyRuntimeOwner` (`not.toHaveProperty`); the promoted Finish patches via `updateV2NodeFields`

## 2. Panel affordance and wiring

- [x] 2.1 Add the optional `sinkPromotion?: { outcomes; onPromote }` prop group to `V2NodePanel.tsx`: a compact endpoint-naming section (label, outcome select defaulting to `def.outcomes[0]`, confirm button) rendered only when the prop is passed; the panel decides nothing (`data-testid="v2-node-panel-sink-promotion"`)
- [x] 2.2 Wire the page: pass the group when `isPromotableSink(draft, selectedV2Node)` in v2 edit mode; on confirm run `promoteSinkToFinish`, then `setDraft` + `recomputeFlow(next, catalog, [result.finishId])` (selectionOverride pairing) + `markDraftChanged()` + a plain success toast; model errors surface as a toast and change nothing; the palette Finish gesture path is untouched

## 3. Tests

- [x] 3.1 Component tests in `pipeline-canvas-page.test.tsx` (existing selection/node-panel triggers): the section renders for a selected stage sink and is absent for a wired node and for a loop end; picking the second outcome and confirming lands the Finish with that outcome and the wired connection in the definition actually sent to validation (POST-body walk plus the `legacyRuntimeOwner` guard); the Finish is selected with its panel open; the palette Finish gesture still works
- [x] 3.2 Run the full UI suite via the CI-canonical `pnpm --dir packages/ui exec vitest run`, CITE the file/test counts against the 67 files / 839 tests baseline, and fix regressions (do not pipe the gate through `tail`) — 67 files / 854 tests passed (baseline 67/839; +12 model, +3 component)

## 4. Gates

- [x] 4.1 Real-browser verification via direct CDP on a throwaway Chrome (fresh `--user-data-dir` + port 9342 OR HIGHER per the portfolio census, `--window-size=1600,1000`; never the user's daily browser; `pnpm --dir packages/ui run build` then `node bin/rasen.js ui --no-open --no-daemon --port <p>`; close the selection summary panel before any handle drag, focus before blur on inputs): author a chain ending in a stage, select the end stage, name its outcome via the panel section, confirm; assert the Finish appears wired and selected and the stage's settings are untouched; build a two-branch frontier via child 4's offer, then promote the barrier sink; exercise the palette Finish gesture once (close panel / focus-before-blur / re-fit-view discipline throughout); record the transcript and driver in the change's evidence dir — ALL 24 CHECKS PASSED (app 9342, CDP 9344; evidence/cdp-transcript.md + cdp-results.json + 8 screenshots; barrier sink reached by taking child 4's offer then deleting the trailing Finish, which drops the join→finish edge with it)
- [x] 4.2 Assert `git diff <last-archived-child-ship>..HEAD -- src/core/pipeline-registry/` is empty (IR frozen), `V2_BODY_PALETTE_KINDS` is still `['AtomicStage']`, and `addFinishNode` plus the palette gesture path are behaviorally untouched (no capability hole) — evidence/gates-4-2.md (empty diff vs 74568906 AND vs f66666d9 + working tree; kinds unchanged; gesture proven untouched textually + behaviorally in jsdom and the real browser)
