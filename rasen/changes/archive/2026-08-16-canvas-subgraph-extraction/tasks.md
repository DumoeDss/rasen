## 1. Model rules and derivation (draft.ts)

- [x] 1.1 Add `subgraphExtractionRefusals(def, selection): string[]` per design D3 — non-AtomicStage kinds named; outside Gate.target / FanOut branches+members(id, hierarchicalPath) / Join inputs+required+optional referencing a selected stage, checked in BOTH raw and `stage:`-prefixed forms; consultation `sourceStage` references — each refusal one author-readable string
- [x] 1.2 Add `deriveSubgraphContract(def, nodeIds): { inputs: WireDefinitionPort[]; artifacts: WireDefinitionArtifact[]; outcomes: string[] }` per design D2 — one input per distinct (target stage, port) with collision-suffixed names and the severed target port as default type; one outcome per distinct (source stage, port) named after the source stage; `'done'` when no outgoing edge is severed; artifacts `[]`
- [x] 1.3 Unit tests for 1.1-1.2 in `packages/ui/test/canvas/draft.test.ts`: each refusal rule in isolation (mixed kinds, gate target, fan-out member, join input, consultation), both `stage:`-prefixed forms on a legacy-shaped fixture, derivation defaults/collisions/no-outgoing-default, empty selection

## 2. Extraction transaction (draft.ts)

- [x] 2.1 Add `extractSubgraph(def, input): { next; declarationId; refId }` per design D1 — re-run refusals; validate reviewed rows via `assertNamedContractRows`/`assertNamedOutcomes` and the id via `isDeclarationIdUnique`; build the custom declaration in one immutable step with the selected stages and internal connections moved VERBATIM; drop them from the root; append the ref via `insertCompositeRef`; rewire severed connections onto the ref's mapped input/outcome ports carrying extension fields, with ids from `v2ConnectionIdFor`
- [x] 2.2 Unit tests for 2.1: body verbatim (execution fields preserved), internal connections moved, crossing rewires (from/to remapped, extension fields intact, fresh ids), no node in `next` gains `legacyRuntimeOwner` (`not.toHaveProperty` over every declaration body node and the ref), duplicate declaration id throws, result accepts a second `insertCompositeRef` (re-referenceable)
- [x] 2.3 Unit test that a valid pre-extraction draft still passes the model-level shape rules post-extraction (declaration contract rows unique/non-blank, ref port names match rewired ports, `BoundedLoop` exits untouched)

## 3. Review UI and wiring

- [x] 3.1 Extend `V2SelectionPanel.tsx` with optional `onPackage` and `packageRefusals` props — a "Package into reusable block" button rendered only when `onPackage` is provided, refusal strings as muted text; the panel stays presentational (`selectionPanelMode` and the refusal rule remain the owners)
- [x] 3.2 Create `packages/ui/src/canvas/V2ExtractReviewPanel.tsx` (the `duplicateDialog` overlay pattern): declaration id input (default `block`/`block-2`/… via `isDeclarationIdUnique`), editable inputs/artifacts/outcomes rows reusing the declarations editor's row UX and surfacing the model's validation messages, body summary (stage count, internal connection count, derived cut), Confirm/Cancel; `data-testid="v2-extract-review-panel"`
- [x] 3.3 Wire the page: compute `subgraphExtractionRefusals(draft, selection)` for the panel props (v2 edit mode only); open the review with `deriveSubgraphContract` defaults; on confirm run `extractSubgraph`, then `setDraft` + `recomputeFlow(next, catalog, [result.refId])` (the selectionOverride path — both selection truths in one tick per the `syncFlowSelection` discipline) + `markDraftChanged()` + success toast naming the declaration; on model error surface the message as a toast and keep the draft unchanged

## 4. Tests

- [x] 4.1 Component tests in `pipeline-canvas-page.test.tsx` reusing child 1's `onSelectionChange` trigger: action visible only for multi selections in v2 edit mode; refusal text for each blocker; review flow (rename an outcome, confirm) yields a rewired connection using the edited port; confirm leaves the ref selected and its panel open; the declarations panel lists the new custom row and its insert action adds a second ref; the definition actually sent to validation carries no `legacyRuntimeOwner` on any moved node or the ref (POST-body guard)
- [x] 4.2 Run the full UI suite via the CI-canonical `pnpm --dir packages/ui exec vitest run`, CITE the file/test counts against the 67 files / 768 tests baseline, and fix regressions (do not pipe the gate through `tail`) — 67 files / 794 tests passed (exit 0); baseline 67/768, +26 (21 model in draft.test.ts + 5 component), zero regressions

## 5. Gates

- [x] 5.1 Real-browser verification via direct CDP on a throwaway Chrome (fresh `--user-data-dir` + `--remote-debugging-port`, e.g. 9333; never the user's daily browser, and not cdp-proxy.mjs which hardwires 127.0.0.1): box-select two stages chained between an upstream stage and a finish → package → edit an outcome name in the review → confirm → the ref renders with the derived ports, the root graph is rewired upstream→ref→finish, the declarations row exists, and a second insert adds another ref; record the transcript in the change's evidence dir — ALL CHECKS PASSED (throwaway Chrome 151 headless on 9338 — 9333-9337 were busy with other sessions' checks; `evidence/cdp-transcript.md` + `evidence/cdp-results.json` + rerunnable driver + 6 screenshots)
- [x] 5.2 Assert `git diff 74568906..HEAD -- src/core/pipeline-registry/` is empty (IR frozen; note the diff base is child 1's ship parent — use the branch point if the LEAD re-pins), `V2_BODY_PALETTE_KINDS` is still `['AtomicStage']`, and the per-declaration-row insert action is untouched (no capability hole) — all three green, recorded in `evidence/gates-5-2.md`
