# Tasks: externalize-artifacts-sha-stamping

> Child 4 (archive-dest) is mid-apply on these same templates. Task 0 discipline for EVERY template task: first read the CURRENT landed text of the target file and verify child 4's destination-aware bookkeeping shape (step numbering, per-destination commit guidance); `git status --porcelain` on each file before editing and before committing — edit only files clean of foreign modifications; pathspec commits (`git commit -- <paths>`) verified with `git show --stat`. Build `node build.js`; tests `npx vitest run` (pnpm broken machine-wide). Preserve child 3's invariant: new branches key on RECORDED ship-log facts, never re-resolved config. Keep skill and command getters in sync in every edited template.

## 1. ship.ts — store embedding + chain stamps

- [x] 1.1 Store-safe proposal read: PR Body Generation reads `proposal.md` via status JSON `changeRoot` (all modes), replacing the repo-relative literal.
- [x] 1.2 Store-mode embedding (when `root.store_id` is present in the status JSON — see apply-time correction in design.md; `planningHome.kind` never reports `store`, it is hardcoded `repo`): embed proposal Why/What + delta specs (`changeRoot/specs/**/spec.md`) in collapsed `<details>` blocks in the PR body; stamp store path + `git -C <root.path> rev-parse HEAD`; dirty store tree (`git -C <root.path> status --porcelain` non-empty) stamped as `<sha> (store tree dirty at ship time)`; non-git store stamped `(store not under git)`; extremely large delta content → link the store path instead of embedding (reviewer-ergonomics cap per design D3).
- [x] 1.3 Ship-log additions: `Store:` + `Store commit:` lines in store mode; under in-ship timing record the archive outcome + note the ship commit IS the archive commit for a REPO-rooted change — for a STORE-rooted change this is false and was corrected in the review round (see section 6, M2).

## 2. archive-change.ts (both getters) — chain closure

- [x] 2.1 Post-bookkeeping append (new step 5.5, both getters — their step-6 bullet wording differs slightly, handled per-getter): after child 4's destination-aware bookkeeping succeeds and before the completion summary, append the `## Archive` section to the workDir ship-log (Date, Ship commit copied from the log's own `Commit:` line, Outcome = archived path / pruned, Archive commit journaled in a follow-up append right after the commit is created); never rewrite the ship-side section; no ship-log → create one with only the archive section (workDir, change-dir fallback per child 2's sticky rule).
- [x] 2.2 Commit-message stamp: per-destination commit guidance gains the form `chore(rasen): archive <name> (specs synced; ship <short-sha>)` sourced from the recorded `Commit:` fact; suffix omitted (never invented) when no ship commit is recorded.

## 3. bulk-archive-change.ts — parity with 2.x

- [x] 3.1 Same per-change ship-log append (step 8b.5) + ship-referencing commit-message form for bulk moves; output examples updated. Also fixed a pre-existing divergence found while editing: the command getter's Guardrails destructive-destination bullet was missing the `--ignored`/`git ls-files` tracked check the skill getter already had (step 8b's own body was correct in both; only the redundant Guardrails restatement had drifted). NOTE: the first pass's "brought to parity" claim was incomplete — the command getter's bullet was still missing the "SEPARATE consent from the batch confirmation in step 7" clause; caught by review as M3 and fully fixed in the review round (section 6). Both getters are now byte-identical on this bullet, verified scripted.

## 6. Review round fixes (2026-07-10, reviewer verdict DONE_WITH_CONCERNS — 0B/2M/4m/4t)

- [x] M1 (ship.ts): in-ship store-mode PR-body embedding read delta specs from `<changeRoot>/specs/**` AFTER the directory had already moved/deleted — real data-loss bug. Fixed: step (b).1 now captures delta spec content when `root.store_id` is present, mirroring the proposal capture; embedding reads the captured copy under in-ship timing.
- [x] M2 (ship.ts): the in-ship `## Archive` section unconditionally asserted `Archive commit == Commit`, false for store-rooted changes (bookkeeping mutates the STORE repo, which ship's own commit never touches). Fixed HONESTLY within template-prose scope: store-rooted + in-ship now records `pending — store-side bookkeeping not committed by this workflow` unless the agent separately commits the store repo and records that SHA; added step (b).5 explaining why. Full store-commit orchestration is explicitly NOT implemented — recorded as a known-open follow-up in planning-context.md (root cause predates this child).
- [x] M3 (bulk-archive-change.ts): completed the Guardrails destructive-destination bullet parity the first pass left partial — command getter now carries the same "SEPARATE consent from the batch confirmation in step 7" clause as the skill getter.
- [x] M4 (archive-change.ts + bulk-archive-change.ts): commit message no longer unconditionally claims "(specs synced)" — four conditional forms (specs-synced × ship-recorded) now documented and used.
- [x] M5 (ship.ts): `Store:` ship-log line's source annotation corrected from `root.store_id`/`changeRoot` to `root.path`.
- [x] M6 (archive-change.ts + bulk-archive-change.ts): step 5.5 / step 8b.5's `workDir` resolution now mints on demand (same as step 5's prune tombstone writer) instead of silently falling back to `changeRoot`, which `external`/`prune` are about to move or delete; documented the sticky-legacy edge case, including that `prune` has no post-deletion recovery path.
- [x] Reviewer's durable findings (T2/T3/T4-adjacent) appended to `rasen/changes/externalize-artifacts/planning-context.md` as known-open follow-ups: `ship.ts` pre-flight task read and ship-log fallback literals misresolve for store-rooted changes (same bug class as the proposal-read fix); CLI-side `rasen archive` writes no chain records (agent-workflow-only by design); store-mode paths have never been live-exercised (recommend a store-mode smoke as a follow-up, separate from repo-mode task 5.3).
- [x] Regen + parity re-verified: same 9 hash entries moved a second time (nothing else); parity suite 6/6 green; `validate` passes; full `npx vitest run` re-run green (124 files / 2310 passed / 22 skipped / 0 failed).

## 4. Regeneration and parity

- [x] 4.1 `node build.js` + `node bin/rasen.js update --force` to regenerate `.claude/skills/**` (`.codex/**` is not a configured delivery target in this repo); hand-edited nothing generated.
- [x] 4.2 Updated `skill-templates-parity` expected hashes for exactly the 6 functions / 3 generated-skill dirs touched (getArchiveChangeSkillTemplate, getBulkArchiveChangeSkillTemplate, getOpsxArchiveCommandTemplate, getOpsxBulkArchiveCommandTemplate, getShipCommandSkillTemplate, getOpsxShipCommandTemplate; rasen-archive-change, rasen-bulk-archive-change, rasen-ship) — confirmed via compute/diff script that exactly these 9 entries moved, nothing else. `npx vitest run test/core/templates/` green (6/6).

## 5. Verification

- [x] 5.1 `node bin/rasen.js validate externalize-artifacts-sha-stamping` passes; full `npx vitest run` green: 124 files, 2310 passed, 22 skipped, 0 failed (the pre-existing Windows `should select all tools with --tools all option` flake did not reproduce this run).
- [x] 5.2 Generated-skill inspection done directly on `.claude/skills/rasen-ship/SKILL.md`, `.claude/skills/rasen-archive-change/SKILL.md`, `.claude/skills/rasen-bulk-archive-change/SKILL.md`, and their `.claude/commands/rasen/*.md` counterparts (NOT `.claude/commands/opsx/*.md`, which is a stale legacy `openspec`-branded artifact unrelated to this regen — confirmed by content mismatch, out of scope). All show the expected new prose.
- [x] 5.3 Live smoke: satisfied live by ship ba96166 + this archive; store-mode smoke remains a recorded follow-up.
- [x] 5.4 Cross-platform: all new prose uses the same angle-bracket placeholder convention as the surrounding template text (`<changeRoot>`, `<root.path>`, `<sha>`, etc.) — no OS-specific path literals introduced; no test code changed (the parity-hash update was a scripted string patch, not new test path literals).
