## 1. Supervisor core

- [x] 1.1 Implement `src/core/management-api/kill-tree.ts`: `isProcessAlive(pid)`, `killProcessTree(pid, { graceMs })` — POSIX negative-pgid SIGTERM→SIGKILL, Windows `taskkill /T` then `/F /T`; ESRCH-silent; escalation timer `unref()`'d and cancellable (adapted from omnicross kill-tree, design D5)
- [x] 1.2 Implement `src/core/management-api/session-registry.ts`: `SessionRecord` shape per design D2, `Map`-backed create/get/list/updateState/touchOutput/finalize, copies from getters, exited-record retention cap 50 with oldest-first pruning; no server dependencies (child-2 daemon must be able to construct it standalone)
- [x] 1.3 Implement `src/core/management-api/supervisor.ts`: `createSessionSupervisor({ registry, resolveAgentCli, maxConcurrent })` with `launch(entry, task, opts)` → spawn per design D1 (argv array, shell:false, cwd=launchProjectRoot, env inherit, stdio ignore/pipe/pipe, POSIX `detached:true`, Windows `windowsHide:true`); dual timers (overall cap + no-output watchdog reset on any stdout/stderr data, both `unref()`'d, cleared on settle); 64 KiB ring-buffer tails per stream; best-effort parse of the stream-json `init` event for `agentSessionId` (parse failure degrades to missing field); `kill(id)` via kill-tree; `shutdownAll(reason)`; SIGKILL escalation, slot release, and registry finalization all keyed off child `close` (three-point checklist)
- [x] 1.4 Implement agent-CLI discovery: `RASEN_CLAUDE_BIN` env override, else PATH scan for `claude` (plus `claude.exe`/`claude.cmd` on Windows), resolved once per server and cached; absence surfaces as a distinct resolvable-error state consumed by the endpoint (503 `agent_cli_unavailable`)

## 2. Whitelist tier and sessions API

- [x] 2.1 Extend the admission whitelist into a tiered data table (design D7): `bounded-cli` (create-change, unchanged) and `supervised-long-runner` (`auto` → `/rasen:auto`, `goal` → `/rasen:goal`, each with skill string and default timeouts); each endpoint admits only its own tier
- [x] 2.2 Add session wire types to `src/core/management-api/wire-types.ts`: `SessionRecordWire`, `LaunchSessionRequest` (`kind`, `task`, optional `changeName`/`timeoutMs`/`noOutputTimeoutMs`), `SessionsResponse` (with optional joined `runState` per entry), `SessionDetailResponse` (record + stdout/stderr tails) — sibling-stable for the UI child
- [x] 2.3 Implement `src/core/management-api/sessions.ts` handlers: POST (validate kind ∈ supervised tier, task via slice-2's rule — non-empty, length cap, control chars except tab/newline; optional `changeName` through change-name validation; timeout overrides clamped to caps ≤12h/≤30min; 409 `no_project` without a launch project; 409 `busy` over cap 3; 503 `agent_cli_unavailable`; 201 with record), GET list (registry records + read-only run-state join via existing runs readers when `changeName` present), GET detail (record + tails, 404 unknown), DELETE (202 `exiting` for live, 200 idempotent for exited, 404 unknown; `terminationReason: 'killed'`)
- [x] 2.4 Wire routing in `router.ts`: admit `/api/v1/sessions` exact and `/api/v1/sessions/<one-segment>` prefix into `isManagementPath` and method admission (DELETE legal only on the id path; deeper suffixes fall through); construct one supervisor per server instance; keep bearer auth, no-CORS, and identity-header behavior untouched
- [x] 2.5 Hook server shutdown in `server.ts`: clean close and SIGINT/SIGTERM paths call `shutdownAll('server-shutdown')` before exit (design D6)

## 3. Tests

- [x] 3.1 Fixture children under test helpers: a SIGTERM-ignoring long-runner, a slow/silent runner, a fast-exiting runner, and a stream-json-emitting runner (prints an `init` line then periodic NDJSON) — all plain node scripts spawned via `process.execPath`
- [x] 3.2 Supervisor unit tests: normal exit finalizes record and frees slot; kill escalates SIGTERM→SIGKILL keyed off child close against the signal-resistant fixture; no-output watchdog fires with reason `no-output-timeout`; overall cap fires with reason `overall-timeout`; watchdog resets on output; tail ring-buffers bound memory; `agentSessionId` captured from the init line and absent-on-garbage without failing the session; registry pruning past the exited cap
- [x] 3.3 Sessions API integration tests (real server on an ephemeral port, fixture CLI override): 201 launch and listing shows live record; DELETE → 202 then listing shows `exited`/`killed`; idempotent DELETE 200; 404 unknown id; 400 invalid kind/task; 409 `busy` past cap and slot released only on true child close; 409 `no_project`; 503 when no agent CLI resolves; run-state join appears for a session with `changeName` (write a valid `auto-run.json` fixture); unauthenticated 401 on every sessions endpoint; no `Access-Control-Allow-Origin` on any sessions response; 405 for DELETE on non-sessions paths; deeper suffix `/api/v1/sessions/x/y` falls through
- [x] 3.4 Shutdown test: server with a live fixture session shuts down cleanly → child tree is dead, record finalized with `server-shutdown` before exit
- [x] 3.5 Cross-platform audit of new code paths: `path.join` everywhere, Windows taskkill branch unit-tested via injection (no real taskkill in CI), no bare `-pid` kill on win32

## 4. Verification

- [x] 4.1 Full suite green (`pnpm test`) in the worktree; no regression in existing management-api/config-api tests (identity headers, submission bridge untouched semantics)
- [x] 4.2 `rasen validate slice3-session-runtime --json` passes
- [x] 4.3 Manual smoke on this machine (not CI): start `rasen ui` on a non-8890 port in a scratch project, POST an `auto` session with a trivial task, watch `GET /api/v1/sessions` go live, DELETE it, confirm `exited`/`killed` and no orphaned `claude` process (`ps`) — this is the child-level rehearsal of the portfolio's kill-a-real-session acceptance
