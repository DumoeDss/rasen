# Tasks — ui-config-redesign-spaces-page

**Working directory**: this child is implemented in the git worktree `E:\AI\ChatAI\Agents\VibeCodingProjects\workflow\Reference\OpenSpec-wt-spaces` (branch `feat/ui-config-spaces-page`, dependencies installed). All file paths below are relative to that worktree root. Read `design.md` first (D1–D6). Reference projects never in code/comments/commits; visual style frozen; version untouched. Build: root `node build.js` = CLI only; UI = `pnpm --filter @atelierai/rasen-ui build`.

## 1. Pinned-spaces config key (D1)

- [x] 1.1 In `src/core/config-keys.ts`: append one registry entry `{ key: 'ui.pinnedSpaces', scopes: ['global'], type: 'array', defaultValue: [], description: 'Pinned planning spaces as <type>:<id> selectors (managed from the Spaces page)', group: 'Appearance' }`. Keep the diff to exactly one appended entry (parallel-worktree merge with W1 touches this file — see proposal Merge note).
- [x] 1.2 In `src/core/config-schema.ts`: add a typed `ui: z.object({ pinnedSpaces: z.array(z.string()).optional() }).passthrough().optional()` block to `GlobalConfigSchema`; in `src/core/global-config.ts` add `ui?: { pinnedSpaces?: string[] }` to the `GlobalConfig` interface.
- [x] 1.3 In `src/commands/config.ts` `buildEditorChoice` (~line 170): treat `ui.pinnedSpaces` like `workflows` — a disabled row (hint: managed from the Spaces page in `rasen ui`; editable via `rasen config set`). Without this the editor would prompt on an array type it has no prompt for.
- [x] 1.4 Tests: add a block to `test/core/config-keys.test.ts` (new key round-trips through the global schema; rejected for non-global scope; non-array value rejected) and to `test/commands/config-editor.test.ts` (row renders disabled). Do NOT touch any existing count assertions beyond what compilation requires — the LEAD reconciles W1's scope-count assertion at merge.

## 2. Local-paths endpoint (D3)

- [x] 2.1 Create `src/core/management-api/local-paths.ts`: `handleLocalPaths(pathParam: string | undefined)` → no param ⇒ enumerate `os.homedir()` and mark it as home in the payload; absolute param ⇒ enumerate it; relative/empty ⇒ 400 `invalid_path`; ENOENT ⇒ 404 `path_not_found`; not-a-dir ⇒ 400 `not_a_directory`; EACCES/EPERM ⇒ 403 `path_forbidden`. Response `{ path, parent, separator, entries }` per design D3: canonical path via `FileSystemUtils.canonicalizeExistingPath`, `parent` = canonical dirname or null when `path.dirname(p) === p`, `separator: path.sep`, entries from `fs.promises.readdir(..., { withFileTypes: true })` sorted dirs-first alphabetical, `isGitRepo` = `<entry>/.git` exists as directory OR file (`fs.stat` try/catch per entry, bounded to dir entries only). Read-only; no registry imports.
- [x] 2.2 Add wire shapes to `src/core/management-api/wire-types.ts`: `LocalPathEntry { name; isDir; isGitRepo }`, `LocalPathsResponse { path; parent: string | null; separator; home?: boolean; entries: LocalPathEntry[] }`.
- [x] 2.3 Tests `test/core/management-api/local-paths.test.ts`: home default; absolute path outside home enumerated; relative rejected 400; missing 404; file-not-dir 400; `.git` dir and `.git` FILE both flag `isGitRepo`; sorted dirs-first; Windows: request with lower-cased drive letter resolves canonically (use `path.join` in expectations).

## 3. Create-space bridge (D4, D5)

- [x] 3.1 In `src/core/management-api/whitelist.ts`: add three bounded-cli rows — `'create-project-space'`, `'register-store-space'`, `'setup-store-space'` (`{ tier: 'bounded-cli', op: <same> }`). Update the table doc comment (bounded tier = exactly four entries).
- [x] 3.2 Create `src/core/management-api/create-space.ts` modeled on `submit.ts`: export `createSpaceCreator(options?)` returning `(body: unknown) => Promise<CreateSpaceResult>`. Validation before spawn (design D5): `kind` ∈ {'project','store'} else 400; `path` typeof string, `path.isAbsolute`, `CONTROL_CHAR_PATTERN`-free (import from `submit.ts`), length ≤ 4096, else 400 `invalid_input`; `id` when present validated via the store module's `validateStoreId` (import from `src/core/store/foundation.js`); for kind 'store' with no `<path>/rasen` directory, `id` is REQUIRED (400 when missing). Verb selection per design D4 table: project ⇒ `['init', path]`; store+existing `rasen/` ⇒ `['store','register',path,'--yes',...(id?['--id',id]:[]),'--json']`; store+fresh ⇒ `['store','setup',id,'--path',path,'--json']`. Spawn `process.execPath [cliEntry, ...argv]` with `shell:false`, cwd = the server process cwd (never client input), same own-installation `resolveCliEntry` pattern, 60s timeout + SIGTERM→2s→SIGKILL with submit.ts's slot-release-on-close discipline, own cap-1 `inFlight` (409 `busy`). Admission check via `getBoundedCliEntry(<op>)` before spawn (500 when the table lost the row). Non-zero exit ⇒ 422 with CLI message (JSON stdout when parseable, else stderr) + exit code + stderr. Zero exit ⇒ re-run `handleSpaces()` and find the entry: kind project ⇒ canonical root match on the target path; store ⇒ store entry whose id matches (register may derive id from metadata/folder — parse `--json` stdout for the id when present, fall back to canonical-root match); not found ⇒ 500 `cli_protocol_error`. Success: `{ ok: true, status: 201, response: { operation: 'init'|'store-register'|'store-setup', space } }`.
- [x] 3.3 Router wiring in `src/core/management-api/router.ts`: add `/api/v1/local-paths` to `MANAGEMENT_PATHS`; `isMethodAdmitted`: GET-only for local-paths, GET+POST for `/api/v1/spaces`; handle `GET /api/v1/local-paths` (query `path` via `url.searchParams`) and `POST /api/v1/spaces` (body via the existing `readJsonBody`; one `createSpaceCreator` instance per router, like `submitChange`).
- [x] 3.4 Tests `test/core/management-api/create-space.test.ts` (mirror `submit.test.ts` patterns, `cliEntryOverride` with a stub script): validation rejections (bad kind / relative path / option-like path / control chars / missing id for fresh store / bad id) all spawn nothing; verb selection for the three branches (assert exact argv); 422 passthrough with exit code + stderr; success finds the space via a stubbed listing (spawn a stub that creates a fake `rasen/` + registry? simpler: stub CLI exits 0 and test injects a `listSpaces` seam — add an optional `listSpacesOverride` to `createSpaceCreator` options for this); cap-1 busy; timeout 504. Router admission tests in `test/core/management-api/router.test.ts`: local-paths GET admitted/POST 405; spaces POST now admitted (update the existing 405-on-POST expectation), PUT/DELETE still 405; both 401 without token.
- [x] 3.5 One integration test (may be slow-marked): through the real CLI entry (`dist/cli/index.js` — requires `node build.js` first) spawn `kind:'project'` init into a temp dir and assert 201 + the space entry appears; guards D4's non-interactive-init assumption. On Windows EBUSY/timeout flake, isolate-rerun before calling it a regression.

## 4. UI — client and types

- [x] 4.1 In `packages/ui/src/api/types.ts`: mirror `LocalPathsResponse`/`LocalPathEntry`, `CreateSpaceRequest { kind; path; id? }`, `CreateSpaceResponse { operation; space: SpaceEntry }`.
- [x] 4.2 In `packages/ui/src/api/client.ts`: `listLocalPaths(path?: string)` → `GET /api/v1/local-paths[?path=]` (encode once); `createSpace(body: CreateSpaceRequest)` → `POST /api/v1/spaces` (json). Reuse the single `request()` seam.
- [x] 4.3 New `packages/ui/src/store/recent-spaces.ts`: localStorage-backed recency — `recordSpaceVisit(selector)` (called from a small effect where the space route resolves; cap ~16, most-recent-first, de-duplicated) and `getRecentSpaces(): string[]`. Guard all storage access in try/catch (private-mode safety).

## 5. UI — Spaces page and switcher (D1, D2)

- [x] 5.1 New `packages/ui/src/components/SpacesPage.tsx`: fetch `listSpaces()` + `getKey('ui.pinnedSpaces')`; search input filters by id/name/root (case-insensitive substring); order: pinned (config order) → projects A-Z → stores A-Z; store rows show member chips (read-only, reuse the existing member-chip styling); each row links via `spaceHref(space, 'board')`; pin toggle per row PUTs the full updated selector array (`putKey('ui.pinnedSpaces', { scope: 'global', value })`) with optimistic update + error banner on failure; pins matching no listed space are skipped in rendering and PRESERVED in writes. Reuse existing warm-editorial classes; no new visual language.
- [x] 5.2 Create-space flow on the page (design D4/D3): kind toggle (project | store), directory picker driven by `listLocalPaths` (breadcrumb/parent navigation from `parent`, entries clickable when `isDir`, git repos visibly marked, plus a free path input accepting an explicit absolute path — the sole escape above home), store-id input (required only when the picked directory lacks `rasen/` — the server enforces; the UI may show the field always for kind store), submit → `createSpace`; success routes into the returned space's board (`spaceHref`); failure shows the CLI's verbatim message. SPA-click nav only (hard navigation drops the token).
- [x] 5.3 In `packages/ui/src/app.tsx`: add `<Route path="/spaces" component={SpacesPage} />` before the default route. In `packages/ui/src/components/SpaceSwitcher.tsx`: build the option list as pinned-first (fetch `ui.pinnedSpaces` via `getKey`), then recents (`getRecentSpaces()`), fill to cap 8 alphabetically; ALWAYS include the current space; append the `__all__` option "All spaces…" which `route('/spaces')` instead of switching; record visits via `recordSpaceVisit` where the space route is derived (one effect in `Layout` or the switcher).
- [x] 5.4 UI tests: `packages/ui/test/components/spaces-page.test.tsx` (listing renders both namespaces + members; search filters; pin reorders + writes the array; dead pin ignored+preserved; create flow happy path routes into new space (mock client); CLI error rendered verbatim; picker marks git repos, navigates via parent, accepts typed absolute path) and update `space-switcher.test.tsx` (cap at 8, pinned first, current space always present, "All spaces…" routes to /spaces and does not change space). Update `app.test.tsx` route table expectation if it asserts routes.

## 6. Verification

- [x] 6.1 `node build.js` then targeted suites: `pnpm vitest run test/core/management-api test/core/config-keys.test.ts test/commands/config-editor.test.ts` and `pnpm --filter @atelierai/rasen-ui test`.
- [x] 6.2 Full `pnpm test` in the worktree; Windows CLI-spawning flakes (EBUSY/10s timeout) get an isolated rerun before being called regressions. — Full suite run; all failures except one were flakes (disjoint failure sets across two runs; all pass isolated). The lone deterministic failure is `test/specs/source-specs-normalization.test.ts` on `rasen/specs/archive-ui/spec.md`'s placeholder Purpose — created by the PRIOR `354ce3a` ui-space-redesign archive, outside this change's surface (a pre-existing known-open baseline).
- [x] 6.3 `rasen validate ui-config-redesign-spaces-page` passes (ran from the worktree, which holds the change artifacts + delta specs). Hand smoke's bullets are covered by automated jsdom tests (/spaces render, pin write, create-flow route-into-space, picker git-mark + parent/typed nav, switcher cap ≤8 + "All spaces…"); a live browser walkthrough is left for QA.
