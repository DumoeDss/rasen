## 1. Detection and synthesis (draft.ts)

- [x] 1.1 Add `detectParallelFrontiers(def): Array<{ source, target, branches: string[] }>` per design D1 — ordered (S, T) pairs over root connections, S ≠ T, both editable kinds, neither a FanOut/Join; a branch m is an AtomicStage with in(m) = {S} and out(m) = {T} via S→m and m→T edges; frontier iff ≥ 2 branches; plus `completedFrontier(def, connection)` filtering to frontiers whose branch-edge set contains the just-drawn connection
- [x] 1.2 Add `synthesizeParallelFrontier(def, input): { next; fanOutId; joinId }` per design D2 — re-detect from (source, target) on the live draft and re-check cleanliness/kinds; remove every drawn S→m and m→T connection (consumption rule: none survive); mint via `createParallelPair` with `v2NodeIdFor` ids and the reviewed membership/cap/budget/outcomes; add the wiring on rendered handle ids via `addV2Connection`/`v2ConnectionIdFor` (S→FanOut@input; per member FanOut@<memberId>→m@input and m@done→Join@<memberId>; Join@<proceed>→T@input)
- [x] 1.3 Unit tests for 1.1-1.2 in `packages/ui/test/canvas/draft.test.ts`: clean sandwich detected; two-branch minimum; side-branch (extra in or out edge) excluded and the offer absent when clean branches drop below two; non-AtomicStage branch excluded; S or T already FanOut/Join → no frontier; synthesis consumes the sandwich (no S→m / m→T survives), wires the four families with exact endpoint/port ids, follows the reviewed required/optional split and outcome picks; `createParallelPair` refusal strings surface verbatim; no node in `next` gains `legacyRuntimeOwner` (`not.toHaveProperty`); result membership editable via `setParallelMembers`

## 2. Offer surface and review UI

- [x] 2.1 Extend the toast slot in `PipelineCanvasPage.tsx` with an optional action (`{ label, onClick }` — button inside the toast, auto-dismiss suppressed while an action is present); all existing callers unchanged
- [x] 2.2 Create `packages/ui/src/canvas/V2ParallelReviewPanel.tsx` per design D3 (child-2/3 review pattern): read-only source/target line, branch list with required-vs-optional toggles (default all required), concurrencyCap and budget integer fields under the authoring-draft-errors discipline (defaults cap `max(1, min(3, N))`, budget `max(1, N)`), proceed/failed outcome selects over `def.outcomes` (defaults first/second), refusals in place of Confirm, edits survive an error; `data-testid="v2-parallel-review-panel"`
- [x] 2.3 Wire the page: after a successful v2 connect, run `completedFrontier(nextDraft, connection)` and surface the offer toast with a "Run in parallel" action when it fires; the action opens the review prefilled from detection; on confirm run `synthesizeParallelFrontier`, then `setDraft` + `recomputeFlow(next, catalog, [result.fanOutId])` (selectionOverride pairing) + `markDraftChanged()` + success toast; model errors surface as toast + in-dialog line with the review open; the palette Parallel gesture path (`addRootGesture('parallel')` → `addParallelFrontier`) is untouched

## 3. Tests

- [x] 3.1 Component tests in `pipeline-canvas-page.test.tsx` (existing connect/selection triggers, no new seam): the offer appears only when a connection COMPLETES a frontier (not on the first branch edge); dismissing leaves the draft unchanged; the review's toggles/caps/outcomes land in the definition actually sent to validation (POST-body walk asserting membership metadata, cap, budget, and outcomes, plus no `legacyRuntimeOwner` on the pair); confirm leaves the fan-out selected with its panel open and the drawn sandwich gone from the POSTed definition; the palette parallel gesture still works
- [x] 3.2 Run the full UI suite via the CI-canonical `pnpm --dir packages/ui exec vitest run`, CITE the file/test counts against the 67 files / 815 tests baseline, and fix regressions (do not pipe the gate through `tail`) — 67 files / 838 tests passed, exit 0 (+23 over the 815 baseline: 17 model + 6 component)

## 4. Gates

- [x] 4.1 Real-browser verification via direct CDP on a throwaway Chrome (fresh `--user-data-dir` + a FRESH `--remote-debugging-port` — 9333-9340 are used; `--window-size=1600,1000`; never the user's daily browser; `pnpm --dir packages/ui run build` then `node bin/rasen.js ui --no-open --no-daemon --port <p>`): author S→b1→T, then draw S→b2 and b2→T — the completing edge surfaces the offer; open the review, flip one branch optional, set cap 2, confirm; assert the drawn sandwich connections are gone, the pair plus the four wiring families exist with correct ids, the fan-out is selected, and membership edits work through its panel; then exercise the palette Parallel gesture once more (close the selection panel before handle drags, focus before blur on inputs, re-fit-view before every drag); record the transcript and driver in the change's evidence dir — ALL 25 CHECKS PASSED on throwaway Chrome 151 headless (CDP 9341, app 4550, chunk PipelineCanvasPage-BLQjnmM9.js): every intermediate edge offered nothing, the completing b2→T surfaced the offer, the review prefilled exactly, confirm consumed the sandwich and wired the six-edge pair with exact ids, fan-out selected + panel open, membership/required edits worked through the panel, palette gesture minted fan-out-2/join-2; evidence/cdp-transcript.md + cdp-results.json + 7 screenshots
- [x] 4.2 Assert `git diff <last-archived-child-ship>..HEAD -- src/core/pipeline-registry/` is empty (IR frozen), `V2_BODY_PALETTE_KINDS` is still `['AtomicStage']`, and `addParallelFrontier`/`createParallelPair`/`setParallelMembers` are behaviorally untouched (no capability hole) — evidence/gates-4-2.md: registry diff empty vs 864f45b9 (committed, working tree, untracked); V2_BODY_PALETTE_KINDS byte-identical at draft.ts:736; draft.ts's only diff hunk is a +252/-0 end-of-file append, so all three functions are outside it; gesture + pair editors re-exercised in both suites and the browser gate
