## 1. Typed Inference and Configuration Contracts

- [x] 1.1 Add strict `OmniCrossUpstream` and `StageInference` schemas/types to the Pipeline registry, attach optional `inference` to standard agent stages, and cover every upstream kind, rejected secret/transport fields, and legacy stage parsing in `test/core/pipeline-registry/`.
- [x] 1.2 Extend effective stage/execution inspection to combine `stage.inference` with the existing resolved runtime and model, fail preflight when a routed stage has no model, and expose only the credential-free selection through CLI/management JSON with source-compatible tests.
- [x] 1.3 Register and resolve `omnicross.endpoint`, `omnicross.controlTokenEnv`, `omnicross.requestTimeoutMs`, and `omnicross.leaseTtlSeconds` through existing project/store/global config machinery, including UI/wire metadata, validation bounds, loopback-only endpoint normalization, environment lookup, and config diagnostics tests.
- [x] 1.4 Add optional credential-free frozen inference schemas to canonical runtime execution profiles/agent Actions and legacy `auto-run.json` stage state, preserving old Action/run-state decoding and digest behavior when the field is absent.
- [x] 1.5 Add the named bounded `rasen.inference/1` file contract and cross-check helpers for runtime, model, upstream, broker connection identity, and attempt attribution; test malformed/oversized UTF-8 files and Windows paths with spaces.

## 2. OmniCross Route Lease Deep Module

- [x] 2.1 Create `src/core/omnicross/contracts.ts` with closed Zod schemas for versioned create/renew/release requests, token-bearing create responses, token-free metadata, structured daemon errors, and the shared stable Rasen failure union; add focused contract tests.
- [x] 2.2 Implement connection resolution in `src/core/omnicross/config.ts` using `URL`, explicit IPv4/IPv6/localhost loopback checks, non-secret configuration revision, and dispatch-time control-token environment lookup without mutating or forwarding the control credential.
- [x] 2.3 Implement an injectable, bounded HTTP client for create/renew/release with Admin bearer authentication, `Cache-Control: no-store` validation on create, safe one-retry idempotent create behavior, structured error mapping, response-size limits, and redacted transport diagnostics.
- [x] 2.4 Implement descriptor reduction into the closed `CodexRouteBinding | ClaudeRouteBinding` union: accept only the documented OmniCross environment/argument forms, validate loopback proxy URLs and route/model agreement, and reject arbitrary Rasen-owned CLI overrides, `requires_openai_auth`, `OPENAI_API_KEY`, upstream credentials, and token-bearing argv.
- [x] 2.5 Implement `withOmniCrossRoute` with an injectable clock, attempt-derived bounded idempotency key, expiry safety window, renewal scheduling/retry, shared abort signal, idempotent final release, and safe metadata/warning projection; cover create/spawn/renew/cancel/success/failure cleanup branches with fake-clock tests.
- [x] 2.6 Extend shared diagnostic redaction with an explicit in-memory secret set and thread the control token/route token through all OmniCross HTTP, runner, receipt, warning, and serialization paths; add recursive sentinel-secret absence tests.

## 3. Runtime Route Selection and Frozen Recovery

- [x] 3.1 Extend the runtime route resolver and execution preflight so OmniCross-routed Claude/Codex stages always select their target exec bridge, including same-host routes, while unbound same-host stages remain native and unknown-host routed stages fail closed; update route-matrix tests.
- [x] 3.2 Freeze the effective inference identity before a legacy stage's first dispatch, persist it in that stage's `auto-run.json` entry, return it from `pipeline resume`, and ensure resumed/in-progress stages ignore later Pipeline/model/endpoint edits while never persisting lease ids, tokens, or descriptors.
- [x] 3.3 Lower effective inference into canonical `RuntimeExecutionProfile` and `RunAction.agent`, include it in normal profile/Action digest authority, and make retry/reconciliation reuse the frozen route rather than current configuration; add old-record and post-admission-edit compatibility tests.
- [x] 3.4 Wire the frozen-action production executor and hosted/session driver faces through the shared lease lifecycle for routed agent Actions, mapping lease failures into typed Action outcomes without invoking or silently rerouting the backend when acquisition fails.
- [x] 3.5 Freeze the validated leaf/evaluate worker contract from immutable Definition semantics into canonical profile/Action digest authority, propagate it through retries/reconciliation to both routed runners, and fail closed before leasing historical routed Actions whose contract is unknowable.
- [x] 3.6 Apply the shared SessionHost UTF-8 `maxInputBytes` policy to hosted routed turns before Route Lease acquisition and runtime spawn, with multibyte real-face coverage.
- [x] 3.7 Require complete canonical equality between the caller receipt and committed Action, dispatch only the Record-owned Action, and prove independent inference/runtime/model/sandbox/effort/input/session mutations fail before lease/process while historical exact omissions remain compatible.

## 4. Claude and Codex Dispatch Integration

- [x] 4.1 Add `providerOverride`/validated route binding passthrough to `RunCodexExecOptions`, compose its child-only environment with the existing invocation builder for fresh and exact-thread resume, and assert the dedicated `env_key`, Responses wire, disabled storage, no `requires_openai_auth`, and no user-file access.
- [x] 4.2 Add validated route environment passthrough to Claude fresh and exact-session resume dispatch, enforcing the environment allowlist and frozen model while leaving unrouted invocation bytes and behavior unchanged.
- [x] 4.3 Add `AbortSignal` cancellation to both runners using their existing full process-tree termination and bounded-close paths, returning stable cancellation/route-lost receipts and removing abort listeners/timers on every completion path.
- [x] 4.4 Extend `rasen agent dispatch` and its CLI/completion/localization surfaces with `--inference-file`, resolve the control credential internally, execute the runtime through `withOmniCrossRoute`, preserve the one-JSON-receipt contract, and ensure lease creation fails before binary spawn.
- [x] 4.5 Update the source orchestration templates (not generated `.claude/skills` artifacts) so auto execution trusts the execution view's routed `exec-bridge`, writes only the credential-free attempt file in `ephemeraDir`, records frozen inference through the LEAD's single-writer run-state path, and uses the same route on exact resume.

## 5. End-to-End, Security, and Compatibility Verification

- [x] 5.1 Build a reusable in-process loopback fake OmniCross daemon with authenticated create/renew/delete, configurable structured failures, descriptor fixtures, token rotation, request capture, and fake expiry; do not import or modify the sibling OmniCross repository.
- [x] 5.2 Add a Codex vertical integration test covering Pipeline execution view → frozen inference → `agent dispatch` → fake daemon → fixture Codex argv/env/structured result → release, then exact-thread resume with a new token and unchanged logical route.
- [x] 5.3 Add the equivalent Claude vertical integration test for fresh and exact-session continuation, including model/base URL environment assertions and release after success, spawn failure, timeout, and cancellation.
- [x] 5.4 Add concurrent-stage integration coverage with distinct upstreams/models/tokens, proving no child environment or lease operation crosses routes and releasing one stage leaves the other active.
- [x] 5.5 Add fail-closed integration coverage for unreachable daemon, missing/invalid control auth, upstream/model/format/idempotency errors, unsupported schema, mismatched descriptor, expired/lost renewal, and deleted frozen upstream; assert no runtime spawn and no fallback to the user's CLI login.
- [x] 5.6 Hash bytes, mtime, and size of fixture Codex `config.toml`/`auth.json` and Claude settings/credential files before and after routed tests, scan receipts/logs/run-state/evidence/telemetry captures for sentinel secrets, and assert no persistent OmniCross key/binding artifact is created by Rasen.
- [x] 5.7 Run regression coverage for Pipelines, run-state, granted Actions, `agent dispatch`, Claude, and Codex without `inference`; confirm old records remain readable and existing same-host native/cross-host bridge behavior is unchanged.
- [x] 5.8 Run the focused unit/integration suites, full TypeScript check, build, and full test suite once after integration; include native Windows CI coverage for paths with spaces, IPv6 loopback, process-tree cancellation, environment isolation, and argv handling.
- [x] 5.9 Separate structure-preserving typed-result redaction from bounded diagnostics and prove Claude/Codex evaluate results retain more than 100 gaps while nested route secrets are redacted and the result remains schema-valid.

## 6. Documentation, External Dependency, and Migration

- [x] 6.1 Update `.claude/skills/architecture-index/detail/quick-locate.md` and the AI/workflow module detail pages for `core/omnicross`, inference schemas, routed process selection, lease lifecycle, and frozen action/run-state seams, keeping the top-level index thin.
- [x] 6.2 Update user/developer documentation with Pipeline examples for planner/implementer/ship across Opus/Sonnet/DeepSeek, one-time daemon/config prerequisites, failure recovery, security guarantees, and the explicit rule that Rasen users do not create OmniCross downstream keys/bindings.
- [x] 6.3 Add an opt-in, non-CI real compatibility smoke procedure for a released OmniCross daemon plus real Codex/Claude against local mock upstreams, checking tool calls and user-file hashes; document the minimum OmniCross API version once available and keep paid-provider validation manual.
- [x] 6.4 Document the additive migration and rollback boundary: no migration for unconfigured Pipelines/old records, remove `inference` to disable new routing, never resume an already-frozen routed Action with a pre-feature binary, and treat remote daemon discovery, UI authoring, negotiated response storage, and cost attribution as deferred work.

## 7. Post-cap M4 Strategy Rework: Executable Input Authority

- [x] 7.1 Add the optional closed `agent-turn-input/1` authority binding (`mediaType`, rendering contract, exact UTF-8 byte length, and domain-separated content digest) to canonical agent Actions, preserving historical omission bytes/digests and leaving structured `agent.input` plus leaf/evaluate `workerContract` semantics unchanged.
- [x] 7.2 Add the stable prompt-free `change-run-agent-candidate/1` preview to lifecycle receipts; make start/resume/complete/control stop before agent admission; add explicit `pipeline admit` over a private bounded `agent-turn-input-manifest/1` with exact frontier coverage, typed stale rejection, and atomic Action build/admit/grant; update trusted source LEAD rendering to use preview → render → manifest → admit without accepting caller-authored digest/length authority or re-rendering current skill/Pipeline state on retry.
- [x] 7.3 Make frozen-action `turnInput` a transport assertion across hosted and in-tool, routed and unrouted execution: compare the request's exact UTF-8 length/domain digest with the Record-owned Action after complete receipt equality and reject with typed non-retryable `execution_input_mismatch` before backend selection, lease, SessionHost generation work, launcher, or process.
- [x] 7.4 Add route-independent typed executor results for `execution_input_authority_missing`, `execution_input_mismatch`, and `execution_input_too_large`; enforce the authenticated input against the effective shared `maxInputBytes` before any side effect while retaining final assembled-stdin runner checks as defense in depth.
- [x] 7.5 Implement the historical boundary: fail closed before lease/process for routed Actions lacking turn-input authority, preserve prior request-rendered behavior only for historical unrouted Actions, keep legacy `rasen agent dispatch --prompt-file` unchanged, and never backfill from current content or `JSON.stringify(agent.input)`.
- [x] 7.6 Add real Management API discrimination tests for fake Claude and Codex that keep the Action receipt and committed Record canonically equal, mutate only request `turnInput` (including newline and multibyte cases), assert the typed rejection plus zero lease/process activity, and prove matching requests execute the authenticated base prompt with the frozen leaf/evaluate contract.
- [x] 7.7 Add new unrouted hosted and in-tool tests proving the same mismatch check runs before SessionHost/launcher execution; add historical routed-missing, historical unrouted-compatible, exact-resume, and matching-but-over-limit cases, with mutation receipts showing each new guard fails if bypassed or relabelled.
- [x] 7.8 Run focused frozen-action/Management API/SessionHost/Claude/Codex suites, full typecheck, build, full tests, and `git diff --check`; record proof that no completed task history was rewritten and no prompt/route secret or rendered prompt body was added to canonical Records.
- [x] 7.9 Resolve the Teacher-admission boundary (Decision 13): open `agent-turn-input/1` to a second rendering contract without changing the digest preimage, render and admit the consultation Teacher turn inside the runtime rather than through the driver manifest, exclude runtime-rendered candidates from the preview surface, reject an explicit manifest that reaches one with typed `candidate_stale`, and prove each guard by mutation from a reachable state.
- [x] 7.10 Narrow the daemon face's server-side turn-input substitution to Teacher Actions only, so a consultation-eligible SOURCE Action keeps the caller-transported prompt its committed binding authenticates; make the consultation fixture render a realistic prose prompt (not `canonicalJson(agent.input)`) so the substitution can no longer coincide with the driver's bytes, and prove the repair by mutation through the real HTTP daemon face.
