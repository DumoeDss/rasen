# telemetry-admin-console

Telemetry admin console: same-Worker /admin panel behind Cloudflare Access (path-scoped app on telemetry.rasen.io), in-Worker Access JWT enforcement, /api/admin stats via CF SQL API, custom domain attach
