## 1. Compatibility baselines and test fixtures

- [x] 1.1 Extend `test/helpers/layout-migration-fixture.ts` with explicit helpers for mapping versions 1 and 2, tracked plan-input files, ignored/untracked source entries, generated Issue addresses, receipt versions, ref switching, and byte snapshots without changing existing fixture defaults.
- [x] 1.2 Add mapping-v1 characterization cases to `test/core/store/layout-migration-mapping.test.ts` that pin schema acceptance, E1–E4 precedence, unresolved/conflicting outcomes, target-line selection, the complete plan-schema-v1 canonical body and bytes, plan ids/tokens, diagnostics, and project destinations before v2 parsing is introduced; rerun the same vectors after v2 support to prove byte-identical v1 output.
- [x] 1.3 Add receipt-v1 characterization cases around `receipt.ts`, `layout-write-guard.ts`, status, doctor, and recovery so existing receipts remain readable with identical phase and legacy-archive interpretation.
- [x] 1.4 Add a guard test proving the public `StoreIssues` interface and runtime IssueRecordV1/ExecutionPlanRevisionV1 schemas gain no legacy-import method, `sourceChange`, project field on Issue, or receipt-backed lifecycle behavior.

## 2. Mapping v2 and normalized work disposition

- [x] 2.1 In `src/core/store/layout-migration/mapping.ts`, retain the v1 schema and loader path unchanged and add separate strict v2 active/archive discriminated unions for `project-change` and `store-issue`, including contextual state/reason and cross-branch-field validation.
- [x] 2.2 Extend the loaded mapping and migration plan types in `layout-migration/types.ts` with explicit mapping version, declaration nature, source lifecycle, and materialization/disposition unions while keeping v1 normalized exclusively to project-copy behavior.
- [x] 2.3 Update inventory validation and `layout-migration/plan.ts` so E1 remains binding, undeclared trustworthy E1/E2/E3 results normalize to `project-change`, unresolved/conflicting items require a v2 declaration, and explicit assertions preserve rather than erase their evidence chain.
- [x] 2.4 Add stable actionable mapping diagnostics for E1-to-Issue contradictions, missing v2 declarations, invalid lifecycle state/reason, unknown/cross-branch fields, duplicate or case-fold-colliding Issue ids, non-member projects, and conflicting destinations.
- [x] 2.5 Cover v2 mapping behavior in `layout-migration-mapping.test.ts`: active open-only imports, archived explicit open/resolved/dropped imports, reasons for both terminal states, v1 parity, E1 refusal, E2/E3 default project normalization, explicit conflict resolution, strict unknown-field refusal, and excluded naming/content/cwd/branch heuristics.

## 3. Tracked plan input and same-migration references

- [x] 3.1 Add an in-Store plan-input reader behind the layout-migration dependencies that resolves canonical containment, proves the file tracked and clean against Store HEAD/index/worktree, performs strict UTF-8-without-BOM decoding, and returns Store-relative path plus digest without writing.
- [x] 3.2 Define the one-time plan-input node union as the existing change/intent inputs plus a closed `sourceChange` variant that still requires explicit project and target line; reject unknown fields and any selector outside that variant.
- [x] 3.3 Sequence `layout-migration/plan.ts` so project dispositions, catalogs, and verified/minted Change identities are frozen before `sourceChange` resolution, then require an exact active project Change and matching project/target line and compile the selector to canonical `changeInstanceId`.
- [x] 3.4 Verify canonical `changeInstanceId` input nodes through existing Store reference evidence, reject unreadable/ambiguous/foreign/scope-conflicting references, and pass only normalized runtime node inputs to the Issue compiler.
- [x] 3.5 Bind plan-input path/digest and normalized output into the immutable plan/token and extend apply revalidation so moved, edited, staged, untracked, ignored, external, BOM, invalid-UTF-8, or HEAD-divergent input fails before the first write.
- [x] 3.6 Add focused plan-input tests for top-level `nodes:`, valid standard nodes, valid same-migration selectors, project/line mismatch, missing/ambiguous/archived/Issue selectors, duplicate Change identities, invalid DAGs, content drift, containment aliases, BOM/U+FFFD, and the no-input continuation text and command.

## 4. Pure Issue-domain migration compiler

- [x] 4.1 Add an internal pure compiler under `src/core/store/issues/` that accepts only canonical Issue fields, timestamp, and normalized runtime nodes and returns an explicit generated Issue-tree file inventory with UTF-8 contents and digests.
- [x] 4.2 Reuse `records.ts`, `plans.ts`, planning validation, graph checks, revision digest, and standard serializers to emit only `issue.yaml` and optional `plans/0001.yaml`; do not duplicate schemas or call live StoreIssues mutation methods.
- [x] 4.3 Enforce compilation semantics: active source yields open/null reason, archived source uses its explicit legal state, either terminal state has a non-empty operator reason, `createdAt` is migration resource creation time, and no README, placeholder plan, acceptance, Dispatch, project, cwd, commit, Pipeline, or legacy content is generated.
- [x] 4.4 Add interface-level compiler tests for deterministic bytes/digests, portable text/id failures, terminal reasons, directory/id agreement, no-plan output, canonical revision `0001`/null supersedes, DAG rejection, absence of `sourceChange`, and forbidden-field absence.
- [x] 4.5 Add integration assertions that generated Issues are readable and mutable through existing list/show/plan/state/query paths after publication and that receipt changes never override canonical Issue state.

## 5. Immutable plan and generated-tree staging

- [x] 5.1 Define a strict immutable migration-plan v1/v2 union and version-dispatched reader/apply path: mapping v1, no-mapping, and every existing path without v2-only materialization must continue to emit the byte-identical schema-v1 canonical body and planId, while only a mapping-v2 plan that requires explicit materialization emits schema v2 with `copy-tree`, `generated-tree`, and `retain`, source lifecycle/digest, generated file roles/paths/bytes/digests, destination root, and retirement source; reject unknown versions and cross-version fields without shape inference or silent upgrade.
- [x] 5.2 Update plan blockers, destination-existence checks, case-fold uniqueness, dirty-source reporting, retirement-set construction, JSON/human summaries, and commit suggestions to handle generated Issue roots without treating them as project-owned copies.
- [x] 5.3 Extend `layout-migration/apply.ts` staging to write each generated Issue tree from the plan's explicit file list, never copy the legacy coordinator tree, and never discover generated files by glob or filename pattern.
- [x] 5.4 Extend staged verification to check containment, exact file inventory/digests, strict UTF-8/no BOM, Issue record path/id, optional plan schema/content digest, and absence of unplanned files before any publication.
- [x] 5.5 Publish staged Issue roots atomically after catalogs/project outputs and before receipt/layout flip; add one layout-publication wrapper that structurally nests the Store/ref migration-run helper inside the existing generated-Issue batch helper and asserts the expected batch is held before a generated plan enters its run-locked callback, hold both from first destination revalidation through prepared operations, rename/digest/completion, receipt, layout flip, staging cleanup, and final durable manifest persistence, then release run-before-batch in `finally`, while preserving the legacy source byte-identically.
- [x] 5.6 Add no-clobber tests for existing Issue roots, duplicate ids, case-fold collisions, file-versus-directory conflicts, project/Issue destination collisions, destination creation after planning, and proof that no force, subset, partial, manual-move, or dual-write route bypasses a blocker.
- [x] 5.7 Extend the existing `src/core/store/issues/locks.ts` abstraction with one batch helper that validates and constructs the normal `(storeUid, issueId)` keys, deduplicates and unsigned-byte-sorts their canonical `issue-lock/v1` key bytes, acquires them before any migration-run lock, records the held batch in the existing ordering context, and releases partial acquisition or callback failure exactly once in reverse order; do not add a layout-local lock path, stale-owner policy, or second Issue mutex.
- [x] 5.8 Add deterministic barrier-driven interleaving tests, without timing sleeps, for ordinary `create`, `setState`, and `publishPlan` at generated-destination precondition, prepared-manifest, rename, completion-mark, receipt, layout-flip, and final-manifest boundaries: prove a create that owns the key first makes migration revalidation refuse unchanged, held-window mutations write nothing, post-release create sees existing while state/plan read the canonical live tree, and generated bytes/digests equal the frozen plan and receipt before release; also prove canonical multi-key order, duplicate collapse, later-key acquisition failure and callback exception reverse-release, and two same/different-ref migrations with overlapping/disjoint Issue sets plus ordinary commands complete or return bounded contention/semantic refusals without deadlock.

## 6. Source safety, resume, rollback, and retirement

- [x] 6.1 Extend Git/source inspection to enumerate ignored as well as untracked entries below every generated-tree source without following symlinks, junctions, or aliases outside the Store; make every non-Git entry an unconditional blocker even with `--include-untracked`.
- [x] 6.2 Preserve and strictly parse the recorded recovery manifest during process-restart resume, regenerate the same canonical Issue-key batch from the immutable plan, reacquire the entire batch before the Store/ref migration-run lock, regenerate and verify staging, reconcile every prepared operation from staged/destination presence plus run identity and planned digest, mark an after-rename match completed, persist the final manifest before reverse release, and retain no-clobber for every unrecorded, ambiguous, or mismatched destination.
- [x] 6.3 Extend rollback tests and implementation so rollback reacquires the same Issue-batch-before-migration-run order, removes generated Issue roots only when a completed operation or after-rename prepared operation proves this run's identity and exact planned digest, persists rollback/failure state before run-then-reverse-batch release, never deletes unknown or mismatched content, restores exact durable preimages, and leaves pre-existing Issues, legacy sources, member repositories, other refs, and unrelated Store paths byte-identical.
- [x] 6.4 Extend retirement to remove only explicit coordinator source paths, keep generated Issue roots and plan inputs, stamp the same receipt phase idempotently, and succeed on retry after partial removal without broad directory deletion.
- [x] 6.5 Add fault injection at plan-input read, generated-file write, generated-tree digest verification, each Issue-batch acquisition, migration-run acquisition, prepared-operation manifest write, immediately after Issue-root/project-tree/receipt rename but before completion marking, completion-mark write, layout flip, source removal, and final manifest write; assert acquired locks release in the specified reverse order and each failure leaves either the complete flat state or complete v2 state with a working resume/rollback/retire path.
- [x] 6.6 Add source-safety tests for tracked modifications, staged differences, untracked and ignored files/directories/symlinks/junctions, `--include-untracked`, source drift after plan, source preservation through publication, and Git restoration after retirement with matching recursive digest.
- [x] 6.7 Add real process-restart recovery cases for each after-rename/before-completion crash: reload the durable manifest in a fresh module instance, prove matching output ownership from prepared run identity plus digest, exercise both resume and rollback, and prove absent, both-present, unrecorded, and digest-mismatched destinations are blocked without deletion.

## 7. Receipt v2 and typed historical reads

- [x] 7.1 In `layout-migration/receipt.ts`, define strict version-dispatched receipt v1/v2 contracts and one typed read path used by phase detection, diagnostics, recovery, and archive compatibility; invalid receipts report incomplete evidence and prove nothing.
- [x] 7.2 Serialize receipt v2 deterministically with mapping schema/path/digest, explicit Store planning-source revision role, source lifecycle/alias/path/digest, classification/evidence, Issue state/nature/reason, `acceptanceEvidence: unproven` for terminal imports, generated output digests, optional plan-input digest, and phase history.
- [x] 7.3 Preserve existing project-item, minted-identity, dropped-adoption, legacy Archive, shared-spec, retained-doc, superseded-evidence, target-line, and v1 receipt behavior while ensuring no Store HEAD is emitted as a member `codeCommit`.
- [x] 7.4 Update layout-write guards and migration diagnostics to consume the typed reader, accept both receipt versions, keep receipt state historical, and distinguish unreadable/version-unknown evidence without rewriting any receipt.
- [x] 7.5 Add deterministic receipt vectors and tests for v1 reads, v2 round trip, stable field order/newline/no BOM, phase stamping idempotence, Store/member commit-role separation, asserted terminal state with unproven acceptance, output/input digests, source Git recovery, tampering, and unknown versions.

## 8. Actionable diagnostics and archive compatibility

- [x] 8.1 Add stable migration diagnostics and human/JSON rendering for every new mapping, plan-input, source-safety, compilation, destination, verification, provenance, and recovery failure, plus the non-blocking `no plan supplied; no nodes invented` continuation using the existing Issue plan command.
- [x] 8.2 Add a read-only exact receipt query used only by ordinary direct-selector archive planning that, for the current resolved Store identity/ref, finds one v2 `active-change` conversion by old alias, reports invalid/unreadable/ambiguous evidence, excludes interactive selection, intent-template, token-owned `--apply-plan`/`--abort-plan`, archived-source entries, and v1 receipts, and never returns live Issue state.
- [x] 8.3 Integrate the query at the `src/core/archive.ts` direct source-not-found seam with fixed precedence: early command/token conflicts return `archive_option_conflict` without receipt access; a real active Change wins and remains subject to `finalization_outcome_required`; an absent source with one proven conversion returns non-zero `legacy_coordinator_became_issue` before outcome interpretation; absent or inconclusive conversion evidence falls back to the existing outcome/not-found ordering.
- [x] 8.4 Ensure archive confirmation, validation, outcome, reason, successor, target-line, commit, token, spec-sync, ephemera, dry-run, resume, and other options are not forwarded or translated; optional Issue state guidance is printed only as a separate operator action and executes nothing.
- [x] 8.5 Add direct archive tests for unique conversion with and without `--outcome`, real-Change plus missing-outcome precedence, `--apply-plan`/`--abort-plan` conflict precedence and zero receipt reads, archived-source exclusion, other Store/ref isolation, v1/invalid/unreadable/ambiguous receipts, every finalization-flag family, human/JSON parity, non-zero status, and byte-identical Store/member/Git state.

## 9. Cross-platform path and encoding matrix

- [x] 9.1 Extend `layout-migration-windows-paths.test.ts` with `path.win32` and native-Windows cases for mixed-case drive letters, slash/backslash aliases, UNC/long paths, case-fold Issue collisions, junction containment, and Store-relative receipt paths.
- [x] 9.2 Add `path.posix` and macOS/Linux cases for case-sensitive siblings, case-insensitive collision simulation, symlink containment/escape, long paths, and identical multi-ref behavior without hardcoded separators.
- [x] 9.3 Add non-ASCII Store/mapping/plan/source filenames and Chinese Issue titles to mapping, compiler, staging, receipt, recovery, archive diagnostic, and Git-restore tests; assert exact bytes and paths without sanitization.
- [x] 9.4 Add strict text vectors for UTF-8 without BOM, UTF-8 BOM, invalid byte sequences, U+FFFD, and typical mojibake sentinels across mapping, plan input, generated YAML, and receipt JSON; generated artifacts must round-trip cleanly on Windows, macOS, and Linux.
- [x] 9.5 Add or extend Windows CI execution for the focused migration/Issue/archive suites and ensure macOS/Linux CI runs the native symlink, case-sensitivity, and Git-ref cases with platform-specific skips limited to unavailable filesystem primitives.

## 10. Real scene-bridge fixture and end-to-end journey

- [x] 10.1 Build a committed test fixture from the real scene-bridge shape with active `time-qualified-preview-render-job-and-reference-video` and archived `2026-08-01-core-project-and-scene-lifecycle`, `2026-08-01-protocol-spine-and-live-cube`, and `2026-08-03-named-camera-shot-camera-path-and-timeline`, preserving representative bytes without depending on the external checkout at test time.
- [x] 10.2 Author a v2 fixture mapping that explicitly imports the active coordinator open and explicitly declares each archived coordinator's legal state and reason; include project Changes plus at least one clean plan input using `sourceChange`, and leave one Issue without a plan.
- [x] 10.3 Add an end-to-end CLI journey covering inventory, mapping preview, human/JSON parity, immutable token apply, project identity minting, generated Issue reads, canonical revision/reference verification, no-plan continuation, receipt inspection, publication commit suggestion, and separate retirement.
- [x] 10.4 Extend the journey with a second flat ref and same-named content, destination conflicts, injected interrupted publication/resume, rollback before retirement, retirement retry, post-retirement Git source restore/digest verification, and proof that member code repositories never change.
- [x] 10.5 After retirement, exercise ordinary direct-selector `rasen archive` against the converted active alias with no outcome and assert `legacy_coordinator_became_issue`; assert token-owned apply/abort never query receipts, the three archived-source aliases do not redirect, and a real v2 Change without an outcome still returns `finalization_outcome_required` before normal finalization can proceed.

## 11. Documentation, regression, and release checks

- [x] 11.1 Update Store migration and command documentation with mapping v2 examples, v1 compatibility, plan-input tracking rules, active/archive state semantics, Store-source provenance, no-plan follow-up, archive diagnostic, rollback/retirement recovery, and the explicit 0.1.7 compatibility-only version boundary.
- [x] 11.2 Add regression assertions that no new command/completion, public Issue import method, coordinator index, second receipt authority, automatic acceptance, Change back-reference, member-repository write, or legacy-tree copy was introduced.
- [x] 11.3 Run the focused mapping, plan-gate, provenance, catalog/receipt, apply/recovery, doctor, Issue resource/CLI/query, archive, Windows-path, and end-to-end suites; fix root causes and retain fault evidence for any platform-specific failure.
- [x] 11.4 Run `pnpm lint`, `pnpm test`, and `pnpm build`, then run the repository's release-contract checks that apply to 0.1.7 and verify no unrelated generated or main-spec files changed.
- [x] 11.5 Run strict Change validation, inspect human and JSON status as apply-ready, check `git diff --check`, and strictly decode every changed source/spec/doc/test file as UTF-8 while rejecting BOM, U+FFFD, mojibake, unintended line-ending rewrites, and out-of-scope diffs.
