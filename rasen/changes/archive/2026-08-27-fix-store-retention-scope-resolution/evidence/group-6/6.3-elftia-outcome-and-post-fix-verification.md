# Task 6.3 — the corrected elftia outcome, and post-fix verification (2026-08-27)

## The elftia record, verified rather than restated

Both `document-skills-xlsx-*` Changes were ALREADY archived on 2026-08-25 by the
official finalization engine. Read from `elftia-store` today:

    rasen/projects/ed2cf5bf-.../changes/archive/main/
      2026-08-25-document-skills-xlsx-advanced-authoring--19cd36b1c128
        schemaVersion 2 · outcome landed · reason null
        specSync.applied true · codeMerge 73a84a67 reachable true
      2026-08-25-document-skills-xlsx-completion--38aafd3af2ec
        schemaVersion 2 · outcome landed · reason null
        specSync.applied true · codeMerge 73a84a67 reachable true

So the waiver covered ONLY the retention step; the archiving itself was done
properly and both records are sound. **There is nothing to re-archive, and this
change repairs nothing retroactively.** What it removes is the need for that
waiver on the NEXT such delivery.

## Post-fix verification, on a real pair, not a fixture

Two independent levels, both against the real `issue-registry` Store with the
real machine registry:

1. **The three refusals are gone** (`6.3-three-refusals-post-fix.txt`),
   reproduced against the same `dmpi` pair that produced them on 2026-08-26:
   scope resolves from the planning worktree; the Store checkout now asks for a
   project instead of inventing one from the init-minted root projectId; and an
   explicit `--project` is admitted by the planning-bound gate via the recorded
   pair.

2. **A full finalization succeeded through that pair** (`6.2-pilot-completed.md`)
   — stronger than the "retain prepare succeeds" this task asked for, because it
   is the whole retention/archive path the waiver had to skip: archive to
   `outcome: landed`, spec sync applied, code commit proven reachable,
   association applied, planning branch merged, pair cleaned up losslessly.

Note there is no `rasen retain prepare` CLI verb — `rasen-retain` is a skill;
the retention/archive path through a pair is `rasen archive`, which is what was
exercised.

## Carried forward, not fixed here

`split_planning_truth` (`resolver.ts:2220-2226`) is raised with
`{ target: 'project.planning' }` and NO `fix` field, so it names the problem and
not the repair. The repair that actually works is `rasen store adopt --to <store>`
(plus fast-forwarding any execution worktree still on an older ref). That is the
same correct-but-illegible class the layout-migration rehearsal triaged as (b),
but it lives in the planning resolver and is outside both new specs.
