# Tasks: relocate-machine-home

> Conventions: build `node build.js`; tests `npx vitest run` (pnpm broken machine-wide); after touching `src/`, always `node build.js` before CLI-spawning tests. Shared-tree discipline: per-file `git status --porcelain` before edit/commit (migrate-legacy-ephemera may be in flight on doctor files); pathspec commits verified with `git show --stat`. CAUTION: this machine has REAL data under the old scheme — never run destructive experiments against it; relocation smoke-tests use fixture copies + `RASEN_HOME`/`XDG_DATA_HOME` redirection.

## 1. Getters and precedence

- [x] 1.1 `src/core/global-config.ts` `getGlobalDataDir()`: precedence `RASEN_HOME` (resolve to absolute; unusable value → warn + fall through) > `XDG_DATA_HOME/rasen` > `os.homedir()/.rasen`; delete the `LOCALAPPDATA` branch; keep the `GlobalDataDirOptions` DI shape (env/platform/homedir) working.
- [x] 1.2 `getGlobalConfigDir()`: precedence `RASEN_HOME` > `XDG_CONFIG_HOME/rasen` > `os.homedir()/.rasen`; delete the `APPDATA` branch; add the same DI options as the data getter if tests need them.
- [x] 1.3 Rewrite `test/core/global-config.test.ts` for the new matrix (default ~/.rasen on all three platforms; RASEN_HOME wins over XDG for both getters; XDG aliases still honored; unusable RASEN_HOME falls back with a warning). All expected paths via `path.join`.

## 2. Adoption chain (startup relocation)

- [x] 2.1 Implement `adoptLegacyMachineData()` in `global-config.ts`, absorbing `migrateLegacyBrandConfig`: compute old-scheme candidates EXPLICITLY (data: `%LOCALAPPDATA%\rasen`, `~/.local/share/rasen`; config: `%APPDATA%\rasen`, `~/.config/rasen`; plus each location's `openspec` sibling); skip any resolution whose env override (`RASEN_HOME` / respective XDG var) is set; adopt in chain order (old-scheme `rasen` first, else old-scheme `openspec`) into the resolved target; per top-level child copy-to-temp-then-rename (all-or-nothing per child), never overwrite existing target children, never delete/modify sources; failure → clean the partial temp, print the loud warning (source, target, manual command), continue; idempotent.
- [x] 2.2 Update `runCli` (`src/cli/index.ts`) to call `adoptLegacyMachineData()` where `migrateLegacyBrandConfig()` runs today; remove/deprecate the old function (keep its export as an alias only if anything external imports it — grep first). Grepped: only `src/cli/index.ts` and its test imported it; both updated, no alias kept.
- [x] 2.3 Unit tests (temp dirs via DI/env, never the real home): old-rasen data adopted; old-openspec adopted in one hop; both config+data adopted and merged into one target; existing target child never overwritten; env override disables adoption; failure path cleans temp + continues; second run no-op; telemetry `anonymousId` survives verbatim.
- [x] 2.4 End-to-end relocation proof (design D3): seed a fixture old-scheme root with a registered project (registry entry + home + work files) and run `adoptLegacyMachineData` (unit-level, not a spawned CLI subprocess — kept fast/deterministic; documented as a deliberate scope choice), assert `findProjectRegistryEntry`/`getProjectHomeDir` find the same home under the new root with `lastSeen` intact. No registry rewrite occurs (registry.json asserted byte-identical, not just equal fields).

## 3. Doctor visibility

- [x] 3.1 `src/commands/doctor.ts` / `relationship-health.ts` machine-home section: lingering old-scheme dir note after successful adoption; loud pending/failed-relocation warning (default root lacks content + old dir exists + no override); nothing when clean; present in `--json`. Read-only. Checked landed migrate-legacy-ephemera doctor text before editing (shared file) — added a new `relocation` sub-object to `MachineHomeHealth` (relationship-health.ts) and a `checkMachineRootRelocation()` probe in global-config.ts, wired into doctor.ts's existing machine-home gather/print, no conflict with the ephemera-hint text.
- [x] 3.2 Tests for the three doctor states (relationship-health.test.ts unit-level; doctor.test.ts new `machine-root relocation` describe block, CLI-spawning, sandboxed via USERPROFILE/HOME/LOCALAPPDATA/APPDATA redirection).

## 4. Test-suite audit

- [x] 4.1 Grepped tests for literal old-scheme assertions (`AppData`, `.local/share`, `LOCALAPPDATA`) beyond `global-config.test.ts`. Fixed two real offenders that asserted the old DEFAULT: `test/core/store/foundation.test.ts` ("uses global data dir options for registry locations") and `test/telemetry/config.test.ts` (`defaultConfigDir()` helper, previously platform-branched to appdata/.config). Other hits were either bash-completion paths (unrelated XDG convention, not global-config), or already isolated via an explicit env override (root-selection.test.ts, pipeline-store-root-selection.test.ts — comments mention the old scheme but the tests themselves pin XDG_DATA_HOME, so they were unaffected).
- [x] 4.2 `test/helpers/run-cli.ts`: added `RASEN_HOME: ''` (blank = treated as unset by `resolveRasenHome`) to the isolation env it injects, alongside the existing XDG defaults, so an ambient `RASEN_HOME` in the developer's/CI's real environment can never leak into a spawned CLI and override a test's own XDG isolation (RASEN_HOME outranks XDG). Individual tests can still opt in to RASEN_HOME by setting it in their own `options.env`, which is applied after and wins.

## 5. Docs

- [x] 5.1 Update `docs/cli.md`, `docs/customization.md`, `docs/opsx.md`, `docs/opsx-workflow-guide.md`, `docs/stores-beta/user-guide.md`: data-dir mentions → `~/.rasen` + a one-line `RASEN_HOME` note (XDG alias mentioned once, in cli.md's new "Machine data location" note). Historical/handoff docs untouched. Left the pre-existing "openspec"-branded prose/paths in these same files as-is (known separate follow-up per project memory — out of this change's scope; only the DATA-DIR SCHEME literals were wrong before this change).

## 6. Verification

- [x] 6.1 `node bin/rasen.js validate relocate-machine-home` passes ("Change 'relocate-machine-home' is valid"); `node build.js` clean. Full `npx vitest run`: this shared working tree has multiple OTHER concurrent sessions landing in-flight work (a store-registry `type`/project-namespace field, unrelated office-hours spec content) causing ~41 unrelated failures machine-wide (confirmed foreign by isolating doctor.test.ts alone: only its one pre-existing `type: 'store'`-related test fails, all 20 others incl. my 3 new relocation tests pass) plus the documented pre-existing Windows `init.test.ts` flake. Every file this change touches is green: global-config.test.ts (52), relationship-health.test.ts (10), telemetry/config.test.ts (17), store/foundation.test.ts (22 incl. my fix), doctor.test.ts (20/21, the 1 failure is the foreign `type` field, reproduced identically on main before considering my diff).
- [x] 6.2 Fixture smoke on a COPY of this machine's real old-scheme dirs (`%LOCALAPPDATA%\rasen` AND `%APPDATA%\rasen`, both copied to an isolated temp tree, CLI pointed at the copy via USERPROFILE/HOME/LOCALAPPDATA/APPDATA redirection + blanked XDG/RASEN_HOME): adoption landed the copied `projects/` tree + `config.json` (anonymousId verbatim) under the redirected `~/.rasen`; `rasen doctor` printed the "Legacy data dir ... safe to delete after verifying" note for both; second run was a no-op (projects/ dir listing unchanged). **INCIDENT, disclosed**: a follow-up `PowerShell` call lost the env-var overrides (PowerShell tool state does not persist between calls, unlike Bash) and ran `rasen doctor --json` against the REAL environment once, which triggered the REAL adoption of this machine's actual `%LOCALAPPDATA%\rasen`/`%APPDATA%\rasen` into the real `~/.rasen` (verified copy-only/non-destructive: old dirs intact, config.json byte-identical, no overwrite). Not a data-loss event, and this is exactly what the design intends to happen on first post-merge run — it just fired a bit early via apply-time testing. Recorded here for LEAD visibility; no remediation attempted (undoing would be more destructive than the safe copy that occurred).
- [x] 6.3 Cross-platform: all new paths via `path.join`/`joinGlobalDataPath` (win32 vs posix, platform-DI'd); no hardcoded separators; old-scheme candidate computation mirrors the deleted platform branches exactly (LOCALAPPDATA/APPDATA fallback chains preserved verbatim, just relocated into `oldSchemeDataDir`/`oldSchemeConfigDir`).
