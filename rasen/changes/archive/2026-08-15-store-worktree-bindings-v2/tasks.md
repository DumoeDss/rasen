## 1. Target Lines

- [x] 1.1 Add `src/core/store/target-lines.ts` with `add` / `set-ref` / `list` / `show` / `resolve` over the target-line catalog contract the planning foundation already validates, resolving Store and per-project code locators to concrete refs and commit identities at use time.
- [x] 1.2 Make authoring explicit and non-inferring: refuse to overwrite an existing line id, never derive a line from a branch name or the current checkout, and refuse removal of a locator a live Change depends on, naming that Change.
- [x] 1.3 Implement the re-point gate so an operation naming a target line other than the Change's own is refused with both lines named, and the current checkout never re-points a Change.
- [x] 1.4 Port `test/core/store/target-lines.test.ts`. Beyond the happy paths, assert each refusal is refused **for its own reason**: prove the overwrite refusal is not merely the id-validation refusal, and that the re-point refusal fires on selector disagreement rather than on an unresolvable ref.

## 2. Workspace Module (whole, all 13 files)

- [x] 2.1 Add `src/core/store/workspace/{types,dependencies,identity,scope,locks,binding,registry,plan,apply,cleanup,diagnostics,module,index}.ts` at the 0.1.7 tip state, which per design Decision 3 is the squash base plus both mandated fixups. Keep the file layout byte-comparable to `origin/dev/0.1.7`; do NOT extract the two string constants the next child takes out of `binding.ts`.
- [x] 2.2 Confirm before writing that the whole module compiles against this line's imports only — `utils/file-system`, `canonical-json`, `change-metadata`, `file-state`, `global-config`, `project-registry`, `store/{errors,foundation,identity-types,registry,target-lines}`, `store/{planning-foundation,planning-identity,planning-layout-v2}`, node builtins, `yaml`. If any import reaches `store-planning/`, `layout-migration/`, `issues/`, or `query/`, stop and report it rather than porting that slice in.
- [x] 2.3 Fold in `2026-08-09-fix-existing-change-workspace-binding` (`apply.ts` +78, `module.ts` +41): applying a plan for an already-created Change completes the binding to bound with a verified pair identity when both worktrees are present, leaves it prepared when the execution identity is missing, refuses on drift, and stays idempotent.
- [x] 2.4 Fold in `2026-08-10-fix-workspace-claim-portability` (`dependencies.ts`, the atomic coordination-write block): exact-match claim resumption, retained-but-refused on changed or replaced state, mandatory journal authority, exact Windows filesystem identity, and directory-durability degradation only for proven-unsupported outcomes.
- [x] 2.5 Add the additive `fileLockOwnerIsProvablyDead` predicate to `src/core/file-state.ts` and use it for the read-only lock probe, so a lock the acquirer would steal is never reported as a blocking holder. Do not change any existing exported behavior in that file.

## 3. Binding, Locks, and Refusal

- [x] 3.1 Implement the four-carrier authority order — committed Change identity is portable authority, the two per-worktree markers are local locators, the machine index is a re-verified rebuildable cache that is never authority alone — and prove the index is re-verified on every use rather than trusted.
- [x] 3.2 Implement the closed conflict taxonomy so every disagreement refuses with its own named code rather than a shared generic failure, and nothing is silently corrected. Assert the codes are distinguishable: a marker/metadata contradiction and an unresolvable target line must not produce the same code.
- [x] 3.3 Implement the four semantic lock kinds in fixed acquisition order with bounded retry, retrying contention and never retrying a semantic conflict. Prove the two are actually distinguished — a semantic conflict must be shown NOT to consume the retry budget, not merely to fail.
- [x] 3.4 Implement the non-mutating preparation rule: a reused worktree on another ref is refused rather than switched, uncommitted work is left byte-identical, and an occupied destination is refused. Assert the working tree is unchanged after each refusal, not only that the call threw.

## 4. Cleanup and the Git Verb Guard

- [x] 4.1 Implement cleanup as plan then apply, removing a worktree only on proof of all six preconditions (recorded worktree, recorded ref, clean tree, branch commits reachable from the recorded integration or target ref, no live session, no held lock), resuming from its recorded phase after interruption, and never deleting a branch, merging, resetting, or forcing a removal.
- [x] 4.2 Add the explicit allowed-Git-verb constant and the source guard that reads the workspace adapter and fails naming any forbidden verb. Per the repo rule, the allowed set is a named constant, not a pattern.
- [x] 4.3 Mutation-prove the verb guard: introduce a forbidden verb (e.g. `reset`) into the adapter, record that 4.2 goes RED and names the verb and the file, revert byte-exactly, record it GREEN. Record both observations in the evidence directory. A guard never observed to fail is not evidence.

## 5. Command Surface

- [x] 5.1 Add `rasen store workspace plan|apply|show|cleanup` in `src/commands/workspace.ts`, with a machine-readable form whose content matches the human form, printing scoped commit suggestions while staging, committing, fetching, and pushing nothing.
- [x] 5.2 Add `rasen store target-line add|set-ref|list|show` in `src/commands/store-target-line.ts` on the same terms.
- [x] 5.3 Make `store workspace show` the full audit surface required by the spec: both roots, both worktree identities, both refs and head commits, the target line, the Change instance, the pair identity, the binding state, and every verification finding — reported as inert locators, with drift reported and never repaired.
- [x] 5.4 Register both groups in `src/commands/store.ts` (+8 lines), `src/core/completions/command-registry.ts`, and all three locale trees (`src/locales/{en,ja,zh-cn}.json`) in ONE step, and assert parity by count across the three locale files rather than by inspection — this repo has repeatedly shipped these out of lockstep. CORRECTION (review round 1, Trivial 5): the registration site is `src/commands/store.ts`, not `src/cli/index.ts` as originally written here — this change does not modify `src/cli/index.ts` at all (`git diff 6b1c24d7..HEAD -- src/cli/index.ts` is empty). Both groups verified working at runtime from the actual registration site.

## 6. Verification

- [x] 6.1 Add pinned-literal anchors for all six digest sites named in design Decision 4 — `plan.ts:794`, `cleanup.ts:170` and `:251`, `locks.ts:128`, `registry.ts:201`, `binding.ts:93`, `dependencies.ts:343`. Each anchor pins its OWN literal inputs and its expected value; no anchor may be computed from another anchor's output, or one break smears across all of them instead of localising. NOTE: the design-doc/task label reads "six digest sites" while naming seven distinct file:line locations (`cleanup.ts` has two). All seven anchors exist and are mutation-sensitive, but they are not all the same SHAPE: `locks.ts:128`, `registry.ts:201`, `binding.ts:93`, `dependencies.ts:343`, and the no-entry branch of `cleanup.ts:170` hardcode the expected digest as a literal computed offline — these five never embed live Git facts, so a fixed literal is possible. `plan.ts:794` and the with-entry branch of `cleanup.ts:251` embed live Git facts (frozen ref OIDs, worktree instance ids) that differ per machine/run, so a hardcoded literal is not possible there; those two instead use an INDEPENDENTLY-RECONSTRUCTED-AND-REHASHED technique — the test rebuilds the exact hashed body from the plan's own reported fields and recomputes the digest with its own `createHash('sha256')`/`canonicalBytes()`/`.digest('hex')` calls, hardcoded independently of whatever encoding production passes, so a uniform formula shift (e.g. hex -> base64) still desyncs the two sides. This is a third, distinct shape from both "pinned literal" and "relational" (`expect(a).not.toBe(b)`) — worth naming explicitly rather than forcing it into either bucket.
- [x] 6.2 Strengthen the ported "equal inputs produce an identical plan" case, which as written compares two derivations to each other and is therefore blind to any uniform change of preimage or serialization: it must also assert the token equals the pinned value for those exact inputs.
- [x] 6.3 Make each anchor walk back through the value's own reader or verifier (plan token through plan revalidation, registry digest through index verification, carrier digest through the snapshot reader), so a preimage change cannot hide behind a verifier that stopped checking. Prove each anchor discriminates by mutating the preimage once and recording RED, then reverting. All seven anchors were mutation-proved via a real source-code RED/GREEN cycle (mutate `.digest('hex')` -> `.digest('base64')` in production, confirm the pinned-literal test fails, revert via exact `Edit`, confirm it passes again, confirm zero residue via `git diff`).
- [x] 6.4 For each of the four lock kinds, name its taker in shipped code. Record `change` and `integration` as deliberately unenforced-by-design in this child because their taker is the deferred finalization slice. Do NOT treat an assertion that the order array is well-formed as evidence that the ordering is enforced.
- [x] 6.5 Port the test surface: `test/core/store/{target-lines, workspace-apply, workspace-atomic-write, workspace-baseline, workspace-binding, workspace-cleanup, workspace-git-verb-guard, workspace-identity, workspace-locks, workspace-pairing, workspace-plan, workspace-windows-paths}.test.ts`, `test/commands/{workspace-cli, store-target-line-cli, store-v2-workspace-concurrency, store-v2-workspace-journey}.test.ts`, and `test/helpers/store-workspace-fixture.ts`. Also check `test/core/workspace-migration.test.ts` and report whether it belongs to this child. `test/commands/context-workspace.test.ts` is NOT ported — it belongs to the carved-out session projection (design Decision 2). VERDICT (review round 1, Trivial 4): `test/core/workspace-migration.test.ts` does NOT belong to this child. It exercises the pre-existing, unrelated `src/core/workspace-migration.ts` capability (backed by `rasen/specs/workspace-migration/spec.md`), present and unmodified on both the `dev/0.1.7` reference line and this line, untouched by this change. Correctly excluded from the port list above.
- [x] 6.6 Any ported case that requires the deferred finalization slice (the journey suite's archive-eligibility assertions are the likely ones) must be handled explicitly: defer that CASE with a recorded reason and author finalization-free substitute coverage for the behavior it protected. Do not silently drop it, and do not pull `finalization/` into this child to satisfy it. Two cases in `store-v2-workspace-journey.test.ts` are `it.skip` with a `DEFERRED (task 6.6)` comment naming the finalization dependency and the reason.
- [x] 6.7 Record that `test/core/store/workspace-atomic-write.test.ts` runs and passes, and state in the evidence that its production caller arrives with the finalization slice (design Decision 3) — so the situation is visible to a reviewer rather than discovered by one. 54/54 passed standalone.
- [x] 6.8 Run the focused suites from 6.5 and record the exact command and counts. Then run the compatibility suites this change touches: `test/core/file-state*.test.ts`, `test/commands/store.test.ts`, `test/core/store/` as a whole, and the completion/locale suites.
- [x] 6.9 Windows CI verification: the path-identity, alias, case, and long-path fixtures and the atomic-write durability cases must run on the Windows leg of the CI matrix, not only on Linux and macOS. Record the run reference. Measure whether the new suites need `KNOWN_SLOW_TEST_WEIGHTS_MS` entries in `vitest.config.ts` — unlike the previous child's pure unit suites, these use real Git worktrees and real filesystem fixtures, so the size-derived fallback will likely skew the shards. The matrix-configuration half is DONE: three weight entries added to `vitest.config.ts` from measured solo-run wall-clock ms (`workspace-cleanup.test.ts` 166610, `workspace-cli.test.ts` 166960, `workspace-apply.test.ts` 109103), confirmed additive-only via `git diff`. The run-reference half is left `[ ]` deliberately, not as an oversight: CI only executes on a pull request, and this portfolio opens exactly one PR — at the parent, after S1/S2/S3 are all terminal — so a Windows CI run reference is structurally unobtainable from inside this child's local worktree (precedented by child 1's own task 6.5). This is an inbound acceptance item for portfolio delivery, to be satisfied by the parent PR's CI run, not a defect to chase here. **RESOLVED at portfolio delivery (2026-08-15).** Parent PR #157 merged to dev/0.2.0 at 5e94c3a6; the Windows-leg run reference is the fully-green post-merge CI run on dev/0.2.0 at af259a81: https://github.com/DumoeDss/rasen/actions/runs/31873496727 -- all three windows-pwsh shards success, carrying this child's real-git-worktree suites (workspace-*) on the Windows leg, with green Linux and macOS legs in the same run. PR #157's own run had one red shard, the session-cache admission-fence environment flake fixed by PR #158, which touched none of this child's suites.
- [x] 6.10 Run the portfolio no-regression baseline and compare: `pnpm exec vitest run test/core/store test/core/change-run test/commands/store.test.ts test/commands/store-root-selection.test.ts test/cli-e2e/store-lifecycle.test.ts`. The post-S1 reference is 110 files / 1511 passed / 1 skipped. Report the new counts explicitly; any previously-passing test that now fails is a blocker. Left `[ ]` deliberately: the LEAD takes this specific gate personally, alone, on a quiesced tree — the same discipline that caught this child's own real-git-worktree Windows contention applies to the LEAD's own measurements, so a second concurrent run of this exact command (mine) would either duplicate the wait or contend with theirs. An implementer-side run was started and then killed unlanded on the LEAD's explicit instruction, rather than left to finish; no numbers from it are reported here. The LEAD records the result and reconciles this tick. (**RECONCILED — the LEAD ran it and the result was recorded, but the tick was never carried back here.** Result, verbatim from `.rasen/changes/store-v2-foundation/ephemera/portfolio-run.json` -> `s2GateResult.mainBaseline`: **GREEN, 122 test files / 1744 passed / 6 skipped / 0 failed, 1107s**, against the post-S1 reference of 110 / 1511 / 1 skipped / 0 failed -- **+12 files, +233 tests, no regression**, and no previously-passing test failing. Run at `VITEST_MAX_WORKERS=2` rather than at default parallelism: per `gateMethodologyFinding` the default is no longer trustworthy on this machine, so the reduced-parallelism run is the stricter reading of this task's intent, not a looser one. The frozen command above does not reach this child's four command suites; those were run separately and are recorded in the same object under `s2CommandSuites` -- 4 files / 26 passed / 2 skipped / 0 failed, GREEN -- with the 7 deferred cases reconciling exactly (5 in `workspace-baseline.test.ts` + 2 in `store-v2-workspace-journey.test.ts`, the remaining 1 skip pre-existing in `project-records.test.ts`). Both halves therefore covered. Noted because the session-2 handoff listed this task alongside 6.9 as blocked on the portfolio PR's CI run reference: **it was not** -- unlike 6.9 it never depended on CI, its substance was complete, and only the tick was outstanding.)
- [x] 6.11 Prove the carve-out held: assert `RUNTIME_CONTEXT_VERSION` is still 1, that `src/core/session-runtime-context.ts` and `src/commands/context.ts` are unmodified by this change, and that the daemon and session suites (`test/core/management-api/`, `test/core/session-runtime-context*.test.ts`) are green. `RUNTIME_CONTEXT_VERSION` confirmed 1 and both files confirmed byte-unmodified via `git diff --stat` (empty). Suite re-run to completion, solo: 54 files / 658 passed / 1 skipped / 0 failed, 0 regressions.
- [x] 6.12 Run `pnpm exec tsc --noEmit`, `pnpm run lint`, `pnpm run build`, and `rasen validate 'store-worktree-bindings-v2' --type change --strict`, and record each result. All four re-run fresh (exit 0 each) after every mutation-proof edit and revert, and again as the last action before reporting done: `tsc --noEmit` clean, `eslint src/ test/ vitest.config.ts vitest.setup.ts` clean, `pnpm run build` (incl. the Rust ProcessCapsule helper) succeeded, and `node bin/rasen.js validate 'store-worktree-bindings-v2' --type change --strict` reports "Change 'store-worktree-bindings-v2' is valid".
- [x] 6.13 Audit the child diff: confirm no write outside the two planned worktree roots, no staging/commit/fetch/push in any code path, no `store-planning/` or `layout-migration/` file added, `membership-layout.ts` untouched, no new branded type introduced (design Decision 5), and no unrelated formatting change.
