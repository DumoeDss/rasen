Repository checkboxes track acceptance-owned implementation and local
verification only. Finding boxes mean `FIXED_PENDING_REVIEW`, never
independent confirmation. Confirmed/CLEAN dispositions live only in the
canonical non-author review report and the dedicated review task.

Review Cycle 1 remains immutable 3/3 escalated history. Strategy Cycle 2 Round
1 independently confirmed F1, the lock-free UUID attempt layout, disjoint
create-once writers, and all preserved prior behavior. Final Strategy Cycle 2
Round 2/2 has exactly three open contracts: two Blockers and one Major. No
further review cycle is authorized if Round 2 is not CLEAN.

The old failed physical attempt
`7af1e71b6656d280453effa365a4deb7a6f53e0a268477eafec9b4644e039455`,
the legacy `acceptance-run.json`, and every pre-freeze candidate remain
immutable history. They complete no task or external gate.

## 1. Installed Acceptance Baseline

- [x] 1.1 Install the acceptance-owned protocol, physical harness, external evidence paths, and three named observation-arm entrypoints without modifying product-owner files.
- [x] 1.2 Install the integrated lifecycle, contention, retirement, recovery, daemon-off, and pipeline-binding fixtures as the acceptance-owned baseline.
- [x] 1.3 Replace the architecture document's reserved session-execution-layer statements with the delivered admitted-action, reusable-owner, recovery, and placeholder-value boundary.
- [x] 1.4 Preserve default-deny parent authorization and pending CI state without performing a push, PR, workflow dispatch, or remote mutation.

## 2. Independently Confirmed Findings

- [x] 2.3 **F3 confirmed — transcript/wake causality:** the exact transcript append directly binds touch text, assistant usage/message identity, result/session/timestamps, and transcript result digest to the durable wake.
- [x] 2.4 **F4 confirmed — arm-specific owner validation:** controls revalidate the live admission binding; scheduler terminal evidence preserves preterminal owner history and accepts only the production-valid absent-owner projection with exact cause/action/time.
- [x] 2.5 **F5 confirmed — deliverable candidate:** the exact audited tracked delivery index/tree and explicit manifest exclude every incidental untracked file and package lock.
- [x] 2.6 **F6 confirmed — production supersede:** controlled parent retirement/absence probing and fail-closed archive/history behavior remain required.
- [x] 2.7 **F7 confirmed — current CI state:** failure/supersession invalidates stale success and final acceptance cross-checks current state.
- [x] 2.8 **F8 confirmed — real GitHub REST provenance:** workflow/jobs bind exact repository, run, attempt, SHA, API URL, and HTML URL paths.
- [x] 2.9 **F9 confirmed — full freeze/delivery identity:** repository, original baseline, manifest, fingerprints, and delivered tree remain bound.
- [x] 2.10 **F10 confirmed — live capacity:** launch and arm admission use fresh production capacity inspection.
- [x] 3.1 **F11 confirmed — complete ownership audit:** baseline-derived owned and delivery sets exact-match actual state and manifests.
- [x] 3.2 **F12 confirmed — safe owner route:** every nonzero or malformed envelope becomes a bounded owned product gap without raw output.
- [x] 3.3 **F13 confirmed — completed resume no-op:** a valid completed arm is bounded-validated and returned without observer mutation.
- [x] 3.4 **F14 confirmed — production auto-decompose:** production resolution fails closed before host dispatch.
- [x] 3.5 **F15 confirmed — real transcript durability:** host-loss coverage uses the actual transcript and production bounded probe.
- [x] 3.6 **F16 confirmed — production daemon absence:** production owner selection covers absent, ambiguous, hanging, and live cases.
- [x] 3.7 **F17 confirmed — canonical local evidence:** typed gate claims and all five canonical output/exit pairs are revalidated.
- [x] 3.8 **Scheduler resume confirmed:** checkpoint resume preserves the original transcript range/prefix hash and `capturedAt`.
- [x] 3.9 **Task consistency resolved:** finding boxes mean implementer completion only and CLEAN remains independent.

## 4. Historical Local Evidence

- [x] 4.1 Preserve Round 1 serial local verification as historical implementation evidence only.
- [x] 4.2 Preserve Round 2 five-pair local evidence as reviewed historical implementation evidence only.

## 5. Strategy Cycle 2 — Final Round 2 Contracts

- [x] 5.1 **F1 confirmed — immutable tolerance authority:** result, checkpoint, and final semantics bind directly to `OBSERVATION_ARMS` constants: 5-minute cadence and 10-minute deadline application. Joint result/checkpoint tamper is rejected and the complete ordered timeline remains enforced.
- [x] 5.2 **F2 partially confirmed — immutable layout and writer ownership:** acceptance locks/reclaim and observer canonical-ledger writes are removed; UUID attempts, create-once intent/result/checkpoint/event/summary files, and disjoint arm subtrees are independently confirmed. Preserve this behavior.
- [x] 5.3 **Final Blocker — genuine multiprocess product-admission proof (`FIXED_PENDING_REVIEW`):** replace every test-private slot, dispatch gate, fake dispatch log, and protocol-only process with two real `launch-physical`/observe clients using the actual CLI/HTTP path into an actual management server/router, reusable-session service, durable registry, and coordinator. The existing fake Claude executable fixture is permitted only at the agent-binary boundary. Hold the winner at a deterministic real product admission/dispatch barrier; prove the durable lease/reservation/dispatch fence permits exactly one create/dispatch, the loser receives the typed busy/inconclusive route before Claude dispatch, and both processes' actual registry session plus wake-ledger state agree. Assert no second supervisor/Claude create and no test-private filesystem arbitration.
- [x] 5.4 **Final Major — scheduler reuse is explicitly prohibited (`FIXED_PENDING_REVIEW`):** retain bounded validate-and-copy/reference reuse only for `control-hit-55m` and `control-miss-65m`. Reject `scheduler-cadence-deadline` reuse with a stable typed code before writing any result/reference. Every scheduler result selected by finalization must be native to the target attempt and accompanied by that attempt's bounded-valid immutable scheduler checkpoint chain; no result without checkpoint may finalize. Add scheduler-reuse rejection, missing/wrong-attempt/tampered checkpoint negatives, and one valid fresh-scheduler finalization case.
- [x] 5.5 **Final Blocker — versioned v2 record and sole finalizer authority (`FIXED_PENDING_REVIEW`):** preserve the legacy `acceptance-run.json` bytes in place as bounded immutable v1 history and make them unable to block v2. Define `acceptance-run-v2.json` as the only canonical selected-attempt record for the immutable-attempt protocol. Remove `seedAcceptanceRun` and every alternate/preselection writer; observers and freeze preparation never write v2. The controlled E1 finalizer alone may create `acceptance-run-v2.json` after bounded-validating one attempt summary and its three arm results/checkpoints. All v2 final/CI/delivery readers use only the versioned path, reject an incompatible or different existing v2 record, and may treat an exact existing v2 record only as idempotent validation. Test with the actual preserved legacy ledger shape present.

## 6. Strategy Cycle 2 Final Closure and Freeze

- [x] 6.1 Preserve Strategy Cycle 2 round-0 serial evidence (4 files / 56 tests and five typed output/exit pairs) as reviewed historical implementer evidence; it does not close the three Round 1 findings.
- [x] 6.2 **Final local evidence (`FIXED_PENDING_REVIEW`):** after Tasks 5.3-5.5 were implemented, run only their invalidated serial focused gates, retain five final canonical output/exit pairs, and update typed local evidence without claiming physical, delivery, native CI, or remote success.
- [x] 6.3 Obtain the final independent non-author Strategy Cycle 2 Round 2 delta re-review over the three open findings, preserved F1/layout/F3-F17 behavior, actual legacy ledger shape, real product-admission integration, and exact changed set. The canonical external `review-cycle-report.md` records `FINAL_STRATEGY_REVIEW_CLEAN` with 0 Blocker, 0 Major, 0 Minor, and 0 Trivial.
- [x] 6.4 After CLEAN, complete task/spec consistency, delta-spec sync, child local ship/archive, and every remaining parent-selected repository mutation. The child shipped locally at `8f0d37664688f535608e3c96aa53259ae64c4949`; Rasen synced 7 requirements / 38 scenarios into the main specification and moved the child to the in-repo archive.
- [ ] 6.5 **Parent freeze pending:** through the controlled parent finalize/freeze entrypoint, audit the final index/manifest, reject forbidden and untracked paths, freeze the exact tracked tree, write a candidate-freeze record without fabricating physical evidence, and make all later progress external-only. During E1, the same controlled entrypoint is the sole creator or exact-idempotent validator of `acceptance-run-v2.json`.

## External Terminal Gates — Not Repository Checkboxes

| Gate | Starts only after | External terminal condition |
|---|---|---|
| E1 Final physical evidence | Tasks 5.1-5.5 and 6.1-6.5 complete, Strategy Cycle 2 Round 2 review is CLEAN, and the exact final tracked tree is frozen | The controlled finalizer creates or exact-idempotently validates `acceptance-run-v2.json` for one immutable attempt containing three validated arm results/checkpoints for the same candidate; scheduler timeline/constants, causal chain, terminal-owner contract, and both cache boundaries pass |
| E2 Parent authorization and single delivery | E1 successful and explicit parent authorization exists | Controlled delivery recomputes the frozen tree, performs the one parent commit/push or PR, and proves the delivered SHA tree equals it |
| E3 Exact-SHA native CI | E2 successful | One exact repository/SHA/run/attempt contains both Linux jobs and all three Windows PowerShell shards successfully |
| E4 Final acceptance | E1-E3 successful | Versioned v2 canonical record, selected immutable attempt, local evidence, physical candidate, frozen identity, delivered tree/SHA, workflow, and jobs cross-bind with no open product gap or finding; legacy `acceptance-run.json` remains immutable history |

Any new repository mutation creates a new candidate and immutable attempt.
Old attempts, checkpoints/results, and the v1 ledger remain immutable failed,
inconclusive, unselected, or diagnostic history.
