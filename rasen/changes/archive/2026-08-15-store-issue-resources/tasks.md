## 1. Layout Contract Debt from Child 1

- [x] 1.1 Add `IssueId` and `ExecutionPlanRevisionId` brands, their parsers/predicates, `formatExecutionPlanRevisionId`, `EXECUTION_PLAN_REVISION_WIDTH`, and the `invalid_issue_record` / `invalid_execution_plan` error codes to `src/core/store/planning-validation.ts` — the exact block child 1 deliberately left to this change.
- [x] 1.2 Add the four Store-level Issue addresses (`issue`, `issue-record`, `execution-plans`, `execution-plan`) to `src/core/store/planning-layout-v2.ts`. Each is its own address; none takes a project or target line, and supplying either must not change the result.
- [x] 1.3 Extend `test/core/store/planning-layout-v2.test.ts` and `planning-validation-v2.test.ts` for the new surface, including Windows and POSIX flavors built with the matching platform path API, case-alias rejection, traversal and device-name rejection, and the unpadded / differently-padded / zero revision ordinal cases.
- [x] 1.4 Assert the two new brands are picked up by child 1's brand-vocabulary guard in `planning-foundation-consumer.test.ts` — the guard reads `planning-validation.ts`, so its counted vocabulary must move. If it does not move, the brands landed in a file the guard does not read; fix that rather than the assertion.

## 2. Issue Resources

- [x] 2.1 Add `src/core/store/issues/**` (11 files) at the **squash-base** state per design Context — exclude `migration-compiler.ts`, the `locks.ts` batch additions (`withIssueLockBatch`, `issueLockCanonicalBytes`, `heldIssueLockKeys`, the `onAcquired` seam), and the `reference-verification.ts` extraction. Reference verification stays inline in `module.ts`, which is the 520-line squash version.
- [x] 2.2 Implement `IssueRecordV1` and its states, and the three-method mutation surface `StoreIssues.{create,setState,publishPlan}` — refusing a duplicate identifier and an undefined state rather than overwriting or storing them.
- [x] 2.3 Implement `ExecutionPlanRevisionV1`: immutable revisions addressed by canonical zero-padded ordinal, never rewritten, with no gap or duplicate in the sequence, and `executionPlanDigest` over `executionPlanDigestBody`.
- [x] 2.4 Implement plan-node normalization and the graph checker: two spellings of one plan normalize to one plan, duplicates are refused rather than merged, and dangling dependencies and cycles are refused with the offending nodes named. (**Second tick in this file that was carrying a false clause, same shape as 2.5 below — recorded rather than quietly completed.** "Two spellings of one plan normalize to one plan" was true for equivalent value spellings and FALSE for node ordering: `normalizePlanNodes` was a pure `.map()` preserving both node array order and each node's `dependsOn` order, so `[a,b]` and `[b,a]` were two plans with two digests. The duplicate/dangling/cycle clauses held throughout. Found by the round-1 reviewer as MAJOR-1; **no existing test would have gone red either way**, which is why it survived every gate. Both reviews independently measured the fix as costing zero moved pinned literals — and, crucially, zero verification too, so the operator ruled to implement the sort **with** a new test rather than reword the scenario. Closed in `9ece8b5d`: sort sited in `normalizePlanNodes` only (never in `executionPlanDigestBody`/serialize/read, which would reorder already-published revisions at read time and break `verifyDigest` round-trip), ordered by code points rather than `localeCompare` so the digest cannot vary with a machine's ICU data. New suite `test/core/store/store-issue-plan-canonicalization.test.ts`, 10 tests, two pinned golden digests each derived from its own input. LEAD-verified independently, not taken on report: baseline 13/13 GREEN across it and the anchors; removing the node sort gives **RED 3/10** naming exactly the three node-order cases while the 7.1 digest anchors stay GREEN — which proves both that the guard discriminates and that the anchors genuinely bypass the normalizer. `plans.ts` restored byte-exact, sha256 `A96B2B7184E4E3E2` before and after.)
- [x] 2.5 Implement reference verification against committed Store evidence — a node naming an absent, uncommitted, or out-of-Store Change is refused with the reason named, and the working directory is never consulted as evidence. (**This task was ticked while one third of it was false, and was completed only by the round-1 fix — recorded here rather than left to look like it always held.** `absent` and `out-of-Store` were refused from the start; `uncommitted` was NOT. `query/references.ts` returned `status:'resolved'` with `evidence:null` whenever no committed evidence existed but exactly one machine-local workspace-index entry matched, and `issues/module.ts` then did `if (found === null) continue;` — publishing the node. The path carried **zero** test coverage anywhere in the tree (`localLocator` and `evidence: null` each returned zero hits across `test/`), which is why every gate stayed green over it. Found by the round-1 reviewer as BLOCKER-1; the operator ruled to enforce the spec rather than reword it. Fixed in `10b74431` with a new `issue_reference_uncommitted` refusal and `test/core/store/store-issue-uncommitted-reference.test.ts`, the first coverage this behaviour has ever had. No new task was added for the fix: this is the task that already scoped the behaviour, so amending its record is the honest shape -- a new unchecked task would block archive for work that is done, and a new checked one would inflate the ratio for work the checklist already counted.)
- [x] 2.6 Implement the single Issue lock so all Issue mutation serializes through it, released even on failure, and a provably-dead owner does not permanently block a later operation.

## 3. Aggregate Query

- [x] 3.1 Add `src/core/store/query/**` (7 files) in the SAME change as `issues/**` — the bidirectional import cycle (`issues/module.ts` → `query/refs.js`, `query/issues-read.ts` → `issues/{records,plans,types}.js`) makes them one unit.
- [x] 3.2 Implement `StoreAggregateQuery.{listIssues,showIssue,issuesReferencing,resolveExecutionPlan,listProjects,listTargetLines,listChanges}`, with Changes grouped by project AND target line so one alias in two projects is two entries.
- [x] 3.3 Make every read lock-free and mutation-free: a read must complete while a mutation holds the Issue lock, and a full pass of every read operation must leave the Store byte-identical.
- [x] 3.4 Implement report-don't-refuse: one malformed Issue, project catalog, or Change yields a result carrying every readable item plus a named problem for the unreadable one — never a failed whole read, and never a silent omission.
- [x] 3.5 Make reads digest-aware: a resolved plan carries its ordinal and verified digest, and a revision whose stored digest does not match its content is reported unverifiable rather than returned as valid. (Implemented in `plans.ts`/`issues-read.ts`. Independently verified by task 7.4's new test, `'reports a revision whose stored digest no longer matches its content as unverifiable, never as valid (tasks 3.5, 7.4)'` in `store-aggregate-query.test.ts`: publishes and commits a real plan revision, corrupts the committed body while leaving the recorded `contentSha256` line untouched, and asserts `resolveExecutionPlan` reports `revision: null` with a diagnostic naming the mismatch rather than handing back the tampered plan. 1/1 GREEN solo; reconfirmed in the full-file run. See `evidence/digest-anchor-mutation-proof.md`.)

## 4. Command Surface

- [x] 4.1 Add `rasen store issue` (`new`, `list`, `show`, `state`, `plan`) in `src/commands/store-issue.ts`, with a machine-readable form whose content matches the human form. (Reference CLI tests use `state`/`plan`, not `set-state`/`publish-plan`, and a separate `store changes`/`store projects` pair rather than a `store aggregate` group — implemented per the reference tests, which are authoritative; see task 4.2.)
- [x] 4.2 Add `rasen store changes` / `rasen store projects` in `src/commands/store-aggregate.ts` with `--project` / `--target-line` filters, on the same terms. (Deliberately two top-level `store` siblings, not a `store aggregate` group — see the doc comment in the file.)
- [x] 4.3 Register both groups in `src/commands/store.ts`, `src/core/completions/command-registry.ts`, and all three locale trees (`en.json`, `ja.json`, `zh-cn.json`) in one step. `src/cli/commander-presentation.ts` structurally cross-checks the live Commander tree against `command-registry.ts` at CLI startup and hard-crashes (`CliPresentationError: commander-structure-mismatch`) on any subcommand/flag mismatch, then separately requires English copy for every registered flag/command (`missing-english-copy`) — confirmed by direct reproduction of both failures before fixing them. Verified locale lockstep by key set (all three files edited with parallel key structure, each `ja`/`zh-cn` description a genuine translation, not copied from English) and confirmed via `node -e "JSON.parse(...)"` on all three files plus a clean `pnpm run build` + `node bin/rasen.js store issue --help` / `store changes --help` / `store projects --help` smoke check.

## 5. API and UI Rim (additive only)

- [x] 5.1 Add the Store aggregate read paths to `src/core/management-api/stores.ts` and the router, **beside** what this line built — no existing endpoint changes shape. Each path is a read: no mutation, no lock. (`stores.ts`+`router.ts` committed `b7568d6b`; `test/core/management-api/stores.test.ts` 19/19 GREEN solo, `VITEST_MAX_WORKERS=1`, 180.62s tests / 199.98s wall. Found and fixed a real gap while writing the "reads never mutate" and parity tests: `resolveStoreSpace` resolves via `resolveStoreBinding`'s alias/uid path, which runs a stricter root-health check (`inspectRegisteredStore` -> `inspectOpenSpecRoot`, requiring `rasen/config.yaml` directly at the Store's own registered root — the file real `rasen store setup` always writes, `operations.ts:826`) than the CLI-side `resolveRegisteredStore` path the already-GREEN `store-issue-cli.test.ts` uses. `test/helpers/store-workspace-fixture.ts` (child 2's shared fixture) never writes that file, since none of its other 16 consumers exercise the alias path against it. Fixed locally in `stores.test.ts`'s own `beforeEach` (writes + commits the missing file), not in the shared fixture, to avoid regression risk to already-landed suites.)
- [x] 5.2 Add the new wire types and move `packages/ui/src/api/types.ts` in the SAME step. Add a test that fails when a Store-aggregate wire type exists without its UI mirror — a wire type added without its mirror is a known silent-drift failure mode in this repo, and inspection has not caught it before. (`wire-types.ts` adds a "Store aggregate" section: each `GET` response is a direct type ALIAS to its `StoreAggregateQuery`/`StoreIssues` domain type — never a hand-authored envelope, since `stores.ts`/`router.ts` send `result.response` unwrapped — plus 4 hand-authored request-body interfaces for the 3 `POST` endpoints, whose fields are the optional loosely-typed shape `stores.ts` itself validates at runtime, not the full domain input type. `packages/ui/src/api/types.ts` mirrors all 13 with plain redeclared interfaces (no import path exists into this package; confirmed no `pnpm-workspace.yaml` anywhere in the repo), following the file's existing "Source of truth: ..." mirror convention. New guard `test/core/management-api/store-aggregate-wire-mirror.test.ts` parses `export (type|interface) <Name>` declarations as source text from both files and asserts every named Store-aggregate wire type has a same-named UI export — deliberately independent of the existing `satisfies`-fixture drift tripwire, which only proves an EXISTING mirror type is shape-compatible and says nothing about an omitted mirror. Mutation-proven: renamed `StoreExecutionPlanPublishRequest` -> `StoreExecutionPlanPublishRequestMUTATED` in the real UI mirror file (snapshotted out-of-repo first, sha256 `82d47a97afce03a5983c4896659377498751624ca51fdafa001b18a62c3e7638`) -> RED, exactly 1/13 failed naming that type -> reverted byte-exact from the snapshot (sha256 re-verified equal) -> GREEN 13/13 again. `pnpm exec tsc --noEmit` and `pnpm exec eslint` both clean on the touched files after the revert. `packages/ui` needed its own `pnpm install` in this worktree (empty `node_modules`, standalone package, no workspace link) before its own `tsc --noEmit` would run at all; once installed it surfaced 8 pre-existing errors in 3 files this change never touches (`ConsultationBindingEditor.tsx`, `IssuesDrawer.tsx`, `v2-node-panel-consultation.test.tsx`), confirmed via `git log` to predate this branch — baseline breakage, out of scope, not fixed. **Round-1 correction:** the guard's hardcoded list named only 12 of the section's 13 types (`StoreExecutionPlanNodeInput` was missing) and both of its assertions iterated that list, so nothing could notice a type the core file had and the list did not — round 1's reviewer renamed that type's UI mirror and the guard stayed 13/13 GREEN. Repaired by adding the name AND a completeness direction that parses the core file's own "Store aggregate" section and asserts the list carries everything it exports; both directions mutation-proven at 1/15 RED each, restored byte-exact. Guard is now 15/15. See `evidence/wire-type-mirror-absence-mutation-proof.md`.)
- [x] 5.3 Enforce the complete-scope rule on the Store-scoped mutation surface: an incomplete scope is refused, and a sole candidate is not adopted as the missing part. Assert the sole-candidate case specifically — it is the one an inference would pass. (`findIncompletePlanNodeScopes` + `handleStorePublishPlan`'s pre-check in `stores.ts`; covered by 3 pure-function tests plus 4 fixture-backed scenario tests in `stores.test.ts`, including the sole-candidate case with an explicit precondition assertion that the Store has exactly one project and one target line. All GREEN — see 5.1's run.)
- [x] 5.4 Add `packages/ui/src/components/{StoreIssuesView,StoreAggregateBoard}.tsx` and their suites. Run them with `pnpm -C packages/ui exec vitest run` and record a NON-ZERO test count — the root runner excludes `packages/ui` and will report "passed" having run nothing. (`StoreAggregateBoard.tsx` is the read-only board over `StoreAggregateQuery.listChanges`, rendering every group verbatim — same alias in two projects is two rows, an empty group renders `store-board-group-empty` rather than being dropped. `StoreIssuesView.tsx` is the Store-level Issues list+detail+3-mutation-form view (create / setState / publishPlan); its publish-plan form's per-node project/target-line `<select>`s always start on an empty placeholder value and are never defaulted from the fetched catalog, and Submit stays `disabled` until every draft node's scope AND content fields are non-empty — the sole-candidate case is asserted directly in the suite against 1-project/1-target-line fixtures. 10 new `client.ts` functions + `StoreChangesFilter` added on the existing `request<T>()`/`spaceQuery()` conventions; ~38 new i18n keys added to `en.json`/`ja.json`/`zh-cn.json` with genuine per-locale translations (catalog parity guard requires identical key sets across all three — confirmed, 38/38/38). New fixture file `test/fixtures/store-aggregate.ts` follows the `satisfies <ResponseType>` convention (no `as` casts) — the same `tsc` drift tripwire every other fixture in the directory uses. Two new suites, `test/components/{store-aggregate-board,store-issues-view}.test.tsx`, follow the `archive-page.test.tsx` harness pattern (`vi.mock('../../src/api/client.js', ...)` + `LocationProvider`/`pushState` + `act`+microtask-flush). `pnpm -C packages/ui exec vitest run test/components/store-aggregate-board.test.tsx test/components/store-issues-view.test.tsx`: **11/11 GREEN** (4 board + 7 issues-view). `pnpm -C packages/ui exec tsc --noEmit`: identical 8 pre-existing baseline errors in the 3 untouched files noted in task 5.2's evidence, zero new errors. Deviation, flagged not silently resolved: the two components are NOT wired into `app.tsx`/`Layout.tsx`/`use-space.ts`'s `SWITCHABLE_SECTIONS` — the task's literal scope is the two component files (+ their suites), and no nav-wiring instruction is given here, consistent with how task 5.1 added `stores.ts`/`router.ts` without CLI menu wiring; the components are reachable only by direct import today.)
- [x] 5.5 Decide the two MODIFIED requirements the reference change carried (`board-ui` member chip filter, `management-http-api` loopback/bearer security): read the LIVE 0.2.0 spec text for each, determine whether this change's surface genuinely alters it on THIS line, and if so author the MODIFIED delta from the 0.2.0 text — never from the reference's. If neither is altered, record that finding and add no delta. (Read both live requirements in full, `rasen/specs/board-ui/spec.md:177-199` "Store space board offers a member chip filter" and `rasen/specs/management-http-api/spec.md:6-8` "Loopback and bearer security across the CLI-backed mutation surface". **Neither is genuinely altered by this change's surface — no MODIFIED delta authored for either.** Reasoning: (1) The member-chip filter governs the EXISTING lifecycle-column Kanban `BoardPage`; this change's `StoreAggregateBoard`/`StoreIssuesView` are new, separate components not wired into that page or its navigation (task 5.4's own recorded deviation) — they touch no chip-filter code path at all, so the requirement's normative text is untouched on this line. (2) The security-posture requirement's endpoint enumeration is deliberately narrow by established local convention: every prior addition to the management API since that requirement was written (workflow paths, the spaces path, audit paths, theme paths) added its OWN sibling requirement — "the X paths ... under the management server security posture" — rather than rewriting requirement 1's bulleted list; this change's ADDED requirement "The Store aggregate paths serve Issue, project, and grouped-change reads" (`specs/management-http-api/spec.md`) follows that same convention. Verified empirically, not just by textual pattern-matching: `src/core/management-api/router.ts:325-332` registers all 8 new `/api/v1/stores/*` paths into the SAME `MANAGEMENT_PATHS` set gated by the one uniform bearer-token check (`router.ts:884`) that every other management path — including the ones named in requirement 1's literal enumeration — is gated by; the new paths inherit loopback+bearer security automatically, with no spec text change required to make that true. `design.md`'s "3. The rim is additive only" section independently reaches the identical conclusion ("The ADDED requirements already carry all the new surface") before this task executed it — this task is the check that confirms that prediction against the live text rather than assuming it.)

## 6. Deferral and Substitute Coverage

- [x] 6.1 Do NOT port `test/core/store/store-issue-scope-intent.test.ts` (7 cases). It imports `test/helpers/store-finalization-fixture.ts`, which pulls `finalization/**` and `store-planning/**` — a later slice. Record the deferral with its reason. (Confirmed absent from this worktree's HEAD — never ported. Read the reference file in full at squash-base `0ede6cfb` (215 lines): it imports `StorePlanning` from `src/core/store-planning/index.js` (out of scope, a later slice) and drives `resolveIssueScope`/`assertIssueWriteLocation` only INDIRECTLY, through `StorePlanning.open({ intent: 'store-issue', startPath })` — never calling `issues/scope.ts` directly. Its fixture, `store-finalization-fixture.ts`, imports `ChangeFinalization` from `finalization/index.js` (also out of scope) on top of the already-ported, finalization-free `store-workspace-fixture.ts`. All 3 of its `describe` groups (store-only scope + no locate authority for `specs`/`active-changes`; opening an Issue scope grants no project authority; from a bound execution worktree, the intent still resolves to the Store root not the bound planning worktree) exercise `StorePlanning`'s intent-routing behaviour, not `scope.ts`'s own write-location refusal — genuinely a different layer's coverage, not a duplicate of this child's surface.)
- [x] 6.2 Determine which behaviours of `src/core/store/issues/scope.ts` (207 LOC) are covered ONLY by that file, and author finalization-free substitute coverage for each, driven through `StoreIssues` and the aggregate query directly. Mutation-prove each substitute like any other guard: break the behaviour, record RED, revert, record GREEN. (Repo-wide grep before writing anything found ZERO existing coverage anywhere in this worktree for `resolveIssueScope`'s worktree-root walk, `assertIssueWriteLocation`'s refusal throw, or `requireIssueScopeStore` — not even indirectly; `store-issue-layout.test.ts` covers a different function (`resolveStorePlanningLayoutV2Path`) and `store-issue-locks.test.ts` covers `locks.ts`, unrelated. Worse, re-reading the deferred 6.1 file in full confirmed it NEVER exercises `assertIssueWriteLocation`'s actual refusal branch either — none of its `StorePlanning.open()` calls expect a refusal, and `issue_write_requires_store_checkout` does not appear in any test file anywhere in the reference/upstream tree. This was genuinely uncovered behaviour, not a duplicate port. New file `test/core/store/store-issue-scope.test.ts` (finalization-free, no `StorePlanning`): drives `StoreIssuesModule` directly against `createStoreWorkspaceFixture` (child 2's real-Git fixture) plus a plain `git worktree add` — no binding/plan/apply flow needed, since `assertIssueWriteLocation` only checks marker-file EXISTENCE, and `listAllWorkspaceIndexEntries` naturally returns no match when nothing was ever registered, exercising the refusal's "no index entry" fallback message branch for free. 5 cases: (1) Issue write from the registered Store root; (2) a real second worktree resolved as checkout via the worktree-root walk (not the registeredRoot fallback) — asserts the write landed in the worktree and NOT at the registered root; (3) refusal in a worktree carrying a planted `.rasen/planning-line.json` marker, asserting the thrown `diagnostic.code === 'issue_write_requires_store_checkout'`, the "no index entry" message text, the worktree path (not the registered root) named in the message, and no partial write; (4)/(5) `requireIssueScopeStore` (imported directly from `scope.js`, since it is deliberately unexported from `issues/index.ts`'s barrel per the barrel's own doc comment reserving the write-location rule as internal — forward surface for the future `store-planning` intent resolver) refuses `undefined`/`''` and passes through a real id unchanged. `pnpm exec vitest run test/core/store/store-issue-scope.test.ts`: **5/5 GREEN** (~7.5s test time / ~18.8s wall). Mutation-proven, both guards, same file, sequentially: baseline `scope.ts` sha256 `c2a7fdff0ccc6284d423c6afd27485bf5c311edd360ce37387c5a15afdfde32f` snapshotted before mutating. Mutation A — `assertIssueWriteLocation`'s marker check flipped `!== 'file'` → `=== 'file'`: RED, **3/5 for mutation A alone** (the two no-marker cases and the marker case; both `requireIssueScopeStore` cases stay GREEN). The headline originally recorded here said 4/5, which was the count from the COMBINED run in which mutation B was also live — its own parenthetical said so; round 1's reviewer re-ran A alone and confirmed 3/5. The three failures are each exactly the expected inversion (the two "should succeed, no marker" cases now wrongly refuse naming `is a planning worktree bound to...`; the "should refuse, marker present" case now gets `caught === undefined` against `toMatchObject`). Mutation B — `requireIssueScopeStore`'s guard replaced with `if (false)`: RED, "refuses an undefined or empty store selector" fails with "expected [Function] to throw an error", the unrelated "returns the store id unchanged" case stays GREEN (proves the mutation is isolated to the intended branch, not a broader collapse). Both mutations reverted by exact restoration of the original two lines (not `git checkout --`, per the autocrlf-corruption rule) — sha256 re-verified equal to the baseline (`c2a7fdff...`) before rerunning. Rerun: **5/5 GREEN** again, tree byte-clean.)
- [x] 6.3 Record `test/core/store/store-issue-scope-intent.test.ts` as an inbound acceptance item for the finalization slice, alongside the item child 2 handed forward. (`proposal.md`'s "Deliberately not ported, with evidence" section gained a bullet naming the deferred file, why it is out of scope (routes through `StorePlanning`, fixture pulls `finalization/**`), that substitute coverage ships in this change, and that the file itself is handed forward as an inbound acceptance item for the finalization slice — placed explicitly alongside `membership-layout.ts`, the item `store-worktree-bindings-v2` (child 2) handed forward to the layout-migration slice for the structurally identical reason (a real production consumer graph belonging to a later roadmap slice). Portfolio-level bookkeeping was already correct independent of this task: `.rasen/changes/store-v2-foundation/ephemera/portfolio-run.json`'s `"S4-finalization"` key already carried this exact inbound-item note (planted at plan time, referencing `children[2].deferredTest`) beside `"S5-layout-migration"`'s note for child 2's item — that file is LEAD-owned accounting and was read, not written, here. `design.md` already carried the same record in its "Open Questions" section from propose time; this task's own delta is the `proposal.md` bullet, the surface a reader of the change's WHY/WHAT sees without opening `design.md`.)

## 7. Anti-Blindness Verification

- [x] 7.1 Add pinned-literal anchors for every durable-format site: `executionPlanDigest` (`plans.ts:312`), `serializeExecutionPlanRevision`, the Issue record serializer in `records.ts`, the Issue lock filename digest (`locks.ts:130`, over the `issue-lock/v1` domain preimage), and the read-side content digests (`query/issues-read.ts:29`, `query/refs.ts:447`). Each anchor pins its OWN literal inputs and expected value; no anchor may be computed from another's output. (Five sites, three test files. (1)-(3) new `test/core/store/store-issue-digest-anchors.test.ts`: `executionPlanDigest` pinned to a literal hex digest over a hand-built revision body; `serializeExecutionPlanRevision` pinned to a literal YAML string whose `contentSha256` line is a literal too (never a call to `executionPlanDigest`), so it stays a discriminator for the serializer's own rendering independent of the digest test; `serializeIssueRecord` pinned to a literal YAML string. `pnpm exec vitest run test/core/store/store-issue-digest-anchors.test.ts`: 3/3 GREEN on first run, confirming the hand-derivation (RFC 8785 key sorting, `yaml` stringify's insertion order and scalar quoting) matched real output. (4) `issueLockFileName`, edited in place in `test/core/store/store-issue-locks.test.ts` right after the pre-existing "is deterministic for equal material" self-comparison (kept, since it is still a valid separate claim) — a new case pins the exact `issue-<64 hex>.lock` filename for a literal `{storeUid, issueId}` key. 16/16 GREEN. (5) `query/issues-read.ts:29`'s unexported `digestOf` is reachable only through `collectIssues`/`listIssues`, so its anchor extends the EXISTING divergence test in `store-aggregate-query.test.ts` ("reports a divergent Issue with every copy and picks no winner"): each ref's exact committed bytes are read independently via `f.git(..., ['show', '<ref>:rasen/issues/divergent/issue.yaml'])` and hashed with this test's own fresh `crypto.createHash('sha256')` call, then compared against `IssueDivergence.copies[].sha256` — never calling `digestOf` itself. `query/refs.ts:447`'s `CommittedChangeEvidence.digest` is architecturally different: confirmed via `types.ts` and a `refs.ts:520` read that it is a purely internal de-dup key, never surfaced in any public return type, so a pinned-literal-value anchor is the wrong shape for it. Its anchor is instead a BEHAVIORAL test, new case in the same file, "collapses one Change reachable from two refs, and un-collapses on a byte difference": force `release/0.2` to the same commit as `main` (byte-identical committed Change) and assert `listChanges` reports exactly ONE active entry; then append a comment line to the `release/0.2` copy only (touches no `identity` field `ChangeMetadataSchema` reads) and assert the SAME query now reports exactly TWO entries, one per `foundAtRef` — proving `digest` is a live discriminator in the de-dup key (`refs.ts:520`), not decoration, without ever reading `.digest` directly. `pnpm exec vitest run test/core/store/store-aggregate-query.test.ts`: 25/25 GREEN (183.31s wall, foreground, solo).)
- [x] 7.2 Where a live per-run fact (a timestamp inside the preimage) makes a fixed literal impossible, have the test independently reconstruct the preimage and rehash with its OWN hardcoded hash call — not by calling the production helper. (Anchor 5a (`query/issues-read.ts:29`'s `digestOf`) is exactly this case: its input is a committed blob whose bytes only exist once the test seeds and commits them, so no fixed literal is possible. The extended divergence test in `store-aggregate-query.test.ts` reads each copy's committed bytes independently via `f.git(...['show', ...])` and hashes them with the test's OWN fresh `crypto.createHash('sha256')` call — never by calling `digestOf` a second time. See `evidence/digest-anchor-mutation-proof.md`, Anchor 5a.)
- [x] 7.3 Prove each anchor discriminates, and state the mutation used. It must be a change to the **serializer or preimage itself** with inputs held fixed — reorder a field, change a separator, drop the domain tag. Perturbing an input field does NOT discriminate: it moves only one side and is the break an unstrengthened anchor already catches. An anchor whose expected value is produced by calling production's own serializer a second time is blind and must be rewritten, not re-proved. (All 5 anchors (6 mutations, since anchor 5 has read and write halves) cycled RED → revert (out-of-repo snapshot, sha256-verified byte-exact) → GREEN: (1) `executionPlanDigest` — dropped `nodes` from the digest body, 2/3 RED; (2) `serializeExecutionPlanRevision` — swapped YAML field order, 1/3 RED, independent of (1); (3) `serializeIssueRecord` — swapped YAML field order, 1/3 RED, independent of (1)/(2); (4) `issueLockFileName` — changed the `issue-lock/v1` domain tag, 1/16 RED, independent of the pre-existing symmetric self-comparison test it sits beside; (5a) `digestOf` — sha256→md5, 1/25 RED; (5b) `CommittedChangeEvidence.digest` (`refs.ts:447`) — real committed bytes → fixed placeholder, 1/25 RED, caught by a new behavioral collapse/un-collapse test since this value has no public surface a pinned literal could anchor. Full narrative, exact failure messages, and every sha256 in `evidence/digest-anchor-mutation-proof.md`.)
- [x] 7.4 Walk each anchored value back through its own reader — the plan digest through `validateExecutionPlanRevision(..., { verifyDigest: true })`, the read-side digests through the aggregate read — so a preimage change cannot hide behind a verifier that stopped checking. (Plan digest: confirmed by reading the call chain — `query/issues-read.ts:215`'s `readRevision` calls `parseExecutionPlanRevision(text, { verifyDigest: true })`, which calls the SAME `validateExecutionPlanRevision` the serializer calls (`plans.ts:397`); anchor 1's mutation (dropping `nodes` from the digest body) already broke BOTH the digest-pin test and the serializer test via that shared function, proving the verifier is live on both the write and parse paths. That left the read path itself — `StoreAggregateQuery.resolveExecutionPlan` — unexercised against a genuine digest mismatch, so a new test was added: `'reports a revision whose stored digest no longer matches its content as unverifiable, never as valid (tasks 3.5, 7.4)'` in `store-aggregate-query.test.ts`. It publishes and commits a real plan revision, then corrupts the COMMITTED body text while leaving the recorded `contentSha256` line untouched (an out-of-band hand-edit, not a call to any digest function), and asserts `resolveExecutionPlan` returns `revision: null` with a `diagnostic` naming the `contentSha256` mismatch and empty `readiness` — never a plan trusted at face value. `pnpm exec vitest run test/core/store/store-aggregate-query.test.ts -t unverifiable`: 1/1 GREEN solo (5.54s test time / 25.69s wall); full file reconfirmed together with every other anchor at the end of Section 7 (see 7.3's suite run). This also closes task 3.5, previously deferred here. Read-side digests: `digestOf`'s reader IS the aggregate read by construction — `collectIssues`/`listIssues` (the divergence test's own call path) — so anchor 5a already walks back through the aggregate read; `refs.ts:447`'s digest is likewise only ever observed through `listChanges` (anchor 5b), never read directly, so there is no separate "verifier" to walk back through for it — the aggregate read IS its only consumer.)
- [x] 7.5 For each kind in `STORE_LOCK_ORDER`, name its taker in shipped code on this branch. Record `change` and `integration` as unenforced-by-design (their taker is the finalization slice). Do NOT offer `assertStoreLockOrderAgreesWithWorkspace()` as evidence that ordering survived — it compares two frozen arrays and passes under any partial port. (Grepped every call site of all 5 key constructors across `src/` and `test/`. `issue`: `StoreIssuesModule.{create,setState,publishPlan}` (`issues/module.ts:100,147,209`). `scope`: `TargetLineModule`'s catalog-write methods (`target-lines.ts:334,398`), `WorkspaceModule.apply()` (`workspace/module.ts:167`), `WorkspaceModule.applyCleanup()` (`workspace/module.ts:350`). `workspace`: the same `apply()`/`applyCleanup()` call sites, always taken together with `scope` in one `withWorkspaceLocks([...])`, never alone. `change`/`integration`: zero call sites anywhere in `src/`; the only callers in the whole repo are `changeLockKey`/`integrationLockKey`'s own unit-test lines in `workspace-locks.test.ts:153-154` — confirmed unenforced-by-design, their taker is the not-yet-built finalization slice. Also recorded a nuance: `buildWorkspaceCleanupPlan` (the cleanup PREVIEW, `cleanup.ts:189-213`) peeks at `scope`/`workspace` via `lockIsHeld` to report a blocker precondition, but never acquires them — acquisition happens only in `applyCleanup`. Full table in `evidence/store-lock-order-takers.md`.)

## 8. Gates

- [x] 8.1 Port the test surface: `test/core/store/{store-aggregate-query, store-issue-layout, store-issue-locks, store-query-lock-free, store-query-read-only-guard}.test.ts` and `test/commands/{store-issue-cli, store-aggregate-cli}.test.ts`. Do NOT port `store-issue-migration-compiler.test.ts` (excluded with `f4a48a36`) or `store-issue-scope-intent.test.ts` (task 6.1). Port only the base portion of `store-issue-locks.test.ts` — its batch cases came with `f4a48a36`. All 7 files solo-verified GREEN at `VITEST_MAX_WORKERS=1` after the command-surface (section 4) edits: store-aggregate-query 24/24 (314.95s), store-issue-layout 45/45 (2.25s), store-issue-locks 15/15 (5.79s, base portion only), store-query-lock-free 5/5 (52.29s), store-query-read-only-guard 10/10 (8.75s), store-issue-cli 7/7 (76.79s), store-aggregate-cli 4/4 (62.30s).
- [x] 8.2 Re-derive the no-regression gate's file list from THIS change's own test-file additions and add them to the corrected list; then verify the run's reported file count against the change's test-file additions. A gate command frozen before this child existed will silently not run its suites — that is exactly what happened to child 2's four command suites, and a green gate then did not cover the child's own acceptance suites. (**Derivation half DONE; verification half open, which is why this stays `[ ]`.** Re-derived from `git diff --name-status 501b8943..HEAD -- test packages/ui/test`: this change adds 11 root-runner test files and 3 `packages/ui` files, and modifies 4. **Four of its own acceptance suites sat outside the prior list** -- `store-issue-cli`, `store-aggregate-cli`, `management-api/stores`, `management-api/store-aggregate-wire-mirror` -- reproducing child 2's failure at the same size, and for a cause that will repeat again: the list names `test/commands/*` entries as individual files, and omitted `test/core/management-api/` entirely. The corrected gate is **three commands, not one**: (1) the root run, with `test/core/management-api` added as a DIRECTORY so the next child's additions are absorbed without anyone remembering to, while `test/commands/` stays file-named because promoting it would pull in the 229s/123s suites and make the gate a near-full-suite run; (2) `pnpm run test:types`, because the root `include` is `test/**/*.test.ts` and does not match the `planning-foundation-consumer.test-d.ts` this change modified (S1's branded-vocabulary guard, extended 16 -> 18 names) and typecheck is off by default -- a LOCAL-gate gap only, since `ci.yml:280` does run it; (3) `pnpm -C packages/ui exec vitest run`, per task 5.4. Full derivation table and rationale in `evidence/task-8-2-rederived-gate.md`, which also records two findings beyond this task's scope: this change's two UI component suites are **not** in `ci.yml:156`'s hardcoded four-file list and so never run on the portfolio's PR, and `packages/ui typecheck` IS invoked -- by `release.yml:90`, not `ci.yml` -- where it currently fails with 11 pre-existing errors. **VERIFICATION HALF NOW CLOSED, so this task is ticked.** The expected file count was written down BEFORE the run, not derived after seeing it: 50 (`test/core/store`) + 76 (`test/core/change-run`) + 54 (`test/core/management-api`) = 180 directory suites, plus the 9 individually-named files, none of which fall inside those three directories = **189**. The run reported **189**. Exact match, so no suite was silently absent -- the specific failure this task exists to catch, and which had already happened twice in this portfolio. Full record in `evidence/task-8-3-gate-run-and-environment.md`.)
- [x] 8.3 Run the gate at `VITEST_MAX_WORKERS=2` for a trustworthy number, and never triage a full-run failure by its shape — re-run the file solo before concluding anything. A busy filesystem on this machine produces genuine assertion failures, not just timeouts; only a solo re-run separates contention from defect. Do not accept "every file passes solo" as the gate either. (**RUN TAKEN, RED, TRIAGED -- and deliberately still `[ ]`, because this task's own text forbids the shortcut that would close it.** Run on the MERGED base at `VITEST_MAX_WORKERS=2`: 8 files failed / 181 passed (189 files), 27 tests failed / 2613 passed / 9 skipped, 2825s, plus 2 unhandled `[vitest-worker]: Timeout calling "onTaskUpdate"`. All 8 failing files were re-run solo, one at a time: **7 passed clean**. A hypothesis worth recording because it was FALSIFIED -- four failures sat in `management-api/`, exactly where the ten newly-merged commits landed, so "the merge broke the session surface" was the obvious reading; all four pass solo. The 8th, `workspace-cli.test.ts`, failed its first solo re-run too and was initially recorded as a real defect; taking the actual error text rather than stopping at "solo also fails" corrected that. Three runs gave three DIFFERENT failure sets (3 / 4 / 1, no overlap in names), never an assertion -- only `Test timed out in 30000ms` and `EPERM` from `fs.rmSync` in the fixture cleanup. Cause measured, not inferred: `%TEMP%` held **3983** leftover `rasen-*` directories, many full Git repos, and `rasen --version` startup ranged **2223-29470ms on the same binary** while every test in that suite runs within 2x of the 30s timeout. Cleared 3430 of them (older than 1h; 23 kept for concurrent sessions; 0 failures) and re-measured: startup spread collapsed from 13x to **1.1x** (2319-2558ms). **No regression was identified.** But "no regression identified" is not "the suite passes", and only the latter closes this task -- the gate is being re-taken against the repaired environment, and 8.3 turns on THAT result. **CLOSED — GREEN, at `VITEST_MAX_WORKERS=1` in three partitions.** `MAX_WORKERS=2` was abandoned on evidence, not preference: three runs at that setting gave 19 distinct failing files across 21 occurrences, only 2 repeating, **zero in all three**, so it cannot produce a stable result on this machine at this file count. Serial removes the contention; partitioning (`VITEST_FILE_PARTITION`, the same mechanism CI shards with) bounds each run, after one 2-worker run was cut off at 118/189 with no summary block at all. Result: **191 files / 191 passed · 2663 passed / 9 skipped / 0 failed**, in 914s + 622s + 528s. The suites that failed in every earlier attempt — `workspace-cli`, `workspace-cleanup`, `workspace-identity`, `bootstrap-obtain`, `sessions-api`, `store-aggregate-query` — passed first time, none rescued by a solo re-run. **That is the stronger claim this task's own text demands** and forbids substituting a per-file sweep for: the suite passes, not merely every file passes alone. The count is +2 against the 189 predicted before the first run, and the delta reconciles EXACTLY rather than being waved off — `store-issue-uncommitted-reference.test.ts` (+3, BLOCKER-1's first-ever coverage) and `store-issue-plan-canonicalization.test.ts` (+10, MAJOR-1's guard), plus `stores.test.ts` 19→25, `store-issue-cli` 7→9, `store-aggregate-wire-mirror` 13→15 = +23 tests against 2649. Every added file and test is named. `pnpm run build` was run first: `test/helpers/run-cli.ts:166` `ensureCliBuilt()` only builds when `dist/cli/index.js` is ABSENT and never rebuilds on source change, so a CLI suite run without it reports on a stale binary. **RE-TAKEN after the verify round and still GREEN: 191 files / 2668 passed / 9 skipped / 0 failed.** The first GREEN was invalidated by us on purpose — verify found two Blockers (`evidence/verify-report.md`), the operator ruled to change the code, and the fix touched 12 files including `query/` internals, so the earlier measurement described a tree that no longer existed. The delta reconciles exactly again: **+0 files, +5 tests**, all five of the fix round's new tests landing in `store-aggregate-query.test.ts` (26→31), so 2672+5=2677=2668+9. Recorded rather than smoothed: per-partition file counts moved (67/74/50 → 73/55/63) because `VITEST_FILE_PARTITION` re-derives its weighted split over the whole suite whenever any file's weight changes, so only the total is comparable across runs; and total wall-clock rose 40% (2064s→2886s) for five more tests, which the added work does not explain — most likely `%TEMP%` residue re-accumulating, worth re-checking before trusting any future measurement, though it does not invalidate this one since every partition produced a summary block and nothing failed. Full record in `evidence/task-8-3-gate-run-and-environment.md`.)
- [x] 8.4 Record the intended divergences so a reviewer does not read them as drift: `git diff origin/dev/0.1.7 -- src/core/store/issues/` will be NON-empty by design (the `f4a48a36` exclusion, chiefly `module.ts` at its 520-line squash size), and `src/core/store/query/` should differ only by `references.ts` +5. State both in the evidence. (`evidence/intended-divergence-from-017-tip.md`. `issues/`: 6 files diverge, all attributable to the `f4a48a36` exclusion — `index.ts`/`locks.ts`/`migration-compiler.ts`/`module.ts`/`reference-verification.ts` — except `records.ts`'s 2-line diff, confirmed cosmetic (same C0-control-plus-DEL character class, two equivalent regex escape notations). `query/`: **two** files diverge, not the one design.md's Context predicted — `references.ts` (+5, exactly as predicted, and genuinely behavioral: the tip reports `ambiguous` whenever more than one committed candidate exists, this branch additionally requires the candidates' scopes to differ; recorded as a known, inherited gap from the exclusion, architecturally distinct from anchor 5b's `refs.ts` de-dup logic and not independently covered by this child's own reference-verification tests) and `module.ts` (4 lines, confirmed cosmetic — same single-character null-byte bucket-key separator, two equivalent JS string-escape spellings on each side of the join/split pair) — design.md's prediction corrected in the evidence rather than silently treated as met.)
- [x] 8.5 Measure each new suite's solo wall-clock and add `KNOWN_SLOW_TEST_WEIGHTS_MS` entries in `vitest.config.ts` for any of comparable weight to child 2's, or the macOS and Windows CI shards will skew. Note that child 2's `workspace-cleanup` entry (166610) measured 396.85s solo and looks more than 2x underestimated — do not copy its methodology, measure. (All 11 new root-runner suites now carry a measured solo wall-clock. **Six warrant entries and have them:** `management-api/stores.test.ts` 199980, `store-aggregate-query.test.ts` 314950, `store-issue-cli.test.ts` 76790, `store-aggregate-cli.test.ts` 62300, `store-query-lock-free.test.ts` 52290, and `store-issue-scope.test.ts` 16150 (added here; measured twice at 16150ms and 15960ms, stable). **Five need none**, each measured solo and each below the table's established 13318ms floor: store-issue-layout 2250ms, store-issue-locks 5790ms, store-query-read-only-guard 8750ms, store-issue-digest-anchors 1350ms, store-aggregate-wire-mirror 950ms. Two methodology findings, recorded rather than silently applied. (1) `store-issue-scope` is the SMALLEST absolute cost among this change's entries but the WORST relative skew in the whole table: 5949 bytes implies a 595ms byte-size fallback against ~16000ms measured, a **27x underestimate**, because three of its five cases each build a real second Git worktree. Absolute cheapness is not the criterion for an entry; fallback error is, and judging by absolute cost is how this file would have missed it. (2) `store-aggregate-query` was measured TWICE on this machine, **1.8x apart** -- 314950ms recorded under task 8.1, 177060ms under this task's earlier partial pass -- and the entry has been RAISED from the lower observation to the higher one. That applies to this change's own table the exact principle this task's text applies to child 2's `workspace-cleanup`: under-entering skews a whole shard, over-entering only costs balance. The 1.8x spread is itself the finding -- a single solo measurement on this machine is a point estimate with roughly 2x variance, so no weight here should be treated as precise.)
- [x] 8.6 Windows CI verification: the layout Issue-address fixtures, the identifier case/alias/device-name cases, and the CLI suites must run on the Windows leg of the CI matrix, not only on Linux and macOS. Record the run reference. (No GitHub Actions Windows-matrix run exists to reference: this child ships **local only** per the task-lead brief — no push, no PR — and the CI matrix runs on a pushed branch, which is portfolio task #8 (single PR at parent, LEAD-owned), not this task. What IS true and directly verifiable: this entire implementation was solo-run on a genuinely Windows machine throughout (`win32`, Windows 11 Pro, confirmed by every `git bash`/PowerShell invocation this child used) — not emulated, not WSL. `planning-layout-v2.test.ts`/`planning-validation-v2.test.ts` (task 1.3, extended with the Windows-flavored path-API build, case-alias rejection, traversal and device-name rejection cases) ran solo GREEN on this machine (task 1.3's own evidence). `store-issue-cli.test.ts`/`store-aggregate-cli.test.ts` (the CLI suites, `runCLI` subprocess per case spawning `node bin/rasen.js`) likewise ran solo GREEN on this machine (7/7 and 4/4, task 8.1). So the SUBSTANCE this task cares about — that these fixtures/cases behave correctly under Windows path semantics — is satisfied by direct local execution; only the distinct claim "ran on the Windows leg of the GitHub Actions matrix specifically" is not yet true, and cannot be made true before the branch is pushed. Recorded here rather than silently marked done: the CI-matrix run reference is an open item for whoever executes portfolio task #8.) **Portfolio follow-up recorded (2026-08-15).** The CI-matrix reference deferred above now exists: parent PR #157 merged to dev/0.2.0 at 5e94c3a6, and the fully-green post-merge matrix run on dev/0.2.0 at af259a81 -- https://github.com/DumoeDss/rasen/actions/runs/31873496727 -- includes all three windows-pwsh shards plus windows-node-floor success, so the CLI suites and layout fixtures ran on the Windows leg of the GitHub Actions matrix, with green Linux and macOS legs. (PR #157's own run was green on every shard except the unrelated session-cache admission-fence flake fixed by PR #158.)
- [x] 8.7 Run `pnpm exec tsc --noEmit`, `pnpm run lint`, `pnpm run build`, and `node bin/rasen.js validate 'store-issue-resources' --type change --strict`, and record each result. Use `node bin/rasen.js` for every rasen invocation — the bare `rasen` command on this machine is a 0.1.7 build. (**All four GREEN**, and re-run on a clean tree AFTER this session's edits to `vitest.config.ts` (task 8.5) and to this file -- an earlier identical pass taken BEFORE those edits was discarded rather than reported, because a gate measured before the change it is meant to cover is precisely the defect task 8.2 documents one line above. `pnpm exec tsc --noEmit`: no output, exit 0. `pnpm run lint` (`eslint src/ test/ vitest.config.ts vitest.setup.ts`): no output, exit 0. `pnpm run build` (`node build.js`): completed successfully, TypeScript 5.9.3 plus the source-owned ProcessCapsule win32-x64 helper. `node bin/rasen.js validate 'store-issue-resources' --type change --strict`: `Change 'store-issue-resources' is valid`. `git status --short` empty after building, so `dist/` and the native helper produce no tracked churn. One aside, not a defect of this change and not acted on: the two builds run this session emitted DIFFERENT ProcessCapsule digests -- `b952c616...` then `4bf2c356...` -- from byte-identical Rust source, since nothing this session edited lives under `native/`. That is a direct local confirmation of the repo's known Windows build non-reproducibility, distinct in cause from the Linux one already fixed.)
- [x] 8.8 Audit the child diff: confirm no `finalization/`, `store-planning/`, `layout-migration/`, `layout-write-guard.ts`, `membership-layout.ts`, or `consistency-gates.ts` file is added; that `migration-compiler.ts` is absent; that no existing management-API endpoint, wire type, or locale key changed shape; and that no unrelated formatting change is present. (Audited `git diff 501b8943..HEAD` — child 2's ship commit to this child's HEAD, 76 files / 13019 insertions(+) / 10 deletions(-), all 19 commits authored by this implementer plus two LEAD-owned portfolio/handoff docs interleaved in the shared branch (`rasen/changes/store-v2-foundation/planning-context.md`, `rasen/work/.../handoff/lead-2.md`) that this task's audit does not extend to. `git diff --name-only ... | grep -iE "finalization/|store-planning/|layout-migration/|layout-write-guard\.ts|membership-layout\.ts|consistency-gates\.ts|migration-compiler\.ts"`: zero matches. `src/` diffstat is 28 files / 6048 insertions(+) / **0 deletions** — every touched file (`stores.ts`, `router.ts`, `wire-types.ts`, all three `src/locales/*.json`, `planning-validation.ts`, `planning-layout-v2.ts`, `command-registry.ts`, `store.ts`) is pure addition, confirming no existing endpoint, wire type, or locale key changed shape. `packages/ui/src/i18n/locales/*.json` each show `40 +-`; read in full: the lone `-` in each is the previous last key's trailing line losing its "no comma" terminal as new keys append after it — a JSON structural artifact of appending, not a value change to any existing key. No file outside `.ts`/`.tsx`/`.json`/`.md`/`.test.ts`/`.test-d.ts` appears in the diff, and no unrelated formatting change was found in any file's diff while reading each hunk above.)
