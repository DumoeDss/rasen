## 1. Deterministic test gaps — pipeline show display (scope A)

- [x] 1.1 In `test/commands/pipeline.test.ts`, in the `describe('show', ...)` block (around line 49), add a test that runs `openspec pipeline show goal-loop-measure` **without** `--json` (human-readable output, matching the existing "prints a human-readable table without --json" pattern at line 41) and asserts `stdout` contains `loop=goal[measure](max 5, stall 2)` on the `iterate` stage line. Add a parallel case for `goal-loop-evaluate` asserting `loop=goal[evaluate](max 5, stall 2)`.
- [x] 1.2 Add a regression case: `openspec pipeline show small-feature` (human-readable) still renders `loop=review-cycle(max 3)` for its review-loop stage (the goal-loop generalization must not have changed the review-cycle label).

## 2. Deterministic test gaps — run-state round-trip variants (scope A)

- [x] 2.1 In `test/core/pipeline-registry/run-state.test.ts`, in the `loopConfig (goal-loop measure gate)` describe block (around line 394), add a `loopProgress` round-trip case: build a `RunState` with a `loopProgress` block (`{ kind: 'goal', round: 2, lastScore: 78, measurePassed: false, stallStreak: 1, historyRef: 'goal-run.json' }`), `writeRunState` + `readRunState`, assert every field round-trips; add a parse-only case for an evaluate `loopProgress` (`evaluateSatisfied` instead of `measurePassed`); add a backward-compat case asserting a run-state without `loopProgress` parses unchanged.
- [x] 2.2 Add an evaluate-gate `loopConfig` round-trip case: `loopConfig.gate.kind === 'evaluate'` with `goal` + `rubric` + `workProduct: 'prose'`, write + read, assert goal/rubric preserved and the gate narrows to the evaluate variant. (goal-loop-core only round-trip-tested the measure variant.)
- [x] 2.3 Add a `direction: lte` measure case: a `loopConfig` measure gate with `direction: 'lte'` (smaller-is-better, e.g. latency), parse + round-trip, assert `direction === 'lte'`. goal-loop-core covered `gte` only.
- [x] 2.4 Add a `target` (passed-count) stop-condition case: a measure gate with `target: 10` (instead of or alongside `threshold`), parse + round-trip, assert `target` preserved. goal-loop-core covered `threshold` only.

## 3. Deterministic test gaps — per-pipeline tail structure (scope A)

- [x] 3.1 In `test/core/pipeline-registry/builtins.test.ts`, in the `for (const name of GOAL_LOOP_NAMES)` describe block (around line 88), add a case that asserts the tail divergence: for `goal-loop-measure` and `goal-loop-evaluate`, the final two stages SHALL be `ship` then `archive`, each with `model: 'sonnet'`; for `goal-loop-research`, the final stage SHALL be `report` (skill `openspec-goal-report`) with NO `ship`/`archive` stage. Use `loadPipelineByName(name)` and inspect `pipeline.stages`.
- [x] 3.2 Add a case asserting `goal-loop-research` sets a lowered implementer handoff threshold: `resolveStageHandoffConfig` for its `iterate` stage SHALL resolve `threshold` to `0.35` (source `role`). Import `resolveStageHandoffConfig` from the types module (already imported in `pipeline.test.ts`).

## 4. Verification — no regression, no duplication (scope A)

- [x] 4.1 Run `pnpm test`. Confirm all new cases pass AND the existing goal-loop-core cases (schema parse/narrow, builtins list/parse/validate, `loopConfig` measure `timeoutSec`, skill-generation registration) and the three-built-in-pipeline / review-cycle assertions stay green. If a new case fails, the failure is a real goal-loop-core defect — fix it in-place (localized to the display string in `src/commands/pipeline.ts` or the run-state field in `src/core/pipeline-registry/run-state.ts`), call it out in the ship log, and re-run. Do NOT redesign the mechanism.
- [x] 4.2 Confirm no duplication: the new cases do NOT re-test schema parse/narrow (pipeline.test.ts lines 77–206), builtins list/parse/validate (builtins.test.ts lines 81–137), `loopConfig` measure `timeoutSec` (run-state.test.ts lines 394–459), or skill-generation registration (skill-generation.test.ts lines 50–53, 146).

## 5. Docs chapter — goal-loop in the workflow guide (scope B.1)

- [x] 5.1 In `docs/opsx-workflow-guide.md`, append a new top-level goal-loop chapter (e.g. a new `##` section after the existing §3 per-stage commands, numbered to follow the guide). Match the existing §2/§3 style (prose + tables + worked examples + bash snippets). Cover: (a) the single `/opsx:goal` entry and when to use it vs `/opsx:auto`; (b) LEAD classification keywords (score/latency/optimize/...→measure; rubric/quality/...→evaluate; research/investigate/...→research) and the explicit override (`measure|evaluate|research` selector token or `--pipeline goal-loop-<variant>`); (c) the three backend pipelines and their tails (measure/evaluate → ship → archive; research → report); (d) the define-goal → iterate → tail flow, including the `gate: true` on define-goal that confirms a measure command; (e) `goal-run.json` as the authoritative round record + `loopProgress` cache; (f) the resume model (satisfied → tail; not-passed → lastRound+1; no-record → round 1); (g) a worked example each for measure (e.g. "drive Lighthouse to 90"), evaluate (e.g. "make this module rubric-clean"), and research (e.g. "research and write a brief on X").
- [x] 5.2 Verify the existing §1 overview, §2 autopilot, and §3 per-stage-commands content is unchanged (diff is purely additive — one new chapter appended). Add the three goal-loop pipelines to the §2.2 built-in pipelines table with a forward-reference to the new chapter.

## 6. Office-hours design doc — converge to v4 (scope B.2)

- [x] 6.1 Rewrite `openspec/office-hours/goal-loop-primitive.md` from the v3 single-pipeline draft to the converged v4 design that shipped. Keep the office-hours narrative structure (Problem Statement, What Makes This Cool, Constraints, Premises, Approaches Considered, Recommended Approach, technical design) but update the technical content: (a) ONE user-facing entry `/opsx:goal` + LEAD-classified family of THREE homogeneous backend pipelines (one gate type each), NOT one combined-gate pipeline; (b) the three defects that killed v3 (AND-semantics stall hole, unenforced conditional-tail exclusivity, hand-waved generic prose skill) and how the family dissolves them; (c) implementer-inline research + H.3 relay, NOT a research-sibling subagent (flat-hierarchy invariant); (d) the gate-neutral field name `loopStallLimit` (not `measureStallLimit`, not `stallLimit`); (e) `goal-run.json` as the authoritative loop spine + `loopConfig`/`loopProgress` run-state additions; (f) the discriminated-union schema (StageLoopSchema + gate union). Update the status header from "v3" to "v4 (converged — matches shipped design; see planning-context.md)". Preserve the bilingual voice if present.

## 7. E2e validation runbook (scope C)

- [x] 7.1 Write `openspec/changes/goal-loop-validation/goal-loop-e2e-runbook.md` — a dated, step-by-step runbook for validating the prose-driven goal-loop behavior end-to-end (the behavior vitest cannot reach). It SHALL cover: (a) a sample measure task — a throwaway script (e.g. a tiny Node/shell script that edits a counter file and emits `{score, passed}` JSON) plus a `goal-plan.md` pointing the measure gate at it with `threshold`/`maxRounds`; (b) `/opsx:goal measure <task>` — observe the define-goal gate confirm the command, then observe rounds append records to `goal-run.json` (`{round, score, measurePassed, detail, gitTreeFingerprint}`); (c) maxRounds exhaustion — set `maxRounds: 2` and a threshold the script never meets, observe the outcome marked `maxRounds-exhausted` and NOT reported as success; (d) kill + resume — interrupt after round 1, run `openspec pipeline resume <change>`, confirm the three resume branches (last record satisfied → tail; last record not-passed → lastRound+1; no record → round 1); (e) a note that this is manual because the loop semantics live in the LEAD playbook (Step L prose), not executable code. Reference concrete artifacts by name (`goal-run.json`, `auto-run.json`, `openspec pipeline resume`).

## 8. Verification (no regression)

- [x] 8.1 Run `pnpm test` once more after all test + docs work. Confirm zero regressions across the full suite (existing three-built-in pipelines, review-cycle loop, goal-loop-core cases, and the new scope-A cases). Investigate any Windows CLI-spawning flakiness per memory (EBUSY rmdir / 10s timeout on spec/artifact-workflow tests) by isolated re-run, not as a logic regression.
- [x] 8.2 Run `openspec validate goal-loop-validation` and confirm the change validates (every requirement has ≥1 scenario; spec deltas well-formed). Run `openspec pipeline show goal-loop-measure` (human-readable) and eyeball the `loop=goal[measure](...)` line as a final smoke check.
