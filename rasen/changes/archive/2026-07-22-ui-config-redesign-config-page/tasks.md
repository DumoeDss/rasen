> **Dependency note**: W2 is implemented in the MAIN tree and MUST start only after `ui-config-redesign-store-scope` (W1) is review-clean there — W2 consumes W1's landed API surface. Task 1.1 verifies that surface before anything else. No worktree; artifacts and implementation share this tree.

## 1. Consume W1's wire surface

- [x] 1.1 Re-read W1's landed `src/core/config-api/wire-types.ts` and router: confirm `ConfigScope`/`ConfigSource` carry `'store'`, `scopeValues` carries `store`, list/get responses carry `store: { id, root } | null`, config endpoints accept `?space=` (and reject `space`+`project` together), and store-scope writes are accepted only at store spaces. If anything diverges from W1's delta specs, stop and escalate to the LEAD before proceeding.
- [x] 1.2 Mirror the surface into `packages/ui/src/api/types.ts`: add `'store'` to the UI's `ConfigScope` and `ConfigSource`, `store?` to `scopeValues`, and the response-level store reference type on list/get/put/delete response shapes.
- [x] 1.3 Move the config client calls (`listConfig`, `getKey`, `putKey`, `deleteKey`) in `packages/ui/src/api/client.ts` onto the `?space=` selector built from the route's space (`project:<id>` / `store:<id>`), removing the config-specific `?project=` plumbing and its "config stays on ?project=" comment; writes/unsets send the caller-provided explicit scope in the body as before.

## 2. Page mode and tabs

- [x] 2.1 Rework `packages/ui/src/config/controls.ts`: replace `writableScopes`/`defaultWriteScope` with mode-aware helpers — `localScopeFor(spaceType)` (`project` | `store`), visibility (`isVisibleInMode(entry, mode, spaceType)`: Global = scopes include `global`; Local = scopes include the local scope), and writability (env-override and wildcard stay read-only; array stays read-only). Update `test/config/controls.test.ts` cases accordingly.
- [x] 2.2 Add the tab mapping beside `groupEntries` in `packages/ui/src/config/grouping.ts`: a declared constant `TAB_MAP` — General: Profile/Appearance/Behavior; Project: Project/Archive; Privacy: Telemetry; Advanced: Advanced; Workflow: Workflow/Autopilot — with a trailing bucket for unmapped groups. Pure function returning tabs → groups → entries filtered by the active mode; a tab with no visible entries is omitted. Unit-test the mapping, the empty-tab omission (Privacy in Local mode), and the unmapped-group fallback. Do not assert registry key counts.
- [x] 2.3 Rebuild `ConfigPage.tsx`: remove the store-space deferral stub (`:70-77`); fetch via the space selector for both space types; render the Global/Local segmented control (default Local, design D1) and the tab strip; keep the page-error banner behavior; keep `GatesInventoryPanel` injected in the Autopilot group section inside the Workflow tab, untouched.
- [x] 2.4 Add `packages/ui/src/config/labels.ts`: dot-path → human label constant covering all current registry keys (fallback: the dot-path itself). Keep labels short and neutral; reference projects never named.

## 3. Entry row

- [x] 3.1 `ConfigEntryRow.tsx`: delete the per-row Scope select (`:310-324`) and the `scope` state; the write/unset scope comes from the page mode via props. Title the row on the label with the dot-path as secondary text (`config-entry__key` demoted); keep description, source badge, warnings, and controls as-is.
- [x] 3.2 Implement the inherited-value line (design D3): in Local mode, a visible multi-scope key with no local value shows "Inherited from store `<id>`: `<value>`" (when `scopeValues.store` is set, id from the response store ref) or "Inherited from global: `<value>`" (or the built-in default) — reusing the `config-entry__shadowed` element inverted. Keep the existing shadowed reveal when a local value shadows wider ones (now including a shadowed store value).
- [x] 3.3 Store-inherited rows: when the store layer provides the effective value in Local mode, render the control read-only and add the "Edit in store `<id>` →" affordance navigating via `spaceHref({ type: 'store', id }, 'config')` (SPA navigation — hard navigation drops the token). No store affordance of any kind when the response store ref is null.
- [x] 3.4 Unset follows the mode: offer the single unset button only when the active mode's scope holds a value; remove the per-scope unset button row.

## 4. Tests and verification

- [x] 4.1 Update `packages/ui/test/fixtures/config-list.ts`: entries gain `store` scope values and the response store ref (both null and populated variants); add a store-space fixture. Keep fixtures Windows-safe (no hardcoded separators — build any path values with `path.join` where used in node-side tests).
- [x] 4.2 Update/extend `config-entry-row.test.tsx` and page-level tests (`app.test.tsx` or a config-page test): mode toggle re-targets writes (project vs global scope in request bodies; store scope at a store space) and re-filters keys without reload; tabs render per mapping; empty-tab omission; inherited-from-store line with id + read-only control + store link navigation; inherited-from-global line editable; no scope select anywhere; unset carries the mode's scope; store space renders entries (stub gone); no store affordance without a store ref.
- [x] 4.3 Build and test: `pnpm --filter @atelierai/rasen-ui build` + UI test suite, then the full repo suite from the root; isolate-rerun Windows CLI-spawn flakes (EBUSY/10s) before treating them as regressions. Confirm no root-package or lockfile changes (config-ui-package independence requirement).
- [~] 4.4 Manual smoke via chrome-use (curl needs `--noproxy '*'`; SPA-click navigation only): project space with a `store:` pointer shows the inherited line and the store-edit link switches space; store space edits its own values in Local mode; Global mode edits land in global config; visual style stays within the existing warm-editorial idioms in both themes.
  - PARTIAL: ran a live-server API smoke against the LOCAL W1 build (`node bin/rasen.js ui`, curl `--noproxy '*'`): `GET /api/v1/config?space=project:<id>` returns `{project, store, entries}` with `store: null` (no pointer on the launch project), `handoff.threshold` scopes `["global","store","project"]`; `space`+`project` together → 400 `bad_request`; a `store`-scope PUT at a project space → 400 `invalid_scope`. This confirms the W1↔W2 wire the UI mirror/client depend on. Full CDP browser walkthrough NOT run: (a) no store is registered on this machine (`rasen store list` empty), so the store-inheritance / edit-in-store paths have no live data — they are covered by the jsdom page+row tests instead; (b) the local build reports "UI package not installed", so the server serves the API but not the bundled UI for a pixel render. The store-inheritance behaviors are fully exercised by `config-page.test.tsx` + `config-entry-row.test.tsx`.
- [x] 4.5 From the repo root: `rasen validate ui-config-redesign-config-page --strict`.
