## 1. Core audit module (`src/core/token-audit/`) — Claude path

- [x] 1.1 Create `src/core/token-audit/classify.ts`: port pricing constants (`PRICING`, `HIT_PREFIX_RATIO`, `DROP_CTX_RATIO`, `BURST_GAP_MS`, `TTL_MIN`, `REQUEST_CLASSES`), `classify()`, and `clusterBursts()` from `scripts/token-audit/audit.mjs` verbatim (same math, TypeScript types added).
- [x] 1.2 Create `src/core/token-audit/parse.ts`: port the streaming per-file parser (`parseFile`'s message.id dedup, tool census, `between`-lines tracking) as `parseTranscriptFile()`, typed. Preserve the original script's tolerance byte-for-byte (`if (u && sum > 0)`, `git show 2e8639ee`): an assistant entry with no/non-object `message.usage` is silently skipped, not an error. `TranscriptFormatError` (task 3.1) throws ONLY when `message.usage` IS a present object but carries a non-numeric value for a token field the pricing math reads (post-review correction — see M2, `rasen/changes/agent-audit-command/work/review-report.md`).
- [x] 1.3 Create `src/core/token-audit/audit.ts`: port session-level orchestration (`discoverAgentFiles`, agent labeling, activation-order sort, totals/gapHistogram/byModel aggregation, result assembly) as `runAudit(target, options)`, reusing `claudeProjectsDir()` and the transcript-resolution helpers already exported from `src/core/agent-context.ts` instead of re-deriving the projects-dir slug. (Runtime dispatch to the Codex path is added in section 2.)
- [x] 1.4 Add `getGlobalDataDir()`-based default output resolution: `path.join(getGlobalDataDir(options), 'analytics', 'session-audit-<sid>.json')`, with an explicit `outPath` option overriding it.
- [x] 1.5 Unit tests (`test/core/token-audit/`) for dedup-by-message-id, dual TTL pricing (main vs. subagent), and each churn cause classification path (ttl-expiry, rebase, context-drop, unattributed), asserting against hand-computed expected values on small synthetic inputs.

## 2. Codex rollout support

- [x] 2.1 Relocate `agent-context.ts`'s private `readSessionMeta` to `src/core/codex/rollout.ts` as an exported `readRolloutSessionMeta` (same signature/behavior, no logic change), re-export it from `src/core/codex/index.ts`, and update `agent-context.ts` to import it instead of keeping its own copy — this is the shared session-meta reader both `agent context`'s `findLatestRollout` and the new audit's subagent discovery need.
- [x] 2.2 Create `src/core/token-audit/discover-codex.ts`: `discoverCodexThreadFamily(threadId, sessionsDir)` — BFS over `listRolloutFiles(sessionsDir)` candidates' `session_meta.parent_thread_id`, starting from `threadId`, returning every rollout in the family (main + all transitive subagents) with each rollout's `agent_nickname`/`agent_path`/`thread_source` for labeling.
- [x] 2.3 Create `src/core/token-audit/parse-codex.ts`: per-thread parser deriving one request record per **distinct** `token_count` event (skip re-emissions where `info.total_token_usage.total_tokens` is unchanged from the last recorded value; delta = current cumulative minus last-recorded cumulative, per field), plus turn segmentation from `task_started`/`task_complete` `event_msg` rows (`turn_id`, timestamp) — reusing row-iteration conventions from `src/core/codex/rollout.ts` rather than a new JSONL reader from scratch.
- [x] 2.4 Wire runtime selection into `src/core/token-audit/audit.ts`: `--runtime` explicit override wins; else detect via `detectTranscriptKind`/`CODEX_ROLLOUT_BASENAME` (reused from `src/core/agent-context.ts`) when given a direct path; a bare id with no `--runtime` resolves as Claude (unchanged default). `--runtime codex` with a bare id resolves via `findRolloutPath(threadId, { codexHome })`.
- [x] 2.5 Build the Codex report shape: `session.runtime: 'codex'`, per-agent/turn raw token totals (`inputTokens`, `cachedInputTokens`, `cacheWriteInputTokens`, `outputTokens`, `reasoningOutputTokens`, `totalTokens`), `cacheHitRatio`, `turns[]` — omit `pricing`/`churnEvents`/`resumes`/`billedInputEq` entirely for a Codex report (do not zero-fill them). Bump the schema constant to `rasen-token-audit/2`; a Claude report also gains `session.runtime: 'claude'` but is otherwise unchanged from `/1`'s shape.
- [x] 2.6 Unit tests (`test/core/token-audit/codex/`) for: cumulative-counter delta derivation (including the re-emission-skip case), subagent-family BFS discovery (including a transitively-nested subagent), and turn-boundary attribution — against hand-computed expected values on small synthetic rollouts.

## 3. Fail-soft error handling (both runtimes)

- [x] 3.1 Add `src/core/token-audit/errors.ts` with `TranscriptFormatError` (message, file path, line number, cause string) — the only error type the CLI layer treats as a friendly, non-crashing failure. Shared by the Claude parser (`parse.ts`) and the Codex parser (`parse-codex.ts`).
- [x] 3.2 Wire `parseTranscriptFile()`/`runAudit()` to throw `TranscriptFormatError` on shape violations that would corrupt accounting — for Claude, a PRESENT `message.usage` object carrying a non-numeric token field (an absent/non-object `message.usage` is a benign skip, matching the original script, NOT a throw — post-review correction, M2); for Codex, a missing `info.total_token_usage` on a `token_count` event, or a non-numeric token field within it — while continuing to skip a single unparseable JSON line as before on both runtimes (no behavior change there).
- [x] 3.3 Build `test/fixtures/token-audit/`: one minimal valid synthetic Claude session (one main + one subagent transcript, hand-crafted to exercise message-id dedup, both TTL tiers, and at least one churn event of each cause) plus a non-numeric-token-field variant (the one real throw trigger) and two skip-and-continue variants — `message.usage` entirely absent (`skip-missing-usage/`, proves the benign-skip path, not drift) and a truncated line at a load-bearing point.
- [x] 3.4 Build `test/fixtures/token-audit/codex/`: one minimal valid synthetic Codex rollout family (a main rollout with 2+ turns exercising the cumulative-delta rule, plus a subagent rollout, modeled on the shape confirmed in `test/fixtures/codex-rollout/sample-rollout.jsonl`) plus 2-3 deliberately malformed variants (missing `info.total_token_usage`, non-numeric token field, truncated line). No real session data or real local paths in any fixture, Claude or Codex.
- [x] 3.5 Regression test (`test/core/token-audit/format-drift.test.ts`) asserting, for both runtimes: the valid fixture produces the expected totals exactly; the non-numeric-field fixture throws `TranscriptFormatError` (not an unhandled exception) with an actionable message; the truncated-line and (Claude-only) missing-usage fixtures do NOT throw — they are skipped and the audit completes normally.

## 4. CLI command

- [x] 4.1 Add `AgentCommand.audit(options)` to `src/commands/agent.ts` (thin consumer of `runAudit()`, following the `context`/`wait` pattern): resolves target/`--projects-dir`/`--out`/`--runtime`, calls `runAudit()`, prints the resolved report path (and a one-line totals summary in text mode, shaped per runtime), and catches `TranscriptFormatError` specifically to print the friendly message and exit 1 (any other thrown error surfaces normally).
- [x] 4.2 Register `rasen agent audit <sessionId|path>` in `src/cli/index.ts` under the existing `agentCmd` group, with `--projects-dir <dir>`, `--out <path>`, `--runtime <claude|codex>`, `--json`, `--open` options and `--help` text stating the experimental/format-drift caveat (spec: Experimental status is disclosed).
- [x] 4.3 `--json` mode: on success, emit the full result object (`rasen-token-audit/2`, `session.runtime` set); on format-drift failure (`TranscriptFormatError` — the only type `AgentCommand.audit()` catches, per design.md D3), emit `{"available": false, "reason": "format-drift", "detail": ...}` mirroring `agent context`'s unavailable shape. Corrected post-review (m1, `rasen/changes/agent-audit-command/work/review-report.md`): an ambiguous-prefix error is a plain input error, not a probe-unavailable state — it surfaces normally to the CLI's generic catch and exits non-zero as prose, regardless of `--json`, matching `agent context`'s own established convention for input errors (invalid `--runtime`/`--limit`, missing/unreadable `--transcript`). No `{available:false}` shape for ambiguous-prefix was implemented or is required by any spec scenario.
- [x] 4.4 CLI e2e tests (`test/cli-e2e/agent-audit.test.ts`) covering: analyze-by-id-prefix (Claude) against a fixture projects dir, analyze-by-explicit-path (both runtimes), `--runtime codex` thread-id resolution and subagent-family inclusion, ambiguous-prefix error, `--out` override, and `--json` output shape on success and format-drift for both runtimes.

## 5. Viewer asset and `--open`

- [x] 5.1 Move `scripts/token-audit/viewer.html` to `viewer/audit.html` at the repo root (no content changes required beyond what's needed for `?src=` file-path loading and the runtime-aware render branch in 5.6, if anything).
- [x] 5.2 Add `"viewer"` to `package.json`'s `files` array.
- [x] 5.3 Add a package-relative asset resolver (mirroring the `createRequire(import.meta.url)` + relative-to-`package.json` pattern used in `src/commands/ui-launch.ts`) to locate the installed `viewer/audit.html` at runtime, independent of `process.cwd()`.
- [x] 5.4 Implement `--open`: after a successful audit, resolve the viewer's path and open it via the existing cross-platform `openInBrowser`-style helper (darwin `open`, win32 `cmd /c start`, else `xdg-open`; spawned detached, stdio ignored, unref'd, errors swallowed) with the generated report passed via a `?src=` file reference; always print the report path regardless of whether the browser opened.
- [ ] 5.5 Verify the viewer still loads correctly via plain drag-and-drop (no CLI involved) after the file move — no regression to the existing no-network, self-contained viewer behavior. **Remaining manual step** — the one item this change ships without, honestly surfaced rather than rubber-stamped: needs a real browser (drag a generated `session-audit-*.json` onto `viewer/audit.html`), which this environment cannot exercise. Everything else in the viewer (runtime-aware render branch, caveat banner, `?src=` file loading) is covered by reading the code and the CLI e2e tests that generate reports it would load.
- [x] 5.6 Add a runtime-aware render branch to the viewer: read `session.runtime` (or its absence, treated as `claude`), render Claude's pricing/churn/billed-equivalent views only for a Claude report, and render the raw-totals/cache-hit-ratio/turns view for a Codex report — no assumption that Claude-only fields exist.

## 6. `scripts/token-audit/audit.mjs` disposition

- [x] 6.1 Replace `scripts/token-audit/audit.mjs`'s body with a thin wrapper that delegates to `rasen agent audit` (same CLI args passthrough, including `--runtime`), per design.md D1.
- [x] 6.2 Update `scripts/token-audit/README.md`: add a deprecation notice pointing at `rasen agent audit`/`rasen-audit`, note the new Codex support, keep the existing "Measurement discipline" and provenance sections intact, leave `forensics/` untouched.

## 7. `/rasen-audit` skill

- [x] 7.1 Create `src/core/templates/workflows/audit.ts` exporting `getAuditSkillTemplate()` (name `rasen-audit`), with instructions covering: finding a session id when the user doesn't have one, recognizing a Codex session and routing to `--runtime codex`, running `rasen agent audit`, offering `--open`, interpreting the report's totals (runtime-appropriate vocabulary — churn-cause breakdown for Claude, raw totals/cache-hit-ratio for Codex), and relaying (not reinterpreting) command failures — plus the experimental-status disclosure.
- [x] 7.2 Re-export `getAuditSkillTemplate` from `src/core/templates/skill-templates.ts`.
- [x] 7.3 Register the workflow in `src/core/workflow-registry/builtins.ts`: add `'audit'` to `BUILT_IN_WORKFLOW_IDS` (NOT to `CORE_WORKFLOW_IDS` — full profile only, per spec) and add `{ id: 'audit', dirName: 'rasen-audit', skill: getAuditSkillTemplate }` to `BUILT_IN_ADAPTERS`.
- [x] 7.4 Skill template parity/generation tests: confirm `rasen init`/`update --profile full` generates `.claude/skills/rasen-audit/SKILL.md` and `--profile core` does not (following the existing `workflow-help-command` test pattern).

## 8. Cross-cutting verification

- [x] 8.1 Run the full test suite (`pnpm test`) including the new Claude and Codex fixture tests and CLI e2e tests; confirm no regression in existing `agent context`/`agent wait` tests (including the relocated `readRolloutSessionMeta`).
- [x] 8.2 Confirm every new file-path construction (`--out`, default `~/.rasen/analytics/` resolution, viewer asset resolution, Codex sessions-tree scan, fixture loading) uses `path.join`/`path.resolve` and is exercised (or reasoned about) for Windows path separators — no hardcoded `/`.
- [x] 8.3 `rasen validate agent-audit-command --json` passes with no errors.
- [x] 8.4 Manual smoke check: run `rasen agent audit <a real local Claude session id> --open` and, separately, `rasen agent audit <a real local Codex thread id> --runtime codex --open` once each against actual local transcripts and confirm both reports and the viewer's two render branches look sensible (not part of automated tests; a one-time author sanity check). **Done by the LEAD** against the built CLI: Claude session `0bf3df17` → 290 requests, churn attribution correct; Codex rollout `019f8bae` → runtime discriminant + `cacheHitRatio` + no `billedInputEq` correct. This run is what surfaced M1 (fork-replay) in review round 1 — see `rasen/changes/agent-audit-command/work/review-report.md` — which is now fixed (flag, not exclude) and covered by a synthetic fork fixture/tests. No real session ids, transcripts, or paths from this smoke check are recorded in the repo.
