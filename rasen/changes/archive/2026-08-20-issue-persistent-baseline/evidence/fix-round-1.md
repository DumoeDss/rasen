# Fix round 1 — issue-persistent-baseline (review findings M1, M2)

Round 1: PASS, 0 Blocker / 0 Major / 2 Minor (report: `review-report.md`).
Both are receipt-documentation sentences; no code, tests, or re-runs.

- **M1** — `evidence/5-1-durability.md`, "How future sessions address it": one
  sentence added recording the reviewer-measured out-of-worktree reading
  (`ready` / `0/3` / every node `not-started`, `runStateVisibility: none`) for
  legacy-record claimants until store records carry outcomes — designed g-002
  semantics, not a regression of the 4-2 capture.
- **M2** — `evidence/4-issue-loop.md`, staged 4.3/4.4 section: one warning
  line added — both close steps MUST run from the worktree BEFORE any
  worktree cleanup/reset (terminal observations depend on worktree-local,
  never-committed dated mirrors + undated sources, unrecoverable after
  cleanup); capture the 3/3 receipt at that moment.
