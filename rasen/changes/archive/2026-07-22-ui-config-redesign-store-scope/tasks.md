# Tasks — ui-config-redesign-store-scope

Read `design.md` first: D1–D8 name every decision these tasks implement. Never name reference projects in code, comments, or commit messages. Version stays untouched.

## 1. Registry and inheritance edge (foundation)

- [x] 1.1 In `src/core/config-keys.ts`: widen `ConfigScope` to `'global' | 'store' | 'project'` (line 19); update the `scopes` arrays — the 14 `['global','project']` entries (`autopilot.gates`, `autopilot.selection`, `handoff.threshold`, `handoff.roles.{planner,implementer,reviewer,fixer,shipper}`, `models.default`, `models.roles.{planner,implementer,reviewer,fixer,shipper}`) become `['global','store','project']`; the 3 `['project']` entries (`schema`, `archive.timing`, `archive.destination`) become `['store','project']`; the 8 global-only entries and the `featureFlags` wildcard stay `['global']`. Update the doc comment on `ConfigKeyDefinition.scopes`. No logic changes needed in `findConfigKeyDefinition`/`findWildcardDefinition`/`validateConfigKeyPath` (they already take a scope parameter).
- [x] 1.2 In `test/core/config-keys.test.ts`: extend the registry round-trip test so store-scoped entries validate against the project config schema (a store's config file is the same `rasen/config.yaml` shape, D4); assert the exact scope assignment counts (8 global-only, 3 store+project, 14 all-three) so a future key can't silently miss the store scope; assert `validateConfigKeyPath('handoff.threshold','store').valid === true` and `validateConfigKeyPath('profile','store').valid === false`.
- [x] 1.3 In `src/core/effective-config.ts`: add and export `resolveConfigStoreLayer(projectRoot: string | null | undefined): Promise<{ storeId: string; storeRoot: string } | null>` per design D1 — null when no root / no planning shape / no pointer / malformed pointer; null when `projectRoot` is itself a registered store's root (canonical comparison via `FileSystemUtils.canonicalizeExistingPath` against `listRegisteredStores()` entries' `storeRoot`, Windows-safe); otherwise the registered store's `{ storeId, storeRoot }` (canonical), null when the named store is unregistered. Export a `StoreConfigLayer` type alias for the non-null shape. Uses `classifyOpenSpecDir` from `project-config.js` and `listRegisteredStores` from `store/registry.js`.
- [x] 1.4 Add `test/core/effective-config.test.ts` cases for `resolveConfigStoreLayer`: registered pointer + planning shape → layer; pointer-only (no planning shape) → null; no pointer → null; unregistered store → null; malformed pointer → null; projectRoot IS a registered store root with its own `store:` field → null (no-transitivity, D1 rule 3). Build fixtures with real temp dirs + a store registry under a test `globalDataDir` if `listRegisteredStores` supports the path option (it takes `StorePathOptions`; thread an optional `pathOptions` parameter through `resolveConfigStoreLayer` for testability, defaulting to the real machine root).

## 2. Effective-config resolution with the store layer

- [x] 2.1 In `src/core/effective-config.ts`: add `'store'` to `ConfigSource`; widen `EffectiveConfigEntry.scopeValues` to `{ global?: unknown; store?: unknown; project?: unknown }`; add `store?: { storeId: string; storeRoot: string } | null` to `ResolveEffectiveConfigOptions`. In `resolveEffectiveConfig`, read the store layer via `readProjectConfig(store.storeRoot)` (once, outside the key loop) guarded per key by `definition.scopes.includes('store')`; merge order per key: env-override > project > store > global > default (design D2). `scopeValues.store` carries the raw store value.
- [x] 2.2 In `src/core/effective-config.ts`: give `resolveHandoffThresholdLayers` and `resolveModelConfigLayers` an optional second parameter `storeRoot?: string | null`; read the store config via `readProjectConfig(storeRoot)` (already resiliently validated, D2) and populate the new layer fields (task 4.1's types): `storeThreshold`/`storeRoles` and `storeRoles`/`storeDefault` respectively. Update both functions' doc comments to name the three-scope chain.
- [x] 2.3 Extend `test/core/effective-config.test.ts`: project > store > global precedence per key; store-only value resolves with source `'store'`; `scopeValues.store` populated; a store-root-only resolution (options `{ store }` with no `projectRoot`) reports store values with `scopeValues.project` absent; keys not store-scoped (e.g. `profile`) never read the store layer; `resolveHandoffThresholdLayers`/`resolveModelConfigLayers` with a `storeRoot` populate the store fields.

## 3. Policy resolvers and run-state

- [x] 3.1 In `src/core/project-config.ts`: add `'store'` to `ResolvedGatePolicy.source` and `ResolvedSelectionPolicy.source`; give `resolveAutopilotGatePolicy` an optional 4th parameter `storeConfig?: ProjectConfig | null` and `resolveAutopilotSelectionPolicy` an optional 5th parameter of the same type; check the store value between the project and global checks (same `'on'|'off'` / enum guards). Update both doc comments to the flag > project > store > global > default chain.
- [x] 3.2 In `src/core/pipeline-registry/run-state.ts`: add `'store'` to the recorded `gatePolicy.source` enum (line ~145); keep `'config'` legacy value. Extend the run-state test (in `test/core/pipeline-registry/`) with a `source: 'store'` round-trip and confirm an old run-state without it still parses.
- [x] 3.3 Extend `test/core/project-config.test.ts` gate/selection blocks: store beats global, project beats store, flag beats all, invalid store value falls through, absent storeConfig behaves exactly as before (existing tests must pass unmodified except where they enumerate the source union).

## 4. Pipeline-registry layer types and stage resolution

- [x] 4.1 In `src/core/pipeline-registry/types.ts`: add `storeRoles?`/`storeDefault?` to `ModelConfigLayers` and `'store-role' | 'store-default'` to `ModelSource`; slot them between the project and global branches of `resolveStageRuntimeConfig` (after `projectDefault`, before `globalRoles`). Add `storeThreshold?`/`storeRoles?` to `HandoffConfigLayers` and `'store-role' | 'store-config'` to `ResolvedStageHandoffConfig['source']`; slot them into `resolveStageHandoffConfig`'s threshold cascade and source ladder between `project-config` and `global-role` (role beats scalar within the store scope, mirroring the other scopes). Update both precedence doc comments.
- [x] 4.2 Extend the types tests (`test/core/pipeline-registry/`): store-role beats store-default; store layers sit below project and above global for both model and handoff; sources `store-role`/`store-default`/`store-config` reported; absent store fields change nothing.
- [x] 4.3 In `src/commands/pipeline.ts` (both call sites, ~lines 253–254 and 808–809): `const storeLayer = await resolveConfigStoreLayer(projectRoot);` then pass `storeLayer?.storeRoot` to `resolveHandoffThresholdLayers`/`resolveModelConfigLayers`. Both enclosing methods are already async.
- [x] 4.4 In `src/core/agent-context.ts`: make `resolveHandoffThresholdReport` async; after resolving `projectRoot`, await `resolveConfigStoreLayer(projectRoot)` and pass the store root into `resolveHandoffThresholdLayers`; insert the store check between project and global in the source ladder; add `'store'` to `HandoffThresholdSource`. Update the caller `src/commands/agent.ts:66` to `await`. Extend `test/core/agent-context.test.ts` with a store-source case and confirm existing cases unchanged.

## 5. Root-selection notice

- [x] 5.1 In `src/core/root-selection.ts`: replace the `ignored-store-pointer` member of `RootSelectionNotice` with two kinds — `inheriting-store-config { filePath, storeId }` and `inactive-store-pointer { filePath, storeId }` (design D5). In `resolveNearestOrDeclaredRoot`'s planning-shape branch, when a well-formed pointer exists, check `listRegisteredStores(pathOptions)` (function is already async; thread `globalDataDir`): registered → emit `inheriting-store-config`; unregistered → emit `inactive-store-pointer`. Update `defaultRootSelectionReporter` wording: inheriting = "planning stays local; configuration inherits from store '<id>'"; inactive = warning that the declaration has no effect until the store is registered (`rasen store register <path> --id <id>`). Root choice behavior itself is unchanged (local root still wins).
- [x] 5.2 In `src/commands/pipeline-messages.ts`: replace the `ignoredStorePointerWarning` entry in the params type map, the key list, and the `formatPipelineRootSelectionNotice` branch with the two new keys (`inheritingStoreConfigNotice`, `inactiveStorePointerWarning`), and in `src/locales/en.json`, `src/locales/zh-cn.json`, `src/locales/ja.json` replace the old key with both new keys (all three catalogs in lockstep, D5 risk note). zh-cn wording for inheriting: "…声明了 Store '{store}'：规划保持本地，配置从该 Store 继承。"
- [x] 5.3 Update `test/core/root-selection.test.ts` (and `test/commands/pipeline-messages.test.ts` / `pipeline-store-root-selection.test.ts` if they assert the old notice): both-present + registered store → inheriting notice, root still `nearest`; both-present + unregistered → inactive warning; pointer-only repos unchanged; malformed-beside-planning still silent.

## 6. Config HTTP API

- [x] 6.1 In `src/core/config-api/wire-types.ts`: `WireConfigEntry.scopeValues` becomes `{ global?, store?, project? }` (ConfigSource/ConfigScope widen automatically via the imported types). Add `export interface StoreLayerRef { id: string; root: string }` for the response-level store reference.
- [x] 6.2 In `src/core/config-api/serialize.ts`: extend the warnings loop to `['global','store','project']` so an invalid on-disk store value surfaces as a warning (never rewritten).
- [x] 6.3 In `src/core/config-api/router.ts`: introduce a shared config-context resolver per design D6 — parses optional `space` (query or body) and `project` selectors; both present → 400 `bad_request`; `space` resolves via `resolveSpaceSelector` (errors pass through: `invalid_space`/`space_not_found`/`space_unavailable`); result is either a project context `{ kind:'project', root, ref, storeLayer }` (with `storeLayer = await resolveConfigStoreLayer(root)`, also applied to `?project=` and launch-project fallback paths) or a store context `{ kind:'store', storeId, storeRoot }`. `handleListConfig`/`handleGetConfigKey` call `resolveEffectiveConfig` with `{ projectRoot, store: storeLayer }` for project contexts and `{ store: { storeId, storeRoot } }` for store contexts, and add `store: StoreLayerRef | null` to the response bodies.
- [x] 6.4 In `src/core/config-api/router.ts` writes (`handlePutConfigKey`/`handleDeleteConfigKey`): accept `scope: 'store'` (body enum and `scope=` query); store scope requires a store context (else 400 `invalid_scope` with fix "address the store space (`?space=store:<id>`)"); `scope: 'project'` at a store context → 400 `invalid_scope` (fix: use `"store"`); store writes call `updateProjectConfigKey(storeRoot, key, value|undefined)`; re-resolution after a write uses the same context options so the returned entry shows source `'store'`. Generalize `validateWriteKey`'s wrong-scope hint to list the key's actual settable scopes (registry lookup) instead of the binary other-scope guess.
- [x] 6.5 Extend `test/core/config-api/` router/serialize tests: store-space list/get (store values under `scopeValues.store`, no `scopeValues.project`, response `store` ref set); project with inheritance via plain `?project=` shows `scopeValues.store` + store ref; no-pointer project has null store ref and no store values; `space=project:` equivalent to `?project=`; both selectors → 400; store-scope write lands in the store's `rasen/config.yaml` (comment-preserving) and re-resolves with source `store`; store write at project context → 400; project write at store context → 400; global-only key with `scope:'store'` → error naming settable scopes; unset store scope reverts to global; invalid on-disk store value → warning. Space-selector error vocabulary asserted (`invalid_space`, `space_not_found`).

## 7. CLI display surfaces

- [x] 7.1 In `src/commands/config.ts`: in `printEffectiveConfigView` and the interactive editor's entry load, resolve `const storeLayer = await resolveConfigStoreLayer(projectRoot)` and pass `{ store: storeLayer }` to `resolveEffectiveConfig` (make the two call paths async as needed — the command actions are already async). Add the `store` source label to the editor messages: `configEditor.editor.source.store` = "store" / "store" / "store" in `src/locales/{en,zh-cn,ja}.json` (keep it the bare scope word, matching the existing entries). The editor's write targets stay global/project only (design Non-Goal — no `--scope store` in W1); a store-sourced value simply displays its source like `global` does.
- [x] 7.2 Update `test/core/commands/config*.test.ts` (effective view + editor): a store-inherited value renders with the `store` source label; no behavior change without inheritance.

## 8. Templates and docs

- [x] 8.1 In `src/core/templates/workflows/auto.ts` §0.5 and §0.6: insert the store layer into both precedence sentences — after the project-config step add "(2.5) `autopilot.gates`/`autopilot.selection` in the inherited store's config (when `rasen/config.yaml` declares `store:` beside local planning) -> that value, source `store`" (renumber the following steps; keep the existing sentence style; do NOT touch the vet-gate prose). Then update the `rasen-auto` golden-master hash in `test/core/templates/skill-templates-parity.test.ts` (~line 159): run `pnpm vitest run test/core/templates/skill-templates-parity.test.ts`, take the actual hash from the failure output, paste it.
- [x] 8.2 Add a CHANGELOG.md entry under a `## Unreleased` heading (create the heading if absent; do NOT bump any version): "Store configuration scope" — the chain `env > project > store > global > default`, and a **behavior change** callout: a project declaring `store: <id>` alongside its own local planning previously warned that the declaration was ignored; it now inherits configuration from that store (planning stays local; the root-selection notice wording changed accordingly; remove the `store:` line or set project-level values to opt out).

## 9. Verification

- [x] 9.1 Build (`node build.js`) and run the focused suites: `pnpm vitest run test/core/config-keys.test.ts test/core/effective-config.test.ts test/core/project-config.test.ts test/core/config-api test/core/root-selection.test.ts test/core/agent-context.test.ts test/core/pipeline-registry test/core/templates/skill-templates-parity.test.ts test/commands/pipeline-messages.test.ts test/commands/pipeline-store-root-selection.test.ts`.
- [x] 9.2 Run the full `pnpm test`; on Windows, isolate-rerun any CLI-spawning EBUSY/timeout flakes before calling a regression (known flake pattern). UI package tests must pass untouched (`pnpm --filter @atelierai/rasen-ui test`) — W1 changes no UI code; if a UI test fails, the wire change leaked further than D6 intends: stop and reassess rather than patching the UI (that is W2's boundary).
- [x] 9.3 `rasen validate ui-config-redesign-store-scope` passes; smoke-check by hand: in a temp repo with planning shape + `store:` pointing at a registered temp store, `rasen config` shows the inherited value with source `store`, and the CLI prints the inheriting notice instead of the ignored warning.
