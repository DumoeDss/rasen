## 1. Guards first

- [x] 1.1 Add failing guards pinning, for BOTH the finalize and create intents, that the `split_planning_truth` refusal names the offending project root and carries a `fix` naming adopt (and eject as the alternative)
- [x] 1.2 Demonstrate each guard red against the current message, capturing the output as evidence

## 2. Fix

- [x] 2.1 Give both `split_planning_truth` throw sites a `fix` naming the repair, and include the tested project root in the message
- [x] 2.2 Confirm the refusal stays fail-closed: same code, same trigger conditions, no force/override path added

## 3. Verify

- [x] 3.1 Guards green; run the store-planning suites plus the resolver's own neighbours
- [x] 3.2 `tsc --noEmit`, `rasen validate --strict`, and `git diff --check` clean
