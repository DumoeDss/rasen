# Task 6.2 — dogfood pilot: run to the finalization gate, blocked there (2026-08-27)

## What was done

The pilot target `document-multi-project-issues` was found at 0/3 tasks, but the
work itself was already committed in the execution repo (`rasen-site` 2dc9e31):
`docs/issues.md` exists, carries the three load-bearing facts, and is linked
from that repo's docs-area index. Only the ticks were missing.

Task 2.1 was then actually performed rather than assumed, and it earned its
place: every CLI shape on the page was checked against real rasen 0.2.0, and one
did not match — the publish example passed `--revision 0001`, a flag
`rasen store issue plan` does not have (the revision id is assigned BY the
command; the real input is `--from-file` / `--from-portfolio` /
`--from-decomposition`). Corrected in `rasen-site ad76923`. The site build
passes (`built dist/ — version 0.1.7`), run with `RASEN_REPO_DIR` pointed at a
tagged checkout because dev/0.2.0's CHANGELOG carries no `## 0.1.7` heading.

Ticks recorded in the planning worktree and committed there (`dmpi 0776ddd`).
From the pair the change then reads `✓ Complete` and `validate --strict` valid.

## Where it stopped, and why that is NOT change A's gap

    $ cd E:/rasen-pairs/dmpi
    $ rasen archive document-multi-project-issues --outcome landed --yes
    Using Rasen root: issue-registry (E:\rasen-pairs\dmpi)
    Task status: ✓ Complete
    Specs to update:
      multi-project-issue-docs: create
    ✖ Error: Store and project-local planning both exist; Change finalization is blocked.

Scope resolution SUCCEEDED — the JSON payload resolves
`store=f76edc31…, project=6ca78b98…, targetLine=line-0.2`, which is precisely
what the three refusals this change fixes used to prevent. The block is a
different, pre-existing gate: `split_planning_truth`
(`resolver.ts:2220-2226`), raised because `selectedProjectRoot !== null &&
discoveredLocalPlanning` — the `rasen-site` project is Store-bound AND still
carries a TRACKED project-local planning tree (`rasen/changes/` with three
active changes plus an archive, and `rasen/specs/`), including its own copy of
`document-multi-project-issues`.

Clearing it means deciding which tree is the truth for `rasen-site` and deleting
the other — tracked content in a third repository. That is an operator decision
about a different project, not something this change may take unilaterally, so
the pilot stops here rather than forcing the gate.

Fail-closed held: the Store main checkout is clean at `eb39730`, the pair carries
only the legitimate tick commit, the change is still active, and no archive
transaction was started.

## Finding worth carrying forward

`split_planning_truth` is raised with `{ target: 'project.planning' }` and NO
`fix` field, so the operator is told what is wrong and nothing about the repair.
That is the same defect class `rehearse-legacy-store-layout-migration` triaged
as (b) correct-but-illegible, and it is not covered by the new
`store-layout-migration` spec because it lives in the planning resolver.
