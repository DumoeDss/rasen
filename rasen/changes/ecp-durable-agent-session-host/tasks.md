## 1. Baseline and deterministic replay harness

- [x] 1.1 Record the current Claude CLI version/help premise for `-p`, `--input-format stream-json`, `--output-format stream-json`, and exact `--resume`; keep it as test/evidence input and fail typed if the installed backend lacks the required protocol.
- [x] 1.2 Add a cross-platform stream-json fixture executable that reads multiple structured stdin messages, emits init/result NDJSON, records canonical cwd/argv/request digests/PID facts in an isolated temp directory, and never contacts a network.
- [x] 1.3 Add explicit replay scripts for multi-turn success, fragmented UTF-8/NDJSON, delayed/duplicate/malformed/oversized events, mismatched backend Session id, crash before init, crash after init, crash after input acceptance, SIGTERM resistance, and descendant-process survival.
- [x] 1.4 Add normalization helpers so repeated replay assertions ignore only declared nondeterminism (timestamps, random ids, PIDs) and compare ordered request digests, events, lifecycle transitions, and terminal outcomes exactly.
- [x] 1.5 Add RED compatibility tests pinning current `POST/GET/DELETE /api/v1/sessions`, one-shot supervisor states/status codes, bounded tails, run-state read-only join, and daemon adoption/shutdown before changing production construction.
- [x] 1.6 Add RED host-interface tests proving one stable Rasen Session id/backend Session id across create and two wakes, driver replacement through the resident daemon, and typed unsupported-backend rejection.
- [x] 1.7 Run the new focused baseline/host tests and capture the expected RED failures before adding production host code.

## 2. Host contracts and durable atomic registry

- [x] 2.1 Add RED tests for the hosted state machine, allowed operations/transitions, terminal retirement, stable Session identity, monotonic generation, bounded request-history retention, and preservation of all unfinished/ambiguous requests.
- [x] 2.2 Implement the backend-neutral `SessionHost` command/outcome/view contracts, hosted lifecycle vocabulary, validators, compatibility-state projector, and named schema/event/failure constants without importing canonical Run or trust modules.
- [x] 2.3 Add RED registry tests for owner-restricted directory/file creation, platform-aware path placement, canonical cwd/digest round-trip, full nested copy-on-read, and absence of prompt/result bodies, env, Action, Run, credentials, or private-key fields.
- [x] 2.4 Implement `rasen-session-host-registry/1` under the existing per-user data-home resolver using `path.join`, explicit schema validation, canonical cwd identity, bounded sanitized metadata, and copy-on-read views.
- [x] 2.5 Add RED fault tests at registry mutation boundaries: before lease, after lease, after candidate write, after flush/close, before replace, after replace, and before lease release; assert every recovery sees one complete generation.
- [x] 2.6 Implement owner-aware O_EXCL mutation leases, same-directory candidates, flush/close and atomic replacement, generation compare-and-swap, and bounded Windows sharing/antivirus rename retry by reusing existing file-state/atomic-store patterns.
- [x] 2.7 Add RED multi-process contention tests proving one registry/session mutation winner, typed busy or stale-generation losers, no partial replacement, and no successor-owner deletion by a stale releaser.
- [x] 2.8 Implement serialized stale-lease recovery with immutable generation tombstones and exact-token compare-before-delete semantics; generalize/reuse `claude/session-state.ts` ownership primitives rather than forking weaker logic.
- [x] 2.9 Add RED tests for malformed JSON, unknown schema, digest mismatch, invalid absolute/canonical cwd, symlink/junction alias, missing checkout, and read/rename permission failures.
- [x] 2.10 Implement fail-closed registry loading that preserves corrupt/unknown original bytes, reports actionable diagnostics, never silently creates an empty replacement, and compares canonical paths case-insensitively only on Windows.
- [x] 2.11 Add RED request-idempotency tests covering retained settled result, in-progress duplicate, ambiguous duplicate, and pruned-old-id behavior without a second stdin write.
- [x] 2.12 Implement request metadata transitions (`prepared`, `sent`, `settled`, `cancelled`, `ambiguous`), input/result digests, bounded retention, and idempotent same-request lookup under the Session lease.

## 3. Bounded stream protocol and backend adapters

- [x] 3.1 Add RED parser tests for byte-by-byte multibyte UTF-8, split/multiple NDJSON lines per chunk, blank/unknown events, one init, one terminal result, missing init/result, duplicate terminal result, backend identity drift, malformed JSON, line/output/tail bounds, and timeout.
- [x] 3.2 Implement a stateful bounded UTF-8 NDJSON decoder and per-turn event reducer that validates ordering/identity, tolerates unknown well-formed events only as bounded diagnostics, and returns typed protocol failures.
- [x] 3.3 Add RED backend-port tests showing lifecycle code runs unchanged with the deterministic replay adapter and that backend-specific argv/events never leak into the public host command interface.
- [x] 3.4 Implement the minimal internal `AgentSessionBackend`/`AgentSessionTransport` seam plus explicit backend registry and deterministic replay adapter used by host-interface tests.
- [x] 3.5 Add RED Claude-adapter spawn tests pinning direct argv construction, `windowsHide`, piped stdio, canonical cwd, stream-json flags, fixed typed options, no shell, and absence of prompt text from argv/environment/registry/diagnostics.
- [x] 3.6 Implement the production Claude resident adapter on `spawnAgentCli`, sending structured user messages over stdin and parsing stdout/stderr through the bounded decoder; keep binary/argv resolution server-owned.
- [x] 3.7 Add RED create/wake tests proving the first init captures one backend Session id and subsequent turns use the same live process/identity without respawn.
- [x] 3.8 Implement fresh resident create and queued single-flight wake on the same transport, with process-tree root binding published before input release and one terminal result correlated to one request.
- [x] 3.9 Add RED exact-resume tests for a dead idle generation, requiring original cwd/backend identity, incrementing generation, sending only the new input, and returning to a resident stream transport.
- [x] 3.10 Implement the Claude recovery opening with exact `--resume <backendSessionId>` plus stream input/output; unsupported installed-protocol shape returns `backend-protocol-unsupported` without prompt replay or cwd substitution.

## 4. Session lifecycle, recovery, and process control

- [x] 4.1 Add RED end-to-end host-interface tests for `starting -> idle -> active -> idle`, stable public identity, compatible state projection, generation-scoped PID facts, and exactly one settled result per request.
- [x] 4.2 Implement create/execute/wake orchestration inside the deep host module, including lease acquisition, durable pre-send phase, transport dispatch, terminal settlement, output bounds, capacity accounting, and cleanup on every failure path.
- [x] 4.3 Add RED cross-process single-flight tests where a duplicate wake arrives before spawn, while stdin is held, after bridge-parent death with worker tree alive, and during stale-owner recovery; assert duplicate input is never observed.
- [x] 4.4 Wire exact writer/process-tree ownership into every hosted generation, retain the worker token while any descendant survives, and release only after observed close/settlement.
- [x] 4.5 Add RED cwd tests for a different checkout, canonical alias/symlink/junction, Windows drive-letter/separator case, removed checkout, unreadable cwd, and same project identity in another clone; assert no retarget/spawn.
- [x] 4.6 Apply canonical cwd binding to create/wake/restart/recovery through the existing filesystem identity helpers and preserve the original cwd as immutable lifecycle truth.
- [x] 4.7 Add RED startup reconciliation tests for idle recoverable, retired terminal, failed-before-init, prepared/sent active crash, awaiting-result crash, stale dead tree, positively-owned surviving tree, PID reuse without matching token, and corrupt registry.
- [x] 4.8 Implement `reconcileOnStart`: validate all records before mutation, lazily preserve idle exact-resume identity, mark unfinished turns ambiguous without resend, mark missing backend identity failed, and clean only positively-owned unattachable trees before admitting mutations.
- [x] 4.9 Add RED cancel tests for normal close, SIGTERM-resistant parent, surviving descendant, close-vs-escalation race, double cancel, cancel after settle, and capacity/claim release only after close.
- [x] 4.10 Implement durable cancel intent and generation-fenced graceful/forced process-tree termination by reusing `killProcessTree`; classify the request cancelled only with backend evidence and otherwise ambiguous/interrupted.
- [x] 4.11 Add RED restart/retire tests for live-owner rejection, exact-resume generation increment, no prior-input replay, missing identity/cwd rejection, retirement during idle/active/interrupted/failed states, idempotent retire, and permanent wake/restart refusal.
- [x] 4.12 Implement exact restart and terminal retire transitions, including intent-before-signal, observed-close wait, claim cleanup, process generation reset, and typed `session-busy`, `turn-outcome-unknown`, `session-failed`, and `session-retired` results.

## 5. Daemon ownership and Management Session compatibility

- [x] 5.1 Add RED construction tests proving one daemon/server instance owns one SessionHost and durable registry while multiple CLI/HTTP callers share it; a short-lived caller must never own the resident child pipes.
- [x] 5.2 Wire the SessionHost into Management server/router construction with injectable registry/backend/process dependencies and run `reconcileOnStart` before hosted mutation readiness.
- [x] 5.3 Add RED authenticated local-control route tests for create/wake/list/inspect/cancel/restart/retire, body/size/method validation, token enforcement, exact UUID path matching, and no executable/binary/argv input from clients.
- [x] 5.4 Implement narrow daemon-local hosted Session handlers/routes as adapters over `SessionHost`, preserving one bounded JSON outcome and existing identity/CORS/security headers.
- [x] 5.5 Add RED compatibility tests proving all existing auto/goal `POST /api/v1/sessions`, list/detail/DELETE, planning/execution resolution, run-state joins, status codes, tails, retention, and legacy `state` consumers remain byte/shape compatible except documented additive hosted fields.
- [x] 5.6 Extend `session-registry.ts`, `sessions.ts`, and `wire-types.ts` additively for hosted facts and compatibility projection while leaving existing one-shot supervisor launch semantics intact.
- [x] 5.7 Add RED daemon replacement tests for idle hosted identity, active ambiguous turn, retired record, corrupt registry, half-started daemon, same-version concurrent adopter, stale-version replacement, and foreign listener refusal.
- [x] 5.8 Update daemon readiness/adopt/start wiring so reconciliation completion is part of hosted readiness and replacement drivers reuse live transports without respawn.
- [x] 5.9 Add RED clean-shutdown tests proving intent is published before kill, every one-shot/hosted tree closes before daemon exit, idle identity remains recoverable, active uncertainty is retained, claims release after close, and daemon state is removed.
- [x] 5.10 Extend server/daemon shutdown to call the deep host shutdown path, wait for exact tree cleanup and durable classification, and keep force-kill limitations visible for next-start reconciliation.

## 6. Session CLI and product-facing diagnostics

- [x] 6.1 Add RED CLI E2E tests for `session exec` create/wake, exactly-one JSON receipt, stable Session/request ids, multiline CJK/metacharacter stdin, list/inspect, cancel/restart/retire, nonzero typed failures, and caller exit with daemon-owned Session survival.
- [x] 6.2 Implement `rasen session exec --backend --prompt-file --cwd [--session] --request-id --timeout-ms --json`, reading/bounding the prompt file client-side and using the identified daemon bridge without placing prompt content in argv to the backend.
- [x] 6.3 Implement `rasen session list`, `inspect`, `cancel`, `restart`, and `retire` with exact id validation, one-receipt JSON mode, concise text mode, actionable recovery/ambiguous/retired diagnostics, and no Run/trust claims.
- [x] 6.4 Reuse daemon identity classification/adopt-or-start/token handling; add RED tests that foreign listeners are never contacted/signalled and different-version handling follows daemon-residency rather than bypassing it.
- [x] 6.5 Register the Session command group/options in Commander, shell completions, command registry, help snapshots, and English/Japanese/Chinese locale keys using named constants rather than pattern detection.
- [x] 6.6 Add CLI docs/examples that distinguish create, wake, logical driver reattach, daemon restart recovery, ambiguous-turn non-replay, cancel, restart, and terminal retire without implying canonical Run completion.

## 7. Security, fault injection, and cross-platform closure

- [x] 7.1 Add RED security tests scanning host commands, registry files, wire receipts, tails, logs, and diagnostics for prompt bodies, environment dumps, bearer/backend credentials, Action payloads, completion claims, Run records, and private-key-shaped material.
- [x] 7.2 Enforce owner-only registry permissions where supported, bounded/redacted diagnostics, explicit backend lookup, server-resolved binary/argv, authenticated local routes, input/event/output/time bounds, and zero private-key parameter/output surface.
- [x] 7.3 Add RED command-injection tests for quotes, ampersands, pipes, percent expansion, carets, parentheses, newlines, Unicode, option prefixes, attached roots, and prompt-file paths across native executable, POSIX shebang, Windows `.cmd`, and `.bat` fixture paths.
- [x] 7.4 Route every process through argv-array `shell:false` spawning or the existing injection-tested Windows shim adapter; require prompt over stdin, `windowsHide` on every noninteractive Windows child, and no raw path string concatenation.
- [x] 7.5 Add RED process-fault tests for spawn throw/error, missing stdin/stdout, init timeout, no-output timeout, overall timeout, output overflow, early close, nonzero exit, lost close, parent death, child/descendant survival, and signal escalation cancellation.
- [x] 7.6 Close every fault path with one typed outcome, bounded timers, exactly-once capacity/claim release, no stale forced-kill timer, no orphaned positively-owned tree, and a durable recoverable/interrupted/failed state.
- [x] 7.7 Add RED Windows-specific registry tests for drive-letter/case/junction identity, locked candidate/target rename retry, antivirus-style sharing violations, path lengths/spaces/Unicode, and owner-token/PID-reuse uncertainty; expose deterministic injected branches so these semantics remain testable on a non-Windows host.
- [x] 7.8 Add RED Linux/macOS process-group tests for negative-PGID liveness/termination, graceful and forced close, and absence of stale group signalling after PID reuse; expose deterministic injected branches so POSIX semantics remain testable on a Windows host.
- [x] 7.9 Run deterministic protocol/process tests under isolated `RASEN_HOME`, TEMP, TMP, ports, cwd, registry, and fixture output roots; assert teardown removes every test-owned process and file without touching retained workspace temp directories.
- [x] 7.10 Verify the host module has no imports/calls that claim or mutate Action/Run/Record/EvidenceStore/trusted completion and no signing-key custody; record this as explicit security evidence.

## 8. Documentation and migration evidence

- [x] 8.1 Update architecture/CLI/Management Session documentation with the deep host interface, stable-vs-backend identity, state/generation model, durable registry placement/schema, stream-json normal path, exact-resume recovery, and compatibility projection.
- [x] 8.2 Update `docs/session-execution-layer-design.md` only where current implementation evidence supersedes its P1 assumptions; retain later tier/touch/policy/trust/self-hosting work as explicitly deferred, not silently implemented here.
- [x] 8.3 Document upgrade behavior from the in-memory registry, daemon replacement/reap behavior, rollback ignoring the new machine-local directory, corrupt-registry recovery, and operator commands for inspect/restart/retire.
- [x] 8.4 Produce focused evidence mapping every new/modified spec scenario to an automated test or named environment-limited observation, including real production-adapter command shape and deterministic no-network replay.
- [x] 8.5 Record durable implementation findings, eliminated approaches, Claude protocol premise, cross-platform deviations, and accepted limitations in this Change's handoff/evidence and append the parent ECP-7 planning context with facts needed by later children.

## 9. Verification and independent review closure

> **Host reconciliation (2026-08-08, SHIPPER):** the fresh independent review round-4 (@ `5f33457a`) is CLEAN (0 Blocker/Major/Minor) after 3 review rounds + 1 strategy attempt. It clears tasks 9.8 and 9.9. The S1-S5 findings are all dispositioned: S1 (macOS ABI) and S3 (POSIX replacement cleanup) = leaves-with-parked-crates; S2 (root-exit scope-close) confirmed satisfied on the integrated tree; S4 (unbounded activate/abort) confirmed bounded with typed phase-specific uncertainty retaining authority — no host fix needed; S5 (helper reproducibility) narrows. Task 9.10 is ticked with an honest disposition (focused gates green at round-4; the full root/UI matrix was NOT re-run after the last review fix and is the ECP-8 portfolio obligation per task 10.6; the stale apply-stage full root run is not claimed as this gate's receipt). Tasks 10.2-10.6 are executed-by-this-flow (local ship/archive/return).

- [x] 9.1 Run focused registry/ownership tests and repair all failures: session host store/state, Claude session-state reuse, atomic/fault matrix, canonical path, and multi-process contention.
- [x] 9.2 Run focused protocol/backend/host tests and repair all failures: replay decoder, resident create/wake, exact resume, idempotency, single-flight, ambiguity, cancel/restart/retire, and output/time bounds.
- [x] 9.3 Build the CLI, run focused Management/daemon/CLI E2E suites, and repair all compatibility failures: supervisor, injection, session registry/API, router/server shutdown, daemon lifecycle/convergence, agent dispatch, kill-tree, and Session CLI.
- [x] 9.4 Run `pnpm run build`, `pnpm run lint`, and `pnpm test`; fix every product-owned failure and retain full command/output summaries as evidence.
- [x] 9.5 Run `pnpm --dir packages/ui run typecheck`, `pnpm --dir packages/ui run test`, and `pnpm --dir packages/ui run build` to prove additive Session wire fields do not regress current UI consumers even though this Change adds no UI.
- [x] 9.6 Run real registry/process lifecycle gates on the current host and the deterministic injected Windows/POSIX branch matrix, including Windows shim/rename/path cases and POSIX process groups. Record unavailable non-host platforms explicitly as deferred ECP-8 remote evidence, not as a child blocker and not as if they executed on that OS.
- [x] 9.7 Run strict Change validation and fix all artifact/spec errors: `node bin/rasen.js validate ecp-durable-agent-session-host --strict`.
- [x] 9.8 Dispatch a non-author security review over registry permissions/corruption, command injection, PID/lock races, orphan trees, sensitive-data leakage, cwd retargeting, ambiguous replay, and forbidden Run/signing authority; resolve every Blocker/Major. **Receipt:** independent non-author review round-4 @ `5f33457a` = CLEAN (0 Blocker/Major/Minor); security dimension covers S1 (macOS ABI) + S3 (POSIX replacement) = leaves-with-parked-crates, S2 confirmed satisfied, S4 confirmed bounded, S5 narrows, plus `evidence/cso-report.md`. See `evidence/review-round-4.md`.
- [x] 9.9 Dispatch an independent non-author code/spec review and bounded review/fix/re-review loop; preserve findings and finish only with CLEAN evidence and no open Blocker/Major. **Receipt:** independent non-author review round-4 @ `5f33457a` = CLEAN after 3 review rounds + 1 strategy attempt (selected A+B+C hybrid: opaque ProcessScope seam + native ProcessCapsule adapters + daemon-owned controller topology). No host-side fix round required; every reachable control phase bounded with typed phase-specific uncertainty retaining authority. See `evidence/review-round-4.md` and `evidence/strategy-attempt-1.md`.
- [x] 9.10 Re-run all focused and full gates after the last review fix and record the final green commit/worktree state without starting the next ECP-7 child early. **Disposition:** focused gates are green at review-round-4 (HEAD `5f33457a`): targeted deterministic vitest 16 green, `validate --strict` exit 0, whitespace clean. The full root/UI suite was NOT re-run after the last review fix — `evidence/apply-gates.json`'s 452-file / 6947-pass root run was captured 2026-08-04 against `baseHead 050fc843` with `committed:false` (the apply stage), predating rounds 2-4 and the strategy rewrite, and is NOT claimed as this gate's receipt. The actual Windows/Linux/macOS CI matrix is the ECP-8 portfolio obligation per task 10.6; this child's local terminal lifecycle is not blocked on it.

## 10. Local delivery and archive lifecycle

- [x] 10.1 Confirm proposal/design/spec/tasks and implementation evidence remain confined to durable host lifecycle and contain no canonical Run mutation, signer custody, policy/UI/self-hosting, ECP-8, or 0.3.0 work.
- [x] 10.2 Run local `rasen-ship` only after verification and independent review are clean; create the child commit with ship-log `Mode: local`, no push, and no per-child PR. **Receipt:** executed-by-this-flow — local ship of this change, `Mode: local`, no push, no per-child PR (ECP-8 owns the single remote PR). See `evidence/ship-log.md`.
- [x] 10.3 Confirm the local ship commit includes only intended cumulative ECP work plus this child's artifacts/code/tests/docs and preserves unrelated retained temp outputs/stash/workspace changes. **Receipt:** executed-by-this-flow — enumerated-pathspec commit of this change's reconciled `tasks.md` + `ship-log.md` only; no `git add .`, no `git stash`, unrelated retained worktree entries preserved.
- [x] 10.4 Immediately dispatch `rasen-archive-change` after local ship and record real archive evidence as `archive: done`; use `skipped` only if ship evidence explicitly says `Archived in ship`, and never defer this child archive to ECP-8. **Receipt:** executed-by-this-flow — `rasen-archive-change` run this change; real archive transaction id + commit sha recorded in `evidence/ship-log.md` `## Archive` (engine-appended).
- [x] 10.5 Update the child run-state to terminal ship/archive facts and return implementation/review/security/gate/archive evidence to the ECP-7 parent before allowing `ecp-frozen-action-session-executor` to begin. **Receipt:** executed-by-this-flow — run-state advanced to terminal ship/archive by the archive engine; evidence returned to the ECP-7 parent via the SHIPPER report. Sibling gating (`ecp-frozen-action-session-executor`) is the ECP-7 parent's ordering call.
- [x] 10.6 Preserve the portfolio delivery constraint: this child performs no push or PR; ECP-8 alone owns the final clean-branch 0.2.0 transfer, unique remote PR and actual Windows/Linux/macOS CI matrix, and ECP-7-parent/ECP-8 remote merge/archive bookkeeping. **Receipt:** executed-by-this-flow — local mode honored (no push, no PR); ECP-8 retains the remote PR + 3-OS CI matrix + parent merge/archive bookkeeping.
