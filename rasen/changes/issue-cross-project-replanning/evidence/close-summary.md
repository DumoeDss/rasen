# Portfolio close — issue-cross-project-replanning (LEAD, 2026-08-22)

Delivery: PR #174 MERGED (merge `e488d95a`, CI first-run all-green — the P4 budget
fixes holding). Three children shipped+archived (3f065496/abc2ad14,
c0ace35e/ef6e70cf, 60366c1f/9c9c711b); g-003 first-pass clean, g-002 round-1 doc-fix,
g-001 round-1+1b spec-truth.

## Issue #4 golden close (LEAD-executed)

1. The finale node was already bound to its dogfood ACTIVE instance in revision 0002
   (the implementer's staging) — the LEAD's redundant archive seed was withdrawn
   uncommitted (M-1 shape avoided: two instances of one alias).
2. The cleaner had discarded the finale's ephemera (sanctioned); the LEAD reconstructed
   the terminal run-state FROM THE ARCHIVED EVIDENCE FACTS (stages done, commits named)
   — reconstruction documented inline, not fabrication.
3. Gate → accept: conditions 0001 (the implementer's staged publish, store 2b3afab)
   → gate 3/3 · waiting-human · 0 problems → accept → **done · healthy · 3/3**
   (store commit 3af7041).
4. Attention scan after close: **4 Issues scanned, none need attention** — the machine's
   registry now holds four closed Issues, each closed through its own golden loop.

The P5 exit criteria (roadmap §8) evidence set lives in the g-003 archive's evidence/
(p5-exit-criteria.md with the six receipts).
