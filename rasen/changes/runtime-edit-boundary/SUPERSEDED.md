# Superseded

This change is superseded by `retire-runtime-edit-boundary`.

The runtime command, state/evaluation code, and Claude/Codex hook design in
this directory are historical evidence only. Do not resume this change, sync
its delta specs into the main specification set, or archive it as current
product direction. The successor removes the live capability while retaining
bounded upgrade cleanup for artifacts generated by released versions.

The unchecked verification items in `tasks.md` are not applicable because the
feature is being retired; they are deliberately left unchecked rather than
misrepresented as completed.
