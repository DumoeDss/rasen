/**
 * Shared store-selection guidance for skill template workflows.
 *
 * Interpolated into every workflow's instructions so generated skills
 * consistently teach how to target a registered store with `--store <id>`,
 * or a registered project with `--project <id>`.
 */
export const STORE_SELECTION_GUIDANCE = `**Store selection:** If the user names a Store or the work lives in one, run \`rasen store list --json\` to discover registered Store and project identities, then preserve the complete planning selection on commands that read or write specs and Changes (\`new change\`, \`status\`, \`instructions\`, \`list\`, \`show\`, \`validate\`, \`archive\`, \`doctor\`, \`retain prepare\`, and the top-level \`context\`). \`--store <id>\` selects the Store namespace, \`--project <id>\` selects a project dimension, and \`--target-line <id>\` selects its stable planning line. The selectors are orthogonal: use \`--store S --project P --target-line L\` together for Store v2 project work; \`--project P\` alone continues to select the registered-project namespace and follows its verified planning binding. A Store and project may share one display id because their namespaces remain distinct. The \`rasen pipeline\` inspection group (\`pipeline list\`, \`pipeline show\`, \`pipeline agents\`, \`pipeline classify\`, \`pipeline resume\`) accepts the same selectors; thread the complete selection onto \`pipeline resume <change>\` so Change lookup and run-state recovery use the same project scope. On \`retain prepare\`, these flags select the planning scope only; knowledge-owner selectors remain the separate \`--owner-store\`/\`--owner-project\`. Commands outside those groups do not take planning selectors—in particular \`rasen agent context\` is not the top-level \`rasen context\`. Hints already carry the complete reproducible selection; preserve it on follow-ups. Without explicit selectors, commands use the shared resolver's frozen session, association, binding, or nearest standalone facts.`;
