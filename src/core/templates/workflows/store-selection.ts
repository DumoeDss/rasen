/**
 * Shared store-selection guidance for skill template workflows.
 *
 * Interpolated into every workflow's instructions so generated skills
 * consistently teach how to target a registered store with `--store <id>`.
 */
export const STORE_SELECTION_GUIDANCE = `**Store selection:** If the user names a store (a store is a standalone Rasen repo registered on this machine) or the work lives in one, run \`rasen store list --json\` to discover registered store ids, then pass \`--store <id>\` on the commands that read or write specs and changes (\`new change\`, \`status\`, \`instructions\`, \`list\`, \`show\`, \`validate\`, \`archive\`, \`doctor\`, and the top-level \`context\`). The \`rasen pipeline\` inspection group (\`pipeline list\`, \`pipeline show\`, \`pipeline agents\`, \`pipeline classify\`, \`pipeline resume\`) also accepts \`--store <id>\` and resolves its root exactly like \`validate\` — in a store-scoped run you MUST thread \`--store <id>\` onto \`pipeline resume <change>\` so it reads the change's run-state from the store's change directory, not the cwd. Commands outside those two groups do not take the flag — in particular \`rasen agent context\` (the agent-runtime probe) is NOT the same command as the top-level \`rasen context\` and does NOT accept \`--store\`; do not paste the flag onto it. Hints printed by commands already carry the flag; keep it on follow-ups. Without a store, commands act on the nearest local \`rasen/\` root.`;
