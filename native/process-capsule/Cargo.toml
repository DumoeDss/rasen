[package]
name = "rasen-process-capsule"
version = "0.2.0"
edition = "2021"
rust-version = "1.78"
license = "MIT"
publish = false

[profile.release]
codegen-units = 1
lto = true
opt-level = "z"
panic = "abort"
strip = "symbols"

[dependencies]
