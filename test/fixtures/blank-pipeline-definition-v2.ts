export const BLANK_PIPELINE_DEFINITION_V2_FIXTURE = Object.freeze({
  version: 2 as const,
  id: 'pipeline:blank-parity',
  sourceId: 'canvas:blank-parity',
  name: 'blank-parity',
  inputs: Object.freeze([]),
  artifacts: Object.freeze([]),
  outcomes: Object.freeze([]),
  declarations: Object.freeze([]),
  root: Object.freeze({
    nodes: Object.freeze([]),
    connections: Object.freeze([]),
  }),
});
