{
  "name": "openspec-telemetry",
  "version": "0.1.0",
  "private": true,
  "description": "Maintainer-owned Cloudflare Worker that ingests anonymous OpenSpec CLI usage events into Analytics Engine.",
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^4.86.0"
  }
}
